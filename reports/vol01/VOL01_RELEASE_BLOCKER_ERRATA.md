# Volume I — release-blocking mathematical erratum

**Status:** corrected.

The polynomial in `prob:i01-dossier-03` is changed from `2-3x+5x^2` to `2-3x-5x^2`. The coefficient equations are now

`a+c=2`, `a+b=-3`, `b-c=-5`,

and the third equation follows from the first two. Therefore the one-parameter family of representations is valid and the intended nonuniqueness lesson is preserved.
