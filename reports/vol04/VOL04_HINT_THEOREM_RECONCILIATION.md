# Volume IV — hint/theorem reconciliation

The professional-review source contains **744 exercise/hint pairs** (31 chapters × 24 exercises). `VOL04_HINT_RECONCILIATION.tsv` records every pair, and `VOL04_THEOREM_COVERAGE.tsv` classifies every theorem environment.

The Riemann mapping existence theorem is explicitly marked as a standard external theorem; every other theorem environment must contain a proof environment for this audit to pass.
