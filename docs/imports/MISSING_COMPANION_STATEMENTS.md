# Missing companion statement resolution

Content review of the post-split companion-recovery packet established that the
duplicate solution blocks for Exercises 4.1--4.4 do not have matching statement
blocks in the imported corpus.

The pass therefore records a terminal provenance disposition:

`MISSING_COMPANION_STATEMENT`

It does not invent or reconstruct the missing exercise statements and creates
no forced problem links.

The eight solution blocks are preserved, their prior candidate lists are
archived, and the four semantic solution units are removed from active orphan
ranking into an explicit `MISSING_COMPANION_STATEMENTS.tsv` report.
