\section{Vector Fields}
\label{sec:vector-fields}

Thus far, we have treated vectors as individual geometric objects. A
displacement vector specifies the position of one point relative to another, a
force vector acts on a particle, and a velocity vector describes the motion of
an object. In each case, a single vector is associated with a particular
location or event.

Many physical systems cannot be described by only one vector. The wind has a
different speed and direction at different places, gravitational attraction
changes with position, and the electric field surrounding a charge varies
continuously throughout space. To describe such systems, we introduce the
concept of a vector field.

\begin{definitionbox}{Vector field}
A vector field assigns a vector to every point in a region of space.
\end{definitionbox}

\subsection{Motivation}

Imagine standing on a hill and observing the wind. At one location the wind
blows gently from the west, a few metres away it is stronger, and farther uphill
it changes direction. There is not one wind vector but a collection of vectors,
one associated with every point. Together, these vectors form a wind field.

The same mathematical idea describes Earth's gravitational field, the electric
field surrounding a charged particle, the magnetic field produced by a current,
the velocity field of a flowing river, and the deformation field inside a solid.
Although these systems are physically different, they use the same language.

\subsection{Formal Definition}

A vector field on a region $\Omega\subseteq\mathbb R^3$ is a function
\[
\boxed{
\mathbf F:\Omega\longrightarrow\mathbb R^3
}
\]
that assigns a vector $\mathbf F(\mathbf r)$ to every position
$\mathbf r=(x,y,z)\in\Omega$.

In Cartesian coordinates,
\[
\boxed{
\mathbf F(x,y,z)
=
F_x(x,y,z)\,\mathbf e_x
+
F_y(x,y,z)\,\mathbf e_y
+
F_z(x,y,z)\,\mathbf e_z .
}
\]

The quantities $F_x$, $F_y$, and $F_z$ are no longer fixed numbers. They are
functions of position. Consequently, both the magnitude and direction of the
field may vary from point to point.

\subsection{Visualizing a Vector Field}

A continuous field contains infinitely many vectors, so a drawing can show only
a representative sample. At each selected point, an arrow indicates the local
value of the field. Its orientation shows direction and its length represents
magnitude.

\begin{figure}[htbp]
\centering
\begingroup
\tikzset{
  axis/.style={physics axis},
  vector secondary/.style={physics vector secondary},
  field/.style={field vector},
  force/.style={force vector},
  label/.style={visual label}
}
\input{chapters/chapter01_vectors/fig26_uniform_vector_field.tex}
\endgroup
\caption{A uniform vector field. Every sampled arrow has the same direction and
magnitude.}
\label{fig:uniform-vector-field}
\end{figure}

Increasing the sampling density may improve the visual approximation, but the
arrows remain samples of an underlying continuous function.

\subsection{Uniform and Non-Uniform Fields}

The simplest vector field is constant:
\[
\mathbf F(x,y,z)=(3,-1,2).
\]
Every point in space is assigned exactly the same vector. Uniform fields are
idealizations, but they are useful approximations over sufficiently small
regions. Examples include the gravitational field near Earth's surface, the
electric field between large parallel capacitor plates, and magnetic fields in
certain laboratory regions.

Most physical fields are non-uniform. Consider
\[
\boxed{
\mathbf F(x,y)=(-y,x).
}
\]
Its direction changes continuously with position.

\begin{figure}[htbp]
\centering
\begingroup
\tikzset{
  axis/.style={physics axis},
  vector secondary/.style={physics vector secondary},
  field/.style={field vector},
  force/.style={force vector},
  label/.style={visual label}
}
\input{chapters/chapter01_vectors/fig23_vector_field.tex}
\endgroup
\caption{The rotational field $\mathbf F(x,y)=(-y,x)$. The vectors circulate
counterclockwise around the origin.}
\label{fig:rotational-vector-field}
\end{figure}

Fields of this type arise naturally in rotational fluid flow and in simplified
models of magnetic circulation.

\subsection{Radial Fields}

An especially important class of fields points directly toward or away from a
central point. Let
\[
\mathbf r=x\mathbf e_x+y\mathbf e_y+z\mathbf e_z,
\qquad
r=\lVert\mathbf r\rVert.
\]
The radial unit-vector field is
\[
\widehat{\mathbf r}=\frac{\mathbf r}{r},
\qquad r\neq 0.
\]
Every vector points directly away from the origin. A radial inverse-square field
has the form
\[
\boxed{
\mathbf F(\mathbf r)
=
\frac{C}{r^3}\mathbf r
=
\frac{C}{r^2}\widehat{\mathbf r}.
}
\]

\begin{figure}[htbp]
\centering
\begingroup
\tikzset{
  axis/.style={physics axis},
  vector secondary/.style={physics vector secondary},
  field/.style={field vector},
  force/.style={force vector},
  label/.style={visual label}
}
\input{chapters/chapter01_vectors/fig27_radial_vector_fields.tex}
\endgroup
\caption{Outward and inward radial vector fields. The arrow lengths decrease
with distance from the centre.}
\label{fig:radial-vector-fields}
\end{figure}

The factor $1/r^3$ multiplies the vector $\mathbf r$, whose magnitude is $r$;
therefore the resulting field magnitude varies as $1/r^2$.

\subsection{Example: Earth's Gravitational Field}

Outside a spherically symmetric Earth of mass $M$, Newtonian gravity is
described by
\[
\boxed{
\mathbf g(\mathbf r)
=
-\frac{GM}{r^3}\mathbf r
=
-\frac{GM}{r^2}\widehat{\mathbf r}.
}
\]
The minus sign indicates that the field points toward Earth's centre. Its
magnitude decreases with distance, and its form is spherically symmetric.

This field provides the local acceleration that would be experienced by a small
test mass placed at position $\mathbf r$:
\[
\mathbf F_{\mathrm g}=m\mathbf g.
\]

\subsection{Example: Electric Field of a Point Charge}

A stationary point charge $q$ produces the electric field
\[
\boxed{
\mathbf E(\mathbf r)
=
\frac{1}{4\pi\varepsilon_0}
\frac{q}{r^3}\mathbf r.
}
\]
For $q>0$, the field points outward; for $q<0$, it points inward. Gravity and
electrostatics describe different interactions, yet their central-field
structures are mathematically similar.

\subsection{Streamlines}

A streamline is a curve whose tangent is everywhere parallel to the local field.
If a curve is written as $\mathbf r(s)$, then a streamline satisfies
\[
\frac{d\mathbf r}{ds}\parallel\mathbf F(\mathbf r(s)).
\]
Equivalently,
\[
\frac{d\mathbf r}{ds}
=
\lambda(s)\mathbf F(\mathbf r(s))
\]
for some scalar function $\lambda(s)$.

\begin{figure}[htbp]
\centering
\begingroup
\tikzset{
  axis/.style={physics axis},
  vector secondary/.style={physics vector secondary},
  field/.style={field vector},
  force/.style={force vector},
  label/.style={visual label}
}
\input{chapters/chapter01_vectors/fig28_arrows_and_streamlines.tex}
\endgroup
\caption{Arrow and streamline representations of the same rotational field.}
\label{fig:arrows-and-streamlines}
\end{figure}

Streamlines are visualization tools. They coincide with particle paths only
under appropriate physical conditions, such as steady fluid flow.

\subsection{Physical Interpretation}

A vector field describes how a physical influence is distributed throughout
space. Instead of asking only
\begin{quote}
What force acts on this particular object?
\end{quote}
we ask
\begin{quote}
What force would act on a suitable test object placed at any point?
\end{quote}

This shift from isolated vectors to continuous fields is one of the central
conceptual developments of theoretical physics. It allows interactions to be
described locally rather than solely through direct pairwise action at a
distance.

\subsection{Historical Note}

The modern field concept developed during the nineteenth century. Michael
Faraday introduced lines of force while studying electricity and magnetism.
James Clerk Maxwell translated these physical pictures into differential
equations for electromagnetic fields. In the twentieth century, Einstein
described gravitation geometrically through spacetime, while quantum theory
introduced wavefunctions and quantum fields defined over space and time.

\subsection{Common Mistakes}

A vector field is not one vector; it is a function assigning a vector to every
point. The arrows in a field plot are samples, not separate physical objects.
Their graphical length may also be rescaled for readability, so it need not
represent the field magnitude literally.

A streamline should not automatically be interpreted as a particle trajectory.
The distinction depends on whether the field is stationary and on the equations
governing the motion.

Finally, the expression
\[
\frac{\mathbf r}{r^3}
\]
has magnitude $1/r^2$, not $1/r^3$.

\subsection{Summary}

A vector field is a function from positions to vectors. Its components are
functions of position, so its magnitude and direction can vary throughout
space. Uniform, rotational, and radial fields provide basic patterns that recur
throughout mechanics, gravitation, electromagnetism, and fluid dynamics.
Arrow plots and streamlines provide complementary visualizations of the same
underlying field.

\subsection*{Looking Ahead}

Vector fields provide the language of modern physics, but we still need tools
that describe how they vary from point to point. Gradient, divergence, and curl
measure directional change, local sources and sinks, and rotational structure.
These operators underlie Maxwell's equations, fluid mechanics, heat flow, and
the differential equations of quantum mechanics.

\subsection*{Building Toward Quantum Mechanics}

The progression from vectors to vector fields prepares the reader for the
wavefunction $\psi(\mathbf r,t)$, a field-like mathematical object defined over
space and time. Differential operators act on that function to determine its
spatial structure and evolution. Developing these ideas first in a geometric
and physical setting makes the later quantum formalism much more transparent.
