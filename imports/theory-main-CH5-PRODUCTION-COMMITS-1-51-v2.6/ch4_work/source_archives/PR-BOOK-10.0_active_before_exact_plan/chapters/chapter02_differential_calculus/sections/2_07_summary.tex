\section{Summary and Looking Ahead}
Functions describe dependence, limits define local behavior, and derivatives quantify change. Partial derivatives extend the idea to several variables, while the gradient combines those derivatives into a geometric vector. Directional derivatives extract change along any chosen direction. The next chapter applies differential operators to vector fields and then connects local derivatives with global integrals.
