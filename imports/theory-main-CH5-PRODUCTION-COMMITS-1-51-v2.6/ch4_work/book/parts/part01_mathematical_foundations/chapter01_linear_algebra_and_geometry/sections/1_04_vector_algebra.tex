\section{Vector Algebra}
\label{sec:vector-algebra}

Vector algebra formalizes the geometric operations of combining displacements, changing scale, and comparing directions.

\subsection{Addition and Subtraction}

If
\[
\mathbf A=A_x\mathbf e_x+A_y\mathbf e_y+A_z\mathbf e_z,
\qquad
\mathbf B=B_x\mathbf e_x+B_y\mathbf e_y+B_z\mathbf e_z,
\]
then
\[
\mathbf A+\mathbf B=(A_x+B_x)\mathbf e_x+(A_y+B_y)\mathbf e_y+(A_z+B_z)\mathbf e_z.
\]
Geometrically, vector addition is represented by the head-to-tail rule or, equivalently, by the diagonal of a parallelogram. Subtraction is addition of the opposite vector:
\[
\mathbf A-\mathbf B=\mathbf A+(-\mathbf B).
\]

\begin{bookfigure}{Head-to-tail vector addition. Translating a vector without rotating or rescaling it does not change the vector.}
\input{figures/ch01/vector_addition_head_to_tail.tex}
\end{bookfigure}

\begin{bookfigure}{For vectors with a common origin, $\mathbf A-\mathbf B$ points from the tip of $\mathbf B$ to the tip of $\mathbf A$.}
\input{figures/ch01/vector_subtraction.tex}
\label{fig:vector-subtraction}
\end{bookfigure}

\subsection{Scalar Multiplication}

\begin{bookfigure}{Multiplication by a scalar changes length; a negative scalar also reverses direction.}
\input{figures/ch01/scalar_multiplication.tex}
\label{fig:scalar-multiplication}
\end{bookfigure}

Multiplying a vector by a scalar $\lambda$ changes its magnitude and possibly its direction:
\[
\lambda\mathbf A=(\lambda A_x)\mathbf e_x+(\lambda A_y)\mathbf e_y+(\lambda A_z)\mathbf e_z.
\]
For $\lambda>0$ the direction is unchanged; for $\lambda<0$ it is reversed; and for $\lambda=0$ the result is the zero vector.

\begin{propertybox}[Algebraic laws]
For vectors $\mathbf A,\mathbf B,\mathbf C$ and scalars $\lambda,\mu$,
\begin{align*}
\mathbf A+\mathbf B&=\mathbf B+\mathbf A,\\
(\mathbf A+\mathbf B)+\mathbf C&=\mathbf A+(\mathbf B+\mathbf C),\\
\lambda(\mathbf A+\mathbf B)&=\lambda\mathbf A+\lambda\mathbf B,\\
(\lambda+\mu)\mathbf A&=\lambda\mathbf A+\mu\mathbf A.
\end{align*}
\end{propertybox}

\booktopic{Displacement Between Two Points}

If points $P$ and $Q$ have position vectors $\mathbf r_P$ and $\mathbf r_Q$, the displacement from $P$ to $Q$ is
\[
\Delta\mathbf r=\mathbf r_Q-\mathbf r_P.
\]
This difference is independent of the arbitrary choice of coordinate origin.

\begin{workedexample}
Let $P=(1,-2,3)$ and $Q=(4,2,-1)$. Then
\[
\Delta\mathbf r=(4-1,\,2-(-2),\,-1-3)=(3,4,-4).
\]
\end{workedexample}
