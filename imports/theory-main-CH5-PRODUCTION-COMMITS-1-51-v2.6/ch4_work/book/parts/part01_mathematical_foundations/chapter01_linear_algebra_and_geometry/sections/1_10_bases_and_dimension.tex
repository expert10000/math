\section{Bases and Dimension}
\label{sec:bases-dimension}

A basis is a minimal complete set of vectors from which every vector in a space can be constructed.

\subsection{Basis and Span}

A set $\{\mathbf e_1,\ldots,\mathbf e_n\}$ is a basis when it is linearly independent and spans the space. Every vector then has a unique expansion
\[
\mathbf v=\sum_{i=1}^{n}v^i\mathbf e_i.
\]
The coefficients $v^i$ are the components of $\mathbf v$ in that basis.

\begin{definition}
The dimension of a finite-dimensional vector space is the number of vectors in any basis of that space.
\end{definition}

\begin{bookfigure}{A vector is independent of its description, while its components are defined relative to a basis.}
\input{figures/ch01/basis_vectors.tex}
\label{fig:basis-vectors}
\end{bookfigure}

\subsection{Change of Basis}

The same geometric vector can have different components in different bases. If
\[
\mathbf e'_i=\sum_j R_{ji}\mathbf e_j,
\]
then the components transform so that the complete vector remains unchanged. For an orthonormal rotation, the matrix $R$ satisfies
\[
R^{\mathsf T}R=I.
\]
Consequently lengths and angles are preserved.

\begin{bookfigure}{The same vector described in two rotated bases. The vector remains fixed while its numerical components change.}
\input{figures/ch01/change_of_basis.tex}
\end{bookfigure}

\begin{bookfigure}{A linear transformation is fixed by its action on a basis. Grid lines remain straight and parallel under the transformation shown.}
\input{figures/ch01/linear_transformation.tex}
\end{bookfigure}

\begin{physical}
A change of basis does not change the physical state. It changes only the numerical language used to describe that state. This principle later appears in rotations, normal modes, spin measurements, and transformations between position and momentum representations.
\end{physical}

\begin{workedexample}
In the plane, let
\[
\mathbf e'_1=\frac{\mathbf e_x+\mathbf e_y}{\sqrt2},
\qquad
\mathbf e'_2=\frac{-\mathbf e_x+\mathbf e_y}{\sqrt2}.
\]
These vectors form an orthonormal basis rotated by $45^\circ$. The vector $\mathbf v=\mathbf e_x$ has components $(1,0)$ in the original basis and $(1/\sqrt2,-1/\sqrt2)$ in the rotated basis.
\end{workedexample}

\booktopic{Eigenvectors: a Preview}
For a linear transformation $T$, an eigenvector is a nonzero vector whose direction line is preserved:
\[
T\mathbf v=\lambda\mathbf v.
\]
The scalar $\lambda$ is the eigenvalue. This simple relation later becomes central in normal-mode analysis and in quantum measurement.

\begin{bookfigure}{Two eigenvector directions under a linear transformation. An eigenvalue may stretch, shrink, or reverse a vector.}
\input{figures/ch01/eigenvectors.tex}
\end{bookfigure}
