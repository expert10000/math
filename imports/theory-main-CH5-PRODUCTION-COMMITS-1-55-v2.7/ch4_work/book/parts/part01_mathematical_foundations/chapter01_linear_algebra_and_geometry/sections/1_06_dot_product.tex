\section{Dot Product}
\label{sec:dot-product}

Until now we have learned how to represent vectors and how to combine them through
addition and scalar multiplication. These operations allow us to construct new
vectors, but they do not answer an equally important question:

\begin{center}
\emph{How can we compare two vectors?}
\end{center}

For example, suppose two forces act on an object. Are they parallel? Are they
perpendicular? Do they point in nearly the same direction, or in opposite
directions?

To answer these questions we introduce one of the most important operations in
mathematics and physics: the \emph{dot product}, also called the \emph{scalar
product} or, in more general settings, an \emph{inner product}.

Unlike vector addition, the dot product combines two vectors and produces a
scalar rather than another vector. This scalar measures how strongly the two
vectors are aligned with one another.

\subsection{Motivation}

Imagine pushing a heavy box across a floor. Suppose you apply a force of
$100\,\mathrm{N}$. If the force is directed exactly along the direction of
motion, all of your effort contributes to moving the box.

Now imagine pushing at an angle of $60^{\circ}$. Although the force still has
the same magnitude, only part of it acts in the direction of motion. The
remainder presses the box downward against the floor and does not help it move
forward.

Clearly, the effectiveness of a force depends not only on its magnitude but
also on the angle between the force and the displacement. The mathematical
operation that captures this dependence is the dot product.

\subsection{Geometric Definition}

Consider two nonzero vectors $\mathbf A$ and $\mathbf B$ separated by an angle
$\theta$, where $0\leq\theta\leq\pi$. Their dot product is defined by

\[
\boxed{
\mathbf A\cdot\mathbf B
=
\lVert\mathbf A\rVert\,\lVert\mathbf B\rVert\cos\theta
}
\]

\begin{bookfigure}{The dot product as an angular comparison and a projection. The sign is determined by whether the angle is acute, right, or obtuse.}
\input{figures/ch01/dot_product_angle.tex}
\end{bookfigure}

This equation tells us that the dot product depends on three quantities:

\begin{itemize}
  \item the magnitude of the first vector;
  \item the magnitude of the second vector;
  \item the cosine of the angle between them.
\end{itemize}

Figure~\ref{fig:dot-product-angle} illustrates the geometric meaning of the dot
product.

\begin{figure}[htbp]
  \centering
  \begingroup
  \tikzset{
    vector/.style={physics vector},
    vector secondary/.style={physics vector secondary},
    construction/.style={construction line},
    label/.style={visual label},
    ForceRed/.style={draw=black},
    PhysicsBlue/.style={draw=black}
  }
  \input{legacy_sources/chapters_pre_PR_BOOK_10/chapter01_vectors/fig13_dot_product_angle.tex}
  \endgroup
  \caption{Two vectors separated by an angle $\theta$. The dot product measures
  the component of one vector along the direction of the other.}
  \label{fig:dot-product-angle}
\end{figure}

\subsection{Why the Cosine Appears}

The cosine appears because it extracts the component of one vector along the
direction of the other. Suppose we project $\mathbf B$ onto the direction of
$\mathbf A$. The scalar length of this projection is

\[
\lVert\mathbf B\rVert\cos\theta.
\]

Multiplying this projected length by the magnitude of $\mathbf A$ gives

\[
\lVert\mathbf A\rVert\,\lVert\mathbf B\rVert\cos\theta,
\]

which is precisely the definition of the dot product. Thus, the dot product
measures how much of one vector lies along the direction of the other. This
interpretation is far more important than simply memorizing the formula.

If $\widehat{\mathbf A}=\mathbf A/\lVert\mathbf A\rVert$ is the unit vector in
the direction of $\mathbf A$, then

\[
\mathbf B\cdot\widehat{\mathbf A}
=
\lVert\mathbf B\rVert\cos\theta.
\]

is the scalar component of $\mathbf B$ along $\mathbf A$.

\subsection{Component Formula}

Although the geometric definition provides intuition, practical calculations
are usually performed using vector components. Let

\[
\mathbf A
=A_x\mathbf e_x+A_y\mathbf e_y+A_z\mathbf e_z,
\]

and

\[
\mathbf B
=B_x\mathbf e_x+B_y\mathbf e_y+B_z\mathbf e_z.
\]

The Cartesian basis is orthonormal, so

\[
\boxed{
\mathbf e_i\cdot\mathbf e_j=\delta_{ij}
}
\]

where $\delta_{ij}$ is the Kronecker delta,

\[
\delta_{ij}
=
\begin{cases}
1, & i=j,\\
0, & i\neq j.
\end{cases}
\]

Using distributivity and orthonormality,

\begin{align*}
\mathbf A\cdot\mathbf B
&=
(A_i\mathbf e_i)\cdot(B_j\mathbf e_j)\\
&=
A_iB_j(\mathbf e_i\cdot\mathbf e_j)\\
&=
A_iB_j\delta_{ij}\\
&=
A_iB_i.
\end{align*}

In explicit Cartesian form,

\[
\boxed{
\mathbf A\cdot\mathbf B
=
A_xB_x+A_yB_y+A_zB_z
}
\]

where repeated indices are summed. This remarkably simple expression is one of
the most frequently used formulas in all branches of physics.

\subsection{Special Cases}

The dot product immediately reveals the geometric relationship between two
vectors.

\booktopic{Parallel Vectors}

If $\theta=0^{\circ}$, then $\cos 0^{\circ}=1$, and therefore

\[
\mathbf A\cdot\mathbf B
=
\lVert\mathbf A\rVert\,\lVert\mathbf B\rVert.
\]

The dot product is positive and has its maximum possible value for fixed vector
magnitudes.

\booktopic{Perpendicular Vectors}

If $\theta=90^{\circ}$, then $\cos 90^{\circ}=0$, so

\[
\boxed{
\mathbf A\cdot\mathbf B=0
}
\]

For nonzero vectors, this gives one of the most useful tests in mathematics and
physics:

\begin{center}
\emph{Two vectors are orthogonal if and only if their dot product is zero.}
\end{center}

This criterion appears repeatedly in mechanics, electromagnetism, Fourier
analysis, and quantum mechanics.

\booktopic{Opposite Directions}

If $\theta=180^{\circ}$, then $\cos 180^{\circ}=-1$, and

\[
\mathbf A\cdot\mathbf B
=-\lVert\mathbf A\rVert\,\lVert\mathbf B\rVert.
\]

The dot product is negative, indicating that the vectors point in opposite
directions.

\subsection{Physical Interpretation}

The sign of the dot product carries important physical information:

\begin{itemize}
  \item positive: the vectors point generally in the same direction;
  \item zero: the vectors are perpendicular;
  \item negative: the vectors point generally in opposite directions.
\end{itemize}

Thus, a single scalar quantity contains information about both magnitude and
relative orientation.

The angle can also be recovered from the dot product:

\[
\boxed{
\cos\theta
=
\frac{\mathbf A\cdot\mathbf B}
{\lVert\mathbf A\rVert\,\lVert\mathbf B\rVert}
}
\]

for nonzero vectors $\mathbf A$ and $\mathbf B$.

\begin{bookfigure}{Only the force component parallel to the displacement contributes to mechanical work.}
\input{figures/ch01/work_interpretation.tex}
\label{fig:work-dot-product}
\end{bookfigure}

\subsection{Example: Mechanical Work}

One of the first major applications of the dot product appears in mechanics.
Suppose a constant force $\mathbf F$ moves an object through a displacement
$\mathbf s$. The mechanical work performed is defined as

\[
\boxed{
W=\mathbf F\cdot\mathbf s
}
\]

Substituting the geometric definition gives

\[
W=Fs\cos\theta.
\]

This equation explains several familiar situations:

\begin{itemize}
  \item If the force acts along the displacement $(\theta=0^{\circ})$, the work
  is maximal.
  \item If the force is perpendicular to the displacement
  $(\theta=90^{\circ})$, the work is zero.
  \item If the force opposes the motion $(\theta=180^{\circ})$, the work is
  negative.
\end{itemize}

Thus, the dot product naturally quantifies energy transfer.

\subsection{Worked Example 1.7}

Consider the vectors

\[
\mathbf A=(2,3,1),
\qquad
\mathbf B=(4,-1,5).
\]

Compute their dot product.

\paragraph{Solution.}
Using the component formula,

\begin{align*}
\mathbf A\cdot\mathbf B
&=2\cdot4+3(-1)+1\cdot5\\
&=8-3+5\\
&=10.
\end{align*}

Therefore,

\[
\boxed{
\mathbf A\cdot\mathbf B=10
}
\]

Since the result is positive, the angle between the vectors is less than
$90^{\circ}$.

\subsection{Coordinate Invariance}

The component formula may appear to depend on the choice of Cartesian axes, but
the dot product itself is a geometric scalar and is therefore independent of
how the coordinate system is rotated.

Let an orthogonal transformation be represented by a matrix $R$, so that

\[
\mathbf A'=R\mathbf A,
\qquad
\mathbf B'=R\mathbf B,
\]

with

\[
R^{\mathsf T}R=I.
\]

Then

\begin{align*}
\mathbf A'\cdot\mathbf B'
&=(R\mathbf A)^{\mathsf T}(R\mathbf B)\\
&=\mathbf A^{\mathsf T}R^{\mathsf T}R\mathbf B\\
&=\mathbf A^{\mathsf T}\mathbf B\\
&=\mathbf A\cdot\mathbf B.
\end{align*}

Thus, observers using rotated Cartesian axes calculate different components but
the same dot product. This is why quantities such as length, angle, and work are
coordinate-independent physical scalars.

\subsection{Toward Metric Tensors and Inner Products}

In Cartesian Euclidean space, the dot product can be written

\[
\mathbf A\cdot\mathbf B
=
\delta_{ij}A^iB^j.
\]

The Kronecker delta acts as the Euclidean metric. In more general coordinates or
curved spaces, it is replaced by a metric tensor $g_{ij}$:

\[
\boxed{
\langle\mathbf A,\mathbf B\rangle
=
g_{ij}A^iB^j
}
\]

This is the natural extension of the dot product to differential geometry and
general relativity.

Quantum mechanics generalizes the same idea further. Quantum states belong to a
complex Hilbert space, and their inner product is usually written

\[
\langle\phi\mid\psi\rangle.
\]

Unlike the real Euclidean dot product, the Hilbert-space inner product involves
complex conjugation. Nevertheless, it serves the same structural purpose: it
defines lengths, angles, orthogonality, projections, and probabilities.

\subsection{Historical Note}

The scalar product emerged during the nineteenth century through the work of
Hermann Grassmann and Josiah Willard Gibbs. Their development of vector analysis
transformed mechanics and electromagnetism by providing a compact language for
expressing physical laws. In the twentieth century, the idea was generalized to
abstract inner products in Hilbert spaces, where it became one of the
mathematical foundations of quantum mechanics. There, the inner product
determines probabilities, expectation values, and the orthogonality of quantum
states.

\subsection{Common Mistakes}

Several mistakes occur repeatedly when first learning the dot product:

\begin{itemize}
  \item Confusing the dot product with ordinary multiplication of vectors. The
  result of a dot product is a scalar, not a vector.
  \item Forgetting that the dot product depends on the angle between the
  vectors. Two vectors with the same magnitudes can have very different dot
  products if they point in different directions.
  \item Assuming that a zero dot product means one of the vectors is zero. For
  nonzero vectors, it means that the vectors are perpendicular.
  \item Forgetting to divide by both magnitudes when using the dot product to
  calculate an angle.
  \item Treating $\mathbf A\cdot\mathbf B$ and $\mathbf A\times\mathbf B$ as
  interchangeable operations. They produce different types of objects and
  describe different geometry.
\end{itemize}

\subsection{Summary}

The dot product combines two vectors to produce a scalar. Geometrically,

\[
\mathbf A\cdot\mathbf B
=
\lVert\mathbf A\rVert\,\lVert\mathbf B\rVert\cos\theta,
\]

while in an orthonormal Cartesian basis,

\[
\mathbf A\cdot\mathbf B
=
A_iB_i
=
A_xB_x+A_yB_y+A_zB_z.
\]

It measures alignment, determines angles and orthogonality, extracts vector
components, and expresses physical quantities such as work. Its invariance
under rotations makes it a geometric scalar, while its generalizations lead to
metric tensors and Hilbert-space inner products.

\subsection{Looking Ahead}

The dot product measures similarity and alignment between vectors. There is
another fundamental operation that measures something entirely different: the
area spanned by two vectors and the direction perpendicular to the plane
containing them.

This operation is the cross product, which will be developed in the next
section. Unlike the dot product, the cross product produces a new vector and
plays a central role in rotational motion, angular momentum, torque, magnetic
forces, and Maxwell's equations.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
