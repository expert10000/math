# Overleaf Use

1. Upload the complete ZIP as a new Overleaf project.
2. Set `main.tex` as the Main document.
3. Use pdfLaTeX.
4. Overleaf will run bibliography and index helpers as required.
5. Edit chapter prose only under `chapters/.../sections/`.

The project deliberately uses relative paths and standard TeX Live packages.
