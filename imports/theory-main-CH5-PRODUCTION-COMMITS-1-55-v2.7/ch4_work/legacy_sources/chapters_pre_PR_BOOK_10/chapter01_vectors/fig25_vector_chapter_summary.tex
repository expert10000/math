\begin{tikzpicture}[
  node distance=1.4cm and 1.4cm,
  summary/.style={
    concept node,
    minimum width=3.2cm,
    minimum height=1.1cm,
    text width=2.9cm,
    align=center
  }
]
  \node[summary] (components) {Components and basis\\
    $\mathbf v=v_i\hat{\mathbf e}_i$};
  \node[summary,right=of components] (geometry) {Magnitude and direction\\
    $\|\mathbf v\|,\ \hat{\mathbf v}$};
  \node[summary,right=of geometry] (transform) {Transformations\\
    rotations, reflections, translations};

  \node[summary,below=of components] (dot) {Dot product\\
    angle, projection, work};
  \node[summary,below=of geometry] (cross) {Cross product\\
    area, normal, torque};
  \node[summary,below=of transform] (field) {Vector fields\\
    vectors assigned to space};

  \draw[vector secondary] (components)--(geometry);
  \draw[vector secondary] (geometry)--(transform);
  \draw[vector secondary] (components)--(dot);
  \draw[vector secondary] (geometry)--(cross);
  \draw[vector secondary] (transform)--(field);
  \draw[vector secondary] (dot)--(cross);
  \draw[vector secondary] (cross)--(field);

  \node[concept node,below=1.5cm of cross,text width=8.6cm] (summaryeq) {
    Vectors provide the language of mechanics:
    position, velocity, acceleration, force, momentum, torque, angular momentum,
    electric fields, magnetic fields, and quantum-state geometry.
  };
\end{tikzpicture}
