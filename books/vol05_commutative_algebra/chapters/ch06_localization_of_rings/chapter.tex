\chapter{Localization of Rings}
\label{ch:v06}

Localization of rings adjoins inverses to a chosen multiplicative system. It is governed by a universal property and induces a precise correspondence between primes of the localization and primes of the original ring disjoint from the denominators.

\section*{Learning goals}
After this chapter, the reader should be able to:
\begin{itemize}
\item construct localized rings and simplify explicit fractions;
\item apply the universal property of localization to extend ring homomorphisms uniquely;
\item compute prime ideals of a localization from primes disjoint from the multiplicative set;
\item determine when an element becomes zero or a unit after localization;
\item compare localization at a prime, maximal ideal, and single element;
\item prove that localization commutes with quotients in the standard compatible form;
\end{itemize}
\section*{Conceptual roadmap}
\volroadmap
  {presentation or universal property}
  {exactness/localization}
  {structural consequence}

\section{Fraction construction}
Elements of $S^{-1}A$ are fractions $a/s$ modulo the relation generated by cross-multiplication after multiplying by a further denominator.

\section{Ring operations}
Fractions add and multiply by the usual formulas.

% BEGIN VOL05-EXPANSION V06-example-01
\begin{example}[Integer localization]\label{ex:v06-expand-01}
Localizing $\mathbb Z$ at powers of $6$ gives $\mathbb Z[1/6]$. Fractions with denominators dividing powers of $6$ occur, while $1/5$ does not.
\end{example}
% END VOL05-EXPANSION V06-example-01
\section{Canonical map}
The map $A\to S^{-1}A$ sends $a$ to $a/1$ and makes every element of $S$ invertible.

\section{Universal property}
Any ring map from $A$ sending $S$ to units factors uniquely through $S^{-1}A$.

\section{Zero criterion}
A fraction $a/s$ is zero iff some $t\in S$ annihilates $a$.

% BEGIN VOL05-EXPANSION V06-example-02
\begin{example}[Principal localization]\label{ex:v06-expand-02}
In $k[x,y]_x$, $x$ is a unit, so $(x,y)$ becomes the whole ring. The ideal $(y)$ remains proper and records the part visible where $x$ is invertible.
\end{example}
% END VOL05-EXPANSION V06-example-02
\section{Localized ideals}
Extension sends $I$ to $S^{-1}I$, while contraction returns ideals saturated with respect to $S$.

\section{Prime correspondence}
Prime ideals of $S^{-1}A$ correspond to prime ideals of $A$ disjoint from $S$.

% BEGIN VOL05-EXPANSION V06-example-03
\begin{example}[Prime localization]\label{ex:v06-expand-03}
The ring $\mathbb Z_{(5)}$ consists of $a/b$ with $5\nmid b$. Its unique maximal ideal consists of fractions whose numerator is divisible by $5$.
\end{example}
% END VOL05-EXPANSION V06-example-03
\section{Localization at an element}
$A_f$ is obtained by inverting powers of one element $f$.

\section{Localization at a prime}
$A_{\mathfrak p}$ inverts everything outside $\mathfrak p$ and is a local ring.

\section{Core structural results}

\begin{theorem}[Universal property of localization]\label{thm:v06-01}
If $\varphi:A\to B$ sends every $s\in S$ to a unit, there is a unique map $\widetilde\varphi:S^{-1}A\to B$ with $\widetilde\varphi(a/s)=\varphi(a)\varphi(s)^{-1}$.
\begin{proof}
The formula respects the localization equivalence relation and is forced by the requirement that denominators become inverses, giving existence and uniqueness.
\end{proof}
\end{theorem}

\begin{theorem}[Prime correspondence]\label{thm:v06-02}
Prime ideals of $S^{-1}A$ are in bijection with prime ideals $\mathfrak p\subset A$ satisfying $\mathfrak p\cap S=\varnothing$.
\begin{proof}
Contraction of a prime avoids units, hence avoids $S$. For a prime disjoint from $S$, its extension consists of fractions with numerator in the prime and is prime; extension and contraction are inverse.
\end{proof}
\end{theorem}

\begin{theorem}[Localization commutes with quotient]\label{thm:v06-03}
For an ideal $I$, $S^{-1}(A/I)\cong S^{-1}A/S^{-1}I$ after replacing $S$ by its image in $A/I$.
\begin{proof}
Both rings satisfy the same universal property: maps out correspond to maps from $A$ killing $I$ and inverting $S$.
\end{proof}
\end{theorem}

\section{Worked examples}

\begin{example}[Fraction construction diagnostic]\label{ex:v06-worked-01}
Treat Fraction construction as a short proof dossier. State the hypotheses explicitly, then represent elements by fractions and use the localization universal property to compare maps. Derive the conclusion and identify a failure mode if a required hypothesis is absent. Elements of $S^{-1}A$ are fractions $a/s$ modulo the relation generated by cross-multiplication after multiplying by a further denominator. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{example}

\begin{example}[Ring operations diagnostic]\label{ex:v06-worked-02}
Treat Ring operations as a short proof dossier. State the hypotheses explicitly, then represent elements by fractions and use the localization universal property to compare maps. Derive the conclusion and identify a failure mode if a required hypothesis is absent. Fractions add and multiply by the usual formulas. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{example}

\begin{example}[Canonical map diagnostic]\label{ex:v06-worked-03}
Resolve Canonical map by separating assumptions from inference. State the assumptions, represent elements by fractions and use the localization universal property to compare maps, give the conclusion, and note one condition under which that conclusion is no longer justified. The map $A\to S^{-1}A$ sends $a$ to $a/1$ and makes every element of $S$ invertible. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{example}

\begin{example}[Universal property diagnostic]\label{ex:v06-worked-04}
Audit Universal property in four stages: hypotheses, decisive mechanism, argument, and limitation. For the decisive mechanism, represent elements by fractions and use the localization universal property to compare maps. Any ring map from $A$ sending $S$ to units factors uniquely through $S^{-1}A$. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{example}

\section{Solved problems}
Each extended problem combines several results from the chapter in a longer argument. It is intended to make hypothesis selection, proof strategy, and verification explicit before the shorter exercises begin.

\begin{problem}[Fraction construction diagnostic]\label{prob:v06-problem-01}
Treat Fraction construction as a short proof dossier. State the hypotheses explicitly, then represent elements by fractions and use the localization universal property to compare maps. Derive the conclusion and identify a failure mode if a required hypothesis is absent.
\end{problem}
\begin{solution}
Elements of $S^{-1}A$ are fractions $a/s$ modulo the relation generated by cross-multiplication after multiplying by a further denominator. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Ring operations diagnostic]\label{prob:v06-problem-02}
Treat Ring operations as a short proof dossier. State the hypotheses explicitly, then represent elements by fractions and use the localization universal property to compare maps. Derive the conclusion and identify a failure mode if a required hypothesis is absent.
\end{problem}
\begin{solution}
Fractions add and multiply by the usual formulas. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Canonical map diagnostic]\label{prob:v06-problem-03}
Resolve Canonical map by separating assumptions from inference. State the assumptions, represent elements by fractions and use the localization universal property to compare maps, give the conclusion, and note one condition under which that conclusion is no longer justified.
\end{problem}
\begin{solution}
The map $A\to S^{-1}A$ sends $a$ to $a/1$ and makes every element of $S$ invertible. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Universal property diagnostic]\label{prob:v06-problem-04}
Audit Universal property in four stages: hypotheses, decisive mechanism, argument, and limitation. For the decisive mechanism, represent elements by fractions and use the localization universal property to compare maps.
\end{problem}
\begin{solution}
Any ring map from $A$ sending $S$ to units factors uniquely through $S^{-1}A$. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Zero criterion diagnostic]\label{prob:v06-problem-05}
Resolve Zero criterion by separating assumptions from inference. State the assumptions, represent elements by fractions and use the localization universal property to compare maps, give the conclusion, and note one condition under which that conclusion is no longer justified.
\end{problem}
\begin{solution}
A fraction $a/s$ is zero iff some $t\in S$ annihilates $a$. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Localized ideals diagnostic]\label{prob:v06-problem-06}
Audit Localized ideals in four stages: hypotheses, decisive mechanism, argument, and limitation. For the decisive mechanism, represent elements by fractions and use the localization universal property to compare maps.
\end{problem}
\begin{solution}
Extension sends $I$ to $S^{-1}I$, while contraction returns ideals saturated with respect to $S$. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Prime correspondence diagnostic]\label{prob:v06-problem-07}
Resolve Prime correspondence by separating assumptions from inference. State the assumptions, represent elements by fractions and use the localization universal property to compare maps, give the conclusion, and note one condition under which that conclusion is no longer justified.
\end{problem}
\begin{solution}
Prime ideals of $S^{-1}A$ correspond to prime ideals of $A$ disjoint from $S$. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Localization at an element diagnostic]\label{prob:v06-problem-08}
Audit Localization at an element in four stages: hypotheses, decisive mechanism, argument, and limitation. For the decisive mechanism, represent elements by fractions and use the localization universal property to compare maps.
\end{problem}
\begin{solution}
$A_f$ is obtained by inverting powers of one element $f$. The decisive algebraic mechanism is to represent elements by fractions and use the localization universal property to compare maps. The stated hypotheses determine which structural conclusion is valid.
\end{solution}

\begin{problem}[Universal property of localization]\label{prob:v06-problem-09}
Prove the following structural statement and identify the hypothesis that makes the conclusion work: If $\varphi:A\to B$ sends every $s\in S$ to a unit, there is a unique map $\widetilde\varphi:S^{-1}A\to B$ with $\widetilde\varphi(a/s)=\varphi(a)\varphi(s)^{-1}$.
\end{problem}
\begin{solution}
The formula respects the localization equivalence relation and is forced by the requirement that denominators become inverses, giving existence and uniqueness. The proof hinges on the following algebraic step: represent elements by fractions and use the localization universal property to compare maps.
\end{solution}

\begin{problem}[Prime correspondence]\label{prob:v06-problem-10}
Prove the following structural statement and identify the hypothesis that makes the conclusion work: Prime ideals of $S^{-1}A$ are in bijection with prime ideals $\mathfrak p\subset A$ satisfying $\mathfrak p\cap S=\varnothing$.
\end{problem}
\begin{solution}
Contraction of a prime avoids units, hence avoids $S$. For a prime disjoint from $S$, its extension consists of fractions with numerator in the prime and is prime; extension and contraction are inverse. The proof hinges on the following algebraic step: represent elements by fractions and use the localization universal property to compare maps.
\end{solution}

\begin{problem}[Localization commutes with quotient]\label{prob:v06-problem-11}
Prove the following structural statement and identify the hypothesis that makes the conclusion work: For an ideal $I$, $S^{-1}(A/I)\cong S^{-1}A/S^{-1}I$ after replacing $S$ by its image in $A/I$.
\end{problem}
\begin{solution}
Both rings satisfy the same universal property: maps out correspond to maps from $A$ killing $I$ and inverting $S$. The proof hinges on the following algebraic step: represent elements by fractions and use the localization universal property to compare maps.
\end{solution}

\begin{problem}[Chapter synthesis]\label{prob:v06-problem-12}
Connect the definitions and principal structural theorems of this chapter into one reusable commutative-algebra strategy.
\end{problem}
\begin{solution}
Localization of rings adjoins inverses to a chosen multiplicative system. It is governed by a universal property and induces a precise correspondence between primes of the localization and primes of the original ring disjoint from the denominators. A chapter-specific strategy is to represent elements by fractions and use the localization universal property to compare maps; consequently, any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\section{Exercises with complete solutions}

\begin{exercise}\label{exr:v06-01}
For Fraction construction, distinguish the formal setup from the decisive step. First record the hypotheses; then represent elements by fractions and use the localization universal property to compare maps. Explain the conclusion and a failure mode outside those hypotheses.
\end{exercise}
\begin{hint}
Simplify a/s by multiplying numerator and denominator only by elements allowed by the localization equivalence.
\end{hint}
\begin{solution}
Elements of $S^{-1}A$ are fractions $a/s$ modulo the relation generated by cross-multiplication after multiplying by a further denominator. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\begin{exercise}\label{exr:v06-02}
Use Ring operations as a hypothesis audit. List the assumptions, then represent elements by fractions and use the localization universal property to compare maps. State exactly what follows and what the argument would cease to control if an assumption were dropped.
\end{exercise}
\begin{hint}
To prove a map from \(S^{-1}A\) exists, define \(a/s\mapsto \varphi(a)\varphi(s)^{-1}\) and check it is independent of representatives.
\end{hint}
\begin{solution}
Fractions add and multiply by the usual formulas. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\begin{exercise}\label{exr:v06-03}
Determine which hypotheses are actually needed for Canonical map. Then represent elements by fractions and use the localization universal property to compare maps. State the conclusion and identify what can fail when one essential hypothesis is removed.
\end{exercise}
\begin{hint}
A prime of \(S^{-1}A\) contracts to a prime of A disjoint from S; extend a disjoint prime and check the two operations are inverse.
\end{hint}
\begin{solution}
The map $A\to S^{-1}A$ sends $a$ to $a/1$ and makes every element of $S$ invertible. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\begin{exercise}\label{exr:v06-04}
Work through Universal property from the assumptions outward. Under the stated hypotheses, represent elements by fractions and use the localization universal property to compare maps. Give the resulting conclusion and one boundary case where the argument no longer applies.
\end{exercise}
\begin{hint}
To see whether a/1 is a unit, ask whether some multiple of a lies in S.
\end{hint}
\begin{solution}
Any ring map from $A$ sending $S$ to units factors uniquely through $S^{-1}A$. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\begin{exercise}\label{exr:v06-05}
Work through Zero criterion from the assumptions outward. Under the stated hypotheses, represent elements by fractions and use the localization universal property to compare maps. Give the resulting conclusion and one boundary case where the argument no longer applies.
\end{exercise}
\begin{hint}
For localization at p, every element outside p is a unit and \(pA_p\) is the unique maximal ideal.
\end{hint}
\begin{solution}
A fraction $a/s$ is zero iff some $t\in S$ annihilates $a$. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\begin{exercise}\label{exr:v06-06}
Determine which hypotheses are actually needed for Localized ideals. Then represent elements by fractions and use the localization universal property to compare maps. State the conclusion and identify what can fail when one essential hypothesis is removed.
\end{exercise}
\begin{hint}
Use \(S^{-1}(A/I)\cong S^{-1}A/S^{-1}I\) only after checking the image of S does not force the quotient to collapse unexpectedly.
\end{hint}
\begin{solution}
Extension sends $I$ to $S^{-1}I$, while contraction returns ideals saturated with respect to $S$. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\begin{exercise}\label{exr:v06-07}
For Prime correspondence, distinguish the formal setup from the decisive step. First record the hypotheses; then represent elements by fractions and use the localization universal property to compare maps. Explain the conclusion and a failure mode outside those hypotheses.
\end{exercise}
\begin{hint}
Localization at f can be computed as adjoining an inverse with relation ft=1; use this presentation when explicit algebra helps.
\end{hint}
\begin{solution}
Prime ideals of $S^{-1}A$ correspond to prime ideals of $A$ disjoint from $S$. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\begin{exercise}\label{exr:v06-08}
For Localization at an element, distinguish the formal setup from the decisive step. First record the hypotheses; then represent elements by fractions and use the localization universal property to compare maps. Explain the conclusion and a failure mode outside those hypotheses.
\end{exercise}
\begin{hint}
Check equality locally by cross multiplication with an S-witness rather than cancelling denominators as if the ring were a domain.
\end{hint}
\begin{solution}
$A_f$ is obtained by inverting powers of one element $f$. In this chapter the concrete consequence is that any homomorphism sending the chosen denominators to units factors uniquely through the localized ring.
\end{solution}

\section*{Chapter summary}
The chapter now contributes a worked-problem layer to the progression from rings and modules through localization, Noetherian methods, integral dependence, and derived functors.
% BEGIN VOL05-EXPANSION V06-exercises-01
\section{Graded supplementary exercises}
These exercises extend the chapter with a balanced set of computations, proofs, hypothesis tests, applications, and challenges.

\subsection*{Standard computations}

\begin{exercise}[Unit]\label{exr:v06-expand-01}
Is $2$ a unit in $\mathbb Z[1/2]$?
\end{exercise}
\begin{hint}
Use fraction localization and its universal property.
\end{hint}
\begin{solution}
Yes, inverse $1/2$.
\end{solution}

\begin{exercise}[Localized ideal]\label{exr:v06-expand-02}
Compute $(2)\mathbb Z[1/2]$.
\end{exercise}
\begin{hint}
Use fraction localization and its universal property.
\end{hint}
\begin{solution}
It is the whole ring.
\end{solution}

\begin{exercise}[Prime denominators]\label{exr:v06-expand-03}
Describe denominators in $\mathbb Z_{(7)}$.
\end{exercise}
\begin{hint}
Use fraction localization and its universal property.
\end{hint}
\begin{solution}
They are integers not divisible by $7$.
\end{solution}

\begin{exercise}[Polynomial inverse]\label{exr:v06-expand-04}
Find the inverse of $x^3$ in $k[x,y]_x$.
\end{exercise}
\begin{hint}
Use fraction localization and its universal property.
\end{hint}
\begin{solution}
It is $x^{-3}$.
\end{solution}

\begin{exercise}[Zero fraction]\label{exr:v06-expand-05}
When is $a/s=0$?
\end{exercise}
\begin{hint}
Use fraction localization and its universal property.
\end{hint}
\begin{solution}
When some denominator $u\in S$ satisfies $ua=0$.
\end{solution}

\subsection*{Proofs}

\begin{exercise}[Universal property of localization proof]\label{exr:v06-expand-06}
Prove: If $\varphi:A\to B$ sends every $s\in S$ to a unit, there is a unique map $\widetilde\varphi:S^{-1}A\to B$ with $\widetilde\varphi(a/s)=\varphi(a)\varphi(s)^{-1}$.
\end{exercise}
\begin{hint}
Start from the chapter theorem hypotheses and identify the decisive algebraic map or containment.
\end{hint}
\begin{solution}
The formula respects the localization equivalence relation and is forced by the requirement that denominators become inverses, giving existence and uniqueness.
\end{solution}

\begin{exercise}[Prime correspondence proof]\label{exr:v06-expand-07}
Prove: Prime ideals of $S^{-1}A$ are in bijection with prime ideals $\mathfrak p\subset A$ satisfying $\mathfrak p\cap S=\varnothing$.
\end{exercise}
\begin{hint}
Start from the chapter theorem hypotheses and identify the decisive algebraic map or containment.
\end{hint}
\begin{solution}
Contraction of a prime avoids units, hence avoids $S$. For a prime disjoint from $S$, its extension consists of fractions with numerator in the prime and is prime; extension and contraction are inverse.
\end{solution}

\begin{exercise}[Localization commutes with quotient proof]\label{exr:v06-expand-08}
Prove: For an ideal $I$, $S^{-1}(A/I)\cong S^{-1}A/S^{-1}I$ after replacing $S$ by its image in $A/I$.
\end{exercise}
\begin{hint}
Start from the chapter theorem hypotheses and identify the decisive algebraic map or containment.
\end{hint}
\begin{solution}
Both rings satisfy the same universal property: maps out correspond to maps from $A$ killing $I$ and inverting $S$.
\end{solution}

\begin{exercise}[Structural synthesis]\label{exr:v06-expand-09}
Prove a short corollary by combining Universal property of localization with Prime correspondence. State every hypothesis you use.
\end{exercise}
\begin{hint}
Apply the first theorem to move to its structural description, then use the second theorem on that description.
\end{hint}
\begin{solution}
A valid synthesis first invokes Universal property of localization and then applies Prime correspondence; the conclusion follows after checking the shared hypotheses.
\end{solution}

\subsection*{Counterexamples and hypothesis tests}

\begin{exercise}[Injectivity]\label{exr:v06-expand-10}
Test the claim: $R\to S^{-1}R$ is always injective.
\end{exercise}
\begin{hint}
Try a smallest standard ring or module from this chapter.
\end{hint}
\begin{solution}
False: denominator torsion can map to zero.
\end{solution}

\begin{exercise}[Prime extension]\label{exr:v06-expand-11}
Test the claim: Every prime extends to a proper prime.
\end{exercise}
\begin{hint}
Try a smallest standard ring or module from this chapter.
\end{hint}
\begin{solution}
False if the prime meets $S$.
\end{solution}

\begin{exercise}[Collapse]\label{exr:v06-expand-12}
Test the claim: A nonzero ring can never localize to zero.
\end{exercise}
\begin{hint}
Try a smallest standard ring or module from this chapter.
\end{hint}
\begin{solution}
False if $0$ is inverted.
\end{solution}

\subsection*{Applications and investigations}

\begin{exercise}[Principal open]\label{exr:v06-expand-13}
Interpret $R_f$ geometrically.
\end{exercise}
\begin{hint}
Translate the situation into fraction localization and its universal property.
\end{hint}
\begin{solution}
It describes the region where $f$ is nonzero.
\end{solution}

\begin{exercise}[Arithmetic near $p$]\label{exr:v06-expand-14}
Why use $\mathbb Z_{(p)}$?
\end{exercise}
\begin{hint}
Translate the situation into fraction localization and its universal property.
\end{hint}
\begin{solution}
All integers prime to $p$ become units.
\end{solution}

\subsection*{Challenge problems}

\begin{exercise}[Local-global synthesis]\label{exr:v06-expand-15}
Connect the chapter ideas 'Fraction construction' and 'Localization at a prime' in one rigorous argument.
\end{exercise}
\begin{hint}
State the relevant map, ideal, module, or universal property before drawing the conclusion.
\end{hint}
\begin{solution}
The argument begins with Elements of $S^{-1}A$ are fractions $a/s$ modulo the relation generated by cross-multiplication after multiplying by a further denominator. It then uses the later viewpoint: $A_{\mathfrak p}$ inverts everything outside $\mathfrak p$ and is a local ring. The bridge is supplied by the structural theorems proved in the chapter.
\end{solution}

\begin{exercise}[Hypothesis audit]\label{exr:v06-expand-16}
Choose one theorem from the chapter and explain precisely what can fail if one key hypothesis is removed.
\end{exercise}
\begin{hint}
Use one of the explicit counterexamples in the graded set and compare it with the theorem statement.
\end{hint}
\begin{solution}
The theorem hypotheses are essential because the chapter's quotient, localization, exactness, or finiteness mechanism can fail outside them. A correct answer identifies the dropped hypothesis and exhibits a concrete failure.
\end{solution}

% END VOL05-EXPANSION V06-exercises-01
