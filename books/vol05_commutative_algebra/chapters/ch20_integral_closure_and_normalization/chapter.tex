\chapter{Integral Closure and Normalization}
\label{ch:v20}

Integral closure collects all elements of a fraction field integral over the base ring. Normalization repairs non-normal domains by adjoining these integral elements.

\section*{Learning goals}
The reader should be able to state the definitions precisely, prove the core structural results, move between global and localized formulations, and audit finiteness and exactness hypotheses.

\section*{Conceptual roadmap}
\[
\boxed{\text{presentation or universal property}\;\longrightarrow\;\text{exactness/localization}\;\longrightarrow\;\text{structural consequence}.}
\]

\section{Integrally closed domains}
A domain is integrally closed when every element of its fraction field integral over the ring already lies in the ring.

\section{Integral closure}
The integral closure of $A$ in an extension field consists of all elements integral over $A$.

\section{Normalization}
For a domain, normalization is its integral closure in its fraction field.

\section{UFDs and normality}
Unique factorization domains are integrally closed.

\section{Localization}
Integral closure behaves well under localization.

\section{Finite normalization}
For many finitely generated algebras over fields, normalization is finite, though this requires hypotheses.

\section{Singular examples}
Cusp-like rings such as $k[t^2,t^3]$ normalize to $k[t]$.

\section{Prime spectra}
Normalization induces an integral map on spectra and separates certain singular algebraic behaviors.

\section{Conductor}
The conductor measures the largest ideal shared by a ring and its normalization.

\section{Core structural results}

\begin{theorem}[UFDs are integrally closed]\label{thm:v20-01}
Every unique factorization domain is integrally closed in its fraction field.
\begin{proof}
Write an integral element as $a/b$ in lowest terms. A monic equation implies $a^n$ is divisible by $b$; coprimality and unique factorization force $b$ to be a unit.
\end{proof}
\end{theorem}

\begin{theorem}[Localization of integral closure]\label{thm:v20-02}
If $B$ is the integral closure of a domain $A$ in a field extension and $S\subset A$ is multiplicative, then $S^{-1}B$ is the integral closure of $S^{-1}A$ in the same localized field.
\begin{proof}
Localization preserves monic equations, giving one inclusion. For the reverse, clear finitely many denominators in a monic equation to show a suitable multiple of the element is integral over $A$, hence lies in $B$.
\end{proof}
\end{theorem}

\begin{theorem}[Normalization of a cusp ring]\label{thm:v20-03}
The normalization of $k[t^2,t^3]$ in $k(t)$ is $k[t]$.
\begin{proof}
The element $t$ is integral because it satisfies $X^2-t^2=0$ with coefficient $t^2$ in the subring. Since $k[t]$ is a PID and therefore integrally closed, no larger integral subring inside $k(t)$ is needed.
\end{proof}
\end{theorem}

\section{Worked examples}

\begin{example}[Integrally closed domains diagnostic]\label{ex:v20-worked-01}
Give a rigorous worked analysis of Integrally closed domains. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion. A domain is integrally closed when every element of its fraction field integral over the ring already lies in the ring. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{example}

\begin{example}[Integral closure diagnostic]\label{ex:v20-worked-02}
Give a rigorous worked analysis of Integral closure. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion. The integral closure of $A$ in an extension field consists of all elements integral over $A$. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{example}

\begin{example}[Normalization diagnostic]\label{ex:v20-worked-03}
Give a rigorous worked analysis of Normalization. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion. For a domain, normalization is its integral closure in its fraction field. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{example}

\begin{example}[UFDs and normality diagnostic]\label{ex:v20-worked-04}
Give a rigorous worked analysis of UFDs and normality. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion. Unique factorization domains are integrally closed. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{example}

\section{Solved dossiers}
Each dossier is a canonical solved problem. The provenance ledger records which retained corpus rules guided its topic and which dossiers were added freshly to complete the chapter.

\begin{problem}[Integrally closed domains diagnostic]\label{prob:v20-dossier-01}
Give a rigorous worked analysis of Integrally closed domains. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
A domain is integrally closed when every element of its fraction field integral over the ring already lies in the ring. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[Integral closure diagnostic]\label{prob:v20-dossier-02}
Give a rigorous worked analysis of Integral closure. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
The integral closure of $A$ in an extension field consists of all elements integral over $A$. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[Normalization diagnostic]\label{prob:v20-dossier-03}
Give a rigorous worked analysis of Normalization. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
For a domain, normalization is its integral closure in its fraction field. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[UFDs and normality diagnostic]\label{prob:v20-dossier-04}
Give a rigorous worked analysis of UFDs and normality. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
Unique factorization domains are integrally closed. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[Localization diagnostic]\label{prob:v20-dossier-05}
Give a rigorous worked analysis of Localization. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
Integral closure behaves well under localization. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[Finite normalization diagnostic]\label{prob:v20-dossier-06}
Give a rigorous worked analysis of Finite normalization. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
For many finitely generated algebras over fields, normalization is finite, though this requires hypotheses. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[Singular examples diagnostic]\label{prob:v20-dossier-07}
Give a rigorous worked analysis of Singular examples. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
Cusp-like rings such as $k[t^2,t^3]$ normalize to $k[t]$. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[Prime spectra diagnostic]\label{prob:v20-dossier-08}
Give a rigorous worked analysis of Prime spectra. State the ring, module, finiteness, localization, or homological hypotheses and derive the central conclusion.
\end{problem}
\begin{solution}
Normalization induces an integral map on spectra and separates certain singular algebraic behaviors. The decisive step is to use the universal property, exactness criterion, localization test, or finite-generation mechanism appropriate to the algebraic structure.
\end{solution}

\begin{problem}[UFDs are integrally closed proof dossier]\label{prob:v20-dossier-09}
Prove the following structural statement and identify the hypothesis that makes the conclusion work: Every unique factorization domain is integrally closed in its fraction field.
\end{problem}
\begin{solution}
Write an integral element as $a/b$ in lowest terms. A monic equation implies $a^n$ is divisible by $b$; coprimality and unique factorization force $b$ to be a unit. This proof isolates the reusable algebraic mechanism behind the result.
\end{solution}

\begin{problem}[Localization of integral closure proof dossier]\label{prob:v20-dossier-10}
Prove the following structural statement and identify the hypothesis that makes the conclusion work: If $B$ is the integral closure of a domain $A$ in a field extension and $S\subset A$ is multiplicative, then $S^{-1}B$ is the integral closure of $S^{-1}A$ in the same localized field.
\end{problem}
\begin{solution}
Localization preserves monic equations, giving one inclusion. For the reverse, clear finitely many denominators in a monic equation to show a suitable multiple of the element is integral over $A$, hence lies in $B$. This proof isolates the reusable algebraic mechanism behind the result.
\end{solution}

\begin{problem}[Normalization of a cusp ring proof dossier]\label{prob:v20-dossier-11}
Prove the following structural statement and identify the hypothesis that makes the conclusion work: The normalization of $k[t^2,t^3]$ in $k(t)$ is $k[t]$.
\end{problem}
\begin{solution}
The element $t$ is integral because it satisfies $X^2-t^2=0$ with coefficient $t^2$ in the subring. Since $k[t]$ is a PID and therefore integrally closed, no larger integral subring inside $k(t)$ is needed. This proof isolates the reusable algebraic mechanism behind the result.
\end{solution}

\begin{problem}[Chapter synthesis]\label{prob:v20-dossier-12}
Connect the definitions and principal structural theorems of this chapter into one reusable commutative-algebra strategy.
\end{problem}
\begin{solution}
Integral closure collects all elements of a fraction field integral over the base ring. Normalization repairs non-normal domains by adjoining these integral elements. The recurring strategy is to encode the algebraic object by kernels, quotients, localization, finite presentation, or a resolution, apply the corresponding universal or exactness principle, and then recover the desired global or local conclusion.
\end{solution}

\section{Exercises with complete solutions}

\begin{exercise}\label{exr:v20-01}
State and apply the central principle behind Integrally closed domains. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
A domain is integrally closed when every element of its fraction field integral over the ring already lies in the ring. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\begin{exercise}\label{exr:v20-02}
State and apply the central principle behind Integral closure. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
The integral closure of $A$ in an extension field consists of all elements integral over $A$. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\begin{exercise}\label{exr:v20-03}
State and apply the central principle behind Normalization. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
For a domain, normalization is its integral closure in its fraction field. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\begin{exercise}\label{exr:v20-04}
State and apply the central principle behind UFDs and normality. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
Unique factorization domains are integrally closed. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\begin{exercise}\label{exr:v20-05}
State and apply the central principle behind Localization. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
Integral closure behaves well under localization. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\begin{exercise}\label{exr:v20-06}
State and apply the central principle behind Finite normalization. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
For many finitely generated algebras over fields, normalization is finite, though this requires hypotheses. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\begin{exercise}\label{exr:v20-07}
State and apply the central principle behind Singular examples. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
Cusp-like rings such as $k[t^2,t^3]$ normalize to $k[t]$. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\begin{exercise}\label{exr:v20-08}
State and apply the central principle behind Prime spectra. Give one concrete algebraic consequence.
\end{exercise}
\begin{hint}
Begin from the exact definition or theorem hypothesis in the corresponding section.
\end{hint}
\begin{solution}
Normalization induces an integral map on spectra and separates certain singular algebraic behaviors. The same principle can then be reused in later localization, support, base-change, resolution, Tor, or Ext arguments.
\end{solution}

\section*{Chapter summary}
The chapter now contributes a canonical solved layer to the progression from rings and modules through localization, Noetherian methods, integral dependence, and derived functors.
