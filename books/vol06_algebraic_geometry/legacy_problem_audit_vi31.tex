\chapter*{VI/31 Legacy-Problem and Scope Audit}
\addcontentsline{toc}{chapter}{VI/31 Legacy-Problem and Scope Audit}
\label{audit:vi31}

\textbf{Audit marker:} \texttt{VI31\_CODIMENSION\_INSTANCE\_AUDIT\_2026-08-23}.

\section*{VI-SCOPE-054: AG8 codimension/height stream}
The live coverage atlas routes \texttt{VI-SCOPE-054} from \texttt{theory-of-algebraic-geometry-8.tex},
\texttt{T03.EXERCISE\_CHILDREN}, selector \texttt{descendant-of(codimension|height)}, uniquely to VI/31.
The canonical theory row \texttt{AG8-T03-CODIMENSION} reconstructs height, codimension, the generic-point local-ring
formula, and the finite-type variety additivity theorem.

\section*{AG8 Problem 2 split}
The live ledger already routes \texttt{AG8-P2b}, the codimension slice of Problem 2, to VI/31.  VI.31.P1 preserves
both codimension statements owned by that slice:
\[
\operatorname{codim}(Y,X)=\inf_{y\in Y}\dim\OO_{X,y},
\qquad
\dim Y+\operatorname{codim}(Y,X)=\dim X
\]
for the source variety setting.  Global/open dimension stays in VI/30, function-field material stays in VI/27, and
the closed-point local-dimension statement stays in VI/32.

\section*{AG8 Problem 3 split}
After VI/30, the parent \texttt{AG8-P3} contains only parts (c) and (a).  VI/31 consumes exactly part (c) as
\texttt{AG8-P3c -> VI.31.P2}.  The source counterexample is
\[
X=\Spec k\sqcup\mathbb A^1_k,\qquad Y=\Spec k,
\]
where $Y$ is an irreducible component, so $\operatorname{codim}(Y,X)=0$ and
$\dim Y+\operatorname{codim}(Y,X)=0\neq1=\dim X$.
The parent row is therefore narrowed to VI/32 only, where part (a) belongs.  No synthetic extra legacy problem is
created; \texttt{AG8-P3c} is an explicit source subpart of the already-tracked multi-part Problem 3.

\section*{Boundary audit}
The equality $\operatorname{codim}(Y,X)=\dim\OO_{X,\eta_Y}$ is used here because it is the exact codimension bridge.
VI/32 retains the independent study of local dimension at arbitrary/closed points, tangent spaces, embedding dimension,
and the remaining AG8 Problem 3(a) failure. Codimension-one loci are introduced only numerically; divisor structures
remain reserved for the divisor chapters.
