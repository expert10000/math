\textbf{E1.} The length is $2$.\par

\textbf{E2.} $\dim k=0$.\par

\textbf{E3.} $\dim\mathbb Z=1$.\par

\textbf{E4.} $\dim k[x]=1$.\par

\textbf{E5.} $(0)\subsetneq(x)\subsetneq(x,y)$.\par

\textbf{E6.} It is the supremum of lengths of prime chains ending at $\mathfrak p$.\par

\textbf{E7.} Only primes disjoint from the multiplicative set remain, with inclusions preserved.\par

\textbf{E8.} They correspond to primes of $A$ containing $I$.\par

\textbf{E9.} $\Spec A$ and $\Spec A_{\rm red}$ have the same ordered prime set.\par

\textbf{E10.} $\max\{\dim A,\dim B\}$.\par

\textbf{E11.} It has dimension $0$.\par

\textbf{E12.} The dimension is $1$.\par

\textbf{E13.} The height is $1$.\par

\textbf{E14.} The height is $2$.\par

\textbf{E15.} There are prime chains of arbitrary finite length.\par

\textbf{E16.} The normalization is integral over the original domain; integral extensions preserve dimension.\par

\textbf{E17.} It is the supremum of chain lengths among primes lying in $D(f)$.\par

\textbf{E18.} It is the supremum of chain lengths inside the closed subset $V(I)$.\par

\textbf{E19.} Exactly when $\mathfrak p\subseteq\mathfrak q$.\par

\textbf{E20.} $V(x,y)\subsetneq V(x)\subsetneq V(0)$.\par

\textbf{E21.} It equals $n$.\par

\textbf{E22.} It is $0$.\par

\textbf{E23.} No.  Noetherian rings of infinite Krull dimension exist.\par

\textbf{E24.} For example, $\dim X=\operatorname{trdeg}_kK(X)$ for a variety is deferred to VI/30.\par

\textbf{E25.} VI/31.\par

\textbf{E26.} VI/32.\par
