\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E1]
Translate membership in $D(f)$ into non-membership of $f$ in a prime ideal and use primality for $fg$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E2]
A prime contains $f^n$ iff it contains $f$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E3]
$\mathfrak p\notin V(I)$ means $I\not\subseteq\mathfrak p$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E4]
If a prime contains every generator, it contains the whole ideal. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E5]
Take complements and use the radical description of closed-set containment. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E6]
Apply E5 in both directions. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E7]
If $f$ is not a unit, $(f)$ is contained in a maximal ideal. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E8]
Use $\sqrt{(0)}=\bigcap_{\mathfrak p\in\Spec A}\mathfrak p$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E9]
The complement of the union is $V(f_1,\dots,f_r)$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E10]
Take complements and translate inclusion of closed sets into radical containment. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E11]
An ideal-membership relation $f^N\in(g_\lambda)$ uses only finitely many generators. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E12]
$(p)$ contains $n$ exactly when $p\mid n$; $(0)$ never contains nonzero $n$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E13]
Inverting $t$ gives Laurent polynomials. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E14]
Use the universal property: both rings universally invert $f$ and $g$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E15]
Use the standard prime correspondence for localization at $S=\{1,f,f^2,\dots\}$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E16]
Track principal closed or principal open subsets under extension and contraction. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E17]
Expand $f\notin\varphi^{-1}(\mathfrak q)$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E18]
Both rings are universal for maps killing $I$ and inverting $f$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E19]
Once $\bar x$ is a unit, the equation $\bar x\bar y=0$ forces $\bar y=0$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
\begin{hint}
% PEDAGOGY-ENRICHED-VI
[VI.08.E20]
$\varepsilon$ is nilpotent, while $1+\varepsilon$ has inverse $1-\varepsilon$. Reduce the question to localization at the chosen element and the basic-open correspondence; track exactly which powers become invertible.
\end{hint}
