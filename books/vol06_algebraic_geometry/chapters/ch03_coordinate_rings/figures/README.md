# Figures for VI/03 - Coordinate Rings

Only canonical figures belong here.

Useful future figures:
- parabola `V(y-x^2)` with the relation `y=x^2`;
- hyperbola `V(xy-1)` and the Laurent-coordinate interpretation;
- two coordinate axes with zero divisors in `k[x,y]/(xy)`;
- geometry/algebra arrow diagram `X -> k[X]`.

Do not place morphism pullback diagrams here; those belong to VI/04.
