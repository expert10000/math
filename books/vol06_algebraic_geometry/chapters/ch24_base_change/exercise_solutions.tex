\paragraph{Solution 1.} The square has upper-left $X\times_SS'$, upper-right $X$, lower-left $S'$, lower-right $S$, and is Cartesian by definition.
\paragraph{Solution 2.} A compatible pair $T\to X$, $T\to S$ is forced to use the structure map $T\to X\to S$, so $X$ represents the pullback.
\paragraph{Solution 3.} $k[x]\otimes_kK\cong K[x]$ by sending $x^n\otimes a$ to $ax^n$.
\paragraph{Solution 4.} It is $K[x]/(x^2+1)$, with factorization depending on $K$.
\paragraph{Solution 5.} Right exactness gives quotient by the image of $I\otimes_AA'$, namely the extended ideal.
\paragraph{Solution 6.} Both $B_b\otimes_AA'$ and $(B\otimes_AA')_{b\otimes1}$ universally make $b\otimes1$ invertible.
\paragraph{Solution 7.} It becomes $D(x)\subseteq\mathbb A^1_K=\Spec K[x]$.
\paragraph{Solution 8.} It becomes $\Spec K[x]/(x^2)$, so the doubled-point structure persists.
\paragraph{Solution 9.} Base change sends both compositions to identity compositions, so the two base-changed maps are inverse.
\paragraph{Solution 10.} $(B\otimes_AA')\otimes_{A'}A''\cong B\otimes_AA''$.
\paragraph{Solution 11.} It is $\mathbb C[x]/((x-i)(x+i))\cong\mathbb C\times\mathbb C$.
\paragraph{Solution 12.} Under $\mathbb C\times\mathbb C$, $(1,0)$ and $(0,1)$ are nontrivial idempotents.
\paragraph{Solution 13.} The result is $\mathbb Q[x]/(x^2-2)$.
\paragraph{Solution 14.} The result is $\mathbb F_2[x]/(x^2)$; interpret detailed nonreduced fiber geometry only in VI/25.
\paragraph{Solution 15.} Both sides represent maps compatible with $X$, $Y$, and the new base $S'$.
\paragraph{Solution 16.} It is the diagonal $X_{S'}\to X_{S'}\times_{S'}X_{S'}$.
\paragraph{Solution 17.} Pullback associativity carries $\Gamma_u$ to $\Gamma_{u_{S'}}$.
\paragraph{Solution 18.} Connectedness and irreducibility can fail after extension to $\mathbb C$, since the scalar extension is two points.
\paragraph{Solution 19.} $L\otimes_kL\cong L[u]/((u-s)^p)$ is nonreduced.
\paragraph{Solution 20.} Tensor product is right exact, so a quotient map remains surjective.
\paragraph{Solution 21.} Localization commutes with tensor product; glue the affine-local statement.
\paragraph{Solution 22.} If their composites in the base-changed source agree after mapping to the original source, monomorphism gives equality; the $S'$-map is already fixed.
\paragraph{Solution 23.} It is $K[x,y]/(xy)$, the same crossing equation over $K$.
\paragraph{Solution 24.} It is $\mathbb A^2_K$ because $k[x,y]\otimes_kK\cong K[x,y]$.
\paragraph{Solution 25.} $X_s=X\times_S\Spec\kappa(s)$.
\paragraph{Solution 26.} VI/24 develops the change-of-base operation and its formal algebra; VI/25 analyzes the resulting fibers over points and field extensions.