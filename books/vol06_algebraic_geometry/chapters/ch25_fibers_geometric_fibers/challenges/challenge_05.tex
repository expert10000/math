\begin{problem}[Challenge: Design a family with controlled special fiber]\label{chall:vi25-05}
Construct an affine family over $\mathbb A^1$ whose generic closed fibers are two distinct reduced points but whose special fiber at $0$ is one doubled point. Then modify the construction so the special fiber is empty instead.
\end{problem}
