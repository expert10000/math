\paragraph{Hint 1.} Base change along $\Spec\kappa(s)\to S$.
\paragraph{Hint 2.} Use $B\otimes_A\kappa(\mathfrak p)$.
\paragraph{Hint 3.} Set $t=0$.
\paragraph{Hint 4.} Set $t=a$ and solve $au=1$.
\paragraph{Hint 5.} Set $t=0$.
\paragraph{Hint 6.} Solve $y=0$ and $y=cx+1$.
\paragraph{Hint 7.} Set $c=0$.
\paragraph{Hint 8.} Tensor with $\mathbb Q$.
\paragraph{Hint 9.} Tensor with $\mathbb F_p$.
\paragraph{Hint 10.} Reduce $T^2+1$ modulo $2$.
\paragraph{Hint 11.} Use irreducibility of $T^2+1$.
\paragraph{Hint 12.} A prime integer is invertible in $\mathbb Q$.
\paragraph{Hint 13.} Use dual numbers.
\paragraph{Hint 14.} Use a product of fields.
\paragraph{Hint 15.} Choose an algebraically closed $\Omega/\kappa(s)$.
\paragraph{Hint 16.} Factor $t^2+1$ over $\mathbb C$.
\paragraph{Hint 17.} Use the previous tensor decomposition.
\paragraph{Hint 18.} Associate the relevant fiber products.
\paragraph{Hint 19.} Check whether $f$ vanishes at $\mathfrak p$.
\paragraph{Hint 20.} Tensor the quotient with $\kappa(\mathfrak p)$.
\paragraph{Hint 21.} Reduce $x^2-2$ modulo $2$.
\paragraph{Hint 22.} Use $T^p-a^p=(T-a)^p$.
\paragraph{Hint 23.} Use base change of products.
\paragraph{Hint 24.} Compare $\Spec K$ with $\Spec(k\times k)$.
\paragraph{Hint 25.} Think about splitting over an algebraic closure.
\paragraph{Hint 26.} Operation versus pointwise analysis.
