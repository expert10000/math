\paragraph{Solution 1.} $X_s$ is upper-left, $X$ upper-right, $\Spec\kappa(s)$ lower-left, and $S$ lower-right; the square is Cartesian.
\paragraph{Solution 2.} It is $B\otimes_A\kappa(\mathfrak p)$.
\paragraph{Solution 3.} The ring is $k[u]/(-1)=0$, so the fiber is empty.
\paragraph{Solution 4.} The ring is $k[u]/(au-1)\cong k$, so the fiber is one reduced point.
\paragraph{Solution 5.} It is $k[u]/(u^2)$, a doubled point.
\paragraph{Solution 6.} The intersection is $(-1/c,0)$ in the plane $z=c$.
\paragraph{Solution 7.} They are $y=0$ and $y=1$, hence parallel.
\paragraph{Solution 8.} It is $\Spec\mathbb Q[x]$.
\paragraph{Solution 9.} It is $\Spec\mathbb F_p[x]$.
\paragraph{Solution 10.} It is $\Spec\mathbb F_2[T]/((T+1)^2)$, nonreduced.
\paragraph{Solution 11.} It is $\Spec\mathbb F_{p^2}$.
\paragraph{Solution 12.} $\mathbb Q\otimes_{\mathbb Z}\mathbb F_p=0$, hence every closed fiber is empty.
\paragraph{Solution 13.} $\Spec k[\varepsilon]/(\varepsilon^2)$ is one topological point but nonreduced.
\paragraph{Solution 14.} $\Spec(k\times k)$ is reduced and has two components.
\paragraph{Solution 15.} For $\bar s:\Spec\Omega\to S$ with $\Omega$ algebraically closed, $X_{\bar s}=X\times_S\Spec\Omega$.
\paragraph{Solution 16.} $\mathbb C\otimes_{\mathbb R}\mathbb C\cong\mathbb C\times\mathbb C$ by CRT.
\paragraph{Solution 17.} Its complex base change is two points, so it becomes reducible.
\paragraph{Solution 18.} $(X\times_SS')\times_{S'}\Spec\kappa(s')\cong X_s\times_{\kappa(s)}\Spec\kappa(s')$ by associativity.
\paragraph{Solution 19.} It is $\Spec\kappa(\mathfrak p)$ if $f\notin\mathfrak p$ and empty if $f\in\mathfrak p$.
\paragraph{Solution 20.} It is $\Spec\kappa(\mathfrak p)$ if $I\subseteq\mathfrak p$ and empty otherwise.
\paragraph{Solution 21.} It is $\Spec\mathbb F_2[x]/(x^2)$.
\paragraph{Solution 22.} For $k=\mathbb F_p(a^p)\subset L=k(a)$, $L\otimes_kL\cong L[T]/((T-a)^p)$ is nonreduced.
\paragraph{Solution 23.} $(X\times_SY)_s\cong X_s\times_{\kappa(s)}Y_s$ by base-change compatibility.
\paragraph{Solution 24.} $\Spec K$ is one point with residue field $K$; two rational points correspond to a product ring $k\times k$.
\paragraph{Solution 25.} For example, an ordinary irreducible fiber can split into several components after algebraic closure.
\paragraph{Solution 26.} VI/24 develops arbitrary change of base; VI/25 specializes to residue-field and geometric-point base changes and studies the resulting fibers.
