\begin{problem}[Functoriality of $\Spec$]\label{prob:vi19-p03}
For $A\xrightarrow{\varphi}B\xrightarrow{\psi}C$, prove $\Spec(\psi\circ\varphi)=\Spec(\varphi)\circ\Spec(\psi)$ as morphisms of locally ringed spaces, and prove the identity law.
\end{problem}

\ifdefined\FullProblemDossiers
\paragraph{Why this problem matters.}
Without functoriality, the ring--geometry dictionary would not respect composition.

\paragraph{Useful ideas before starting.}
Check first on primes, then on localized section maps.

\paragraph{Guided hints.}
\begin{enumerate}
\item Contract a prime of $C$ twice.
\item Compare $A_a\to C_{\psi(\varphi(a))}$ in one and two steps.
\item Do the same for identity maps.
\end{enumerate}

\begin{solution}
For $\mfp\in\Spec C$,
\[
(\psi\circ\varphi)^{-1}(\mfp)=\varphi^{-1}(\psi^{-1}(\mfp)),
\]
so the continuous maps compose correctly.  On $D(a)$ the direct section map is
\[
A_a\to C_{\psi(\varphi(a))},\qquad r/a^n\mapsto\psi(\varphi(r))/\psi(\varphi(a))^n,
\]
which is exactly the composite through $B_{\varphi(a)}$.  Hence the sheaf maps agree.  The identity map contracts every prime to itself and induces identity maps on all localizations.  Thus $\Spec$ is contravariantly functorial.
\end{solution}

\paragraph{What happened mathematically.}
Composition of geometric maps is just composition of algebra maps read backwards.

\paragraph{Extensions.}
Use the result to translate a commutative ring diagram into a reversed commutative diagram of affine schemes.

\paragraph{Provenance.}
\textbf{Canonical derivative of AG3 Problem 12 / VI.19.P1; not an additional legacy migration.}
\else
\paragraph{Provenance.} \textbf{Canonical derivative of AG3 Problem 12 / VI.19.P1; not an additional legacy migration.}
\fi
