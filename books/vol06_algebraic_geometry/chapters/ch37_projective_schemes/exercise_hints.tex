\begin{enumerate}[label=\textbf{\arabic*.},leftmargin=*]
\item Localize the exact quotient sequence, then take degree zero.
\item Lift primes along the quotient map.
\item Remove \(x_2\); the quotient is a polynomial ring in two variables.
\item Divide by \(X^2\), \(Y^2\), or \(Z^2\) on the corresponding chart.
\item \(\dim S=n+1\) and a nonzero principal ideal has height one on each minimal component.
\item Add the ideals \((x_0)+(x_1)\).
\item Use primality of the variables in the UFD.
\item Compare the class of \(x_0\) modulo \(x_0^2\).
\item First show \(x_0\in I^{\mathrm{sat}}\); the reverse inclusion follows from containment in \((x_0)\).
\item No homogeneous prime avoiding \(S_+\) can contain a power of \(S_+\).
\item Use quasi-compactness of \(\Proj S\) or the radical criterion \(S_+\subseteq\sqrt I\).
\item Introduce \(x_0\) and homogenize to degree two.
\item The equation becomes \(x_1x_2-x_0^2\).
\item Keep the same homogeneous equation in affine space.
\item Localization preserves reducedness; so does taking subrings such as degree-zero parts.
\item Localizations of a domain are domains.
\item Invert \(x_0\) or \(x_1\) and use the relations \(x_0z=x_1z=0\).
\item Factor \(x_0x_1\) and inspect minimal primes.
\item Intersect the two point ideals.
\item Dehomogenize on \(x_0\ne0\); the local ideal is \((u,v)^2\).
\item One comes from the quadratic Veronese presentation.
\item If \(\mathfrak m^r g\subseteq I\), then \(x_i^r g\in I\).
\item Add \((x_0)\) to the saturated homogeneous ideal of the closure.
\item On \(x_1\ne0\), the class \(x_0/x_1\) can be nonzero nilpotent modulo \(x_0^2\).
\end{enumerate}
