\paragraph{Hint for E1.} Use integral closedness inside the fraction field.

\paragraph{Hint for E2.} Normality of an affine scheme is exactly normality of its coordinate domain.

\paragraph{Hint for E3.} Its fraction field is itself.

\paragraph{Hint for E4.} Clear denominators in a monic relation.

\paragraph{Hint for E5.} Both rings lie inside the same fraction field and contain $A$.

\paragraph{Hint for E6.} Every element of $\overline A$ is integral over $A$.

\paragraph{Hint for E7.} Apply lying over to the integral extension.

\paragraph{Hint for E8.} Then $A=\overline A$.

\paragraph{Hint for E9.} Use $T^2-t^2$.

\paragraph{Hint for E10.} $k[t]$ is a PID and hence integrally closed.

\paragraph{Hint for E11.} Use $\overline{A_f}=(\overline A)_f$.

\paragraph{Hint for E12.} Cover the open by distinguished affine opens.

\paragraph{Hint for E13.} Both correspond to the zero prime on affine charts.

\paragraph{Hint for E14.} Compare fraction fields on any nonempty affine chart.

\paragraph{Hint for E15.} The first normalization is already normal.

\paragraph{Hint for E16.} Use unique factorization to control denominators.

\paragraph{Hint for E17.} Polynomial rings over fields are UFDs.

\paragraph{Hint for E18.} It satisfies $T^2-T-1$.

\paragraph{Hint for E19.} Solve $t^2-1=0$.

\paragraph{Hint for E20.} Use the cusp $k[t^2,t^3]$.

\paragraph{Hint for E21.} Use the finite integral-closure theorem for finitely generated algebras over a field.

\paragraph{Hint for E22.} Dominant maps from normal integral schemes factor uniquely.

\paragraph{Hint for E23.} Localize $\overline A$ at a prime over $\mathfrak p$.

\paragraph{Hint for E24.} Purely inseparable tensor products may acquire nilpotents.

\paragraph{Hint for E25.} The function fields are canonically equal.

\paragraph{Hint for E26.} For example, height-one/divisor criteria or codimension theory.
