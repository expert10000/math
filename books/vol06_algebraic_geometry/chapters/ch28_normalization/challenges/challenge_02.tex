\begin{problem}[Challenge 2: Normalization of a node in detail]\label{chal:vi28-02}
Starting from $A=k[x,y]/(y^2-x^2(x+1))$ in characteristic not $2$, identify its fraction field with $k(t)$ via $t=y/x$, prove $t$ is integral, and determine the integral closure.
\end{problem}
