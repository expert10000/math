\chapter{Normalization}\label{ch:vi28-normalization}

VI/27 placed every nonempty affine chart of an integral scheme inside one common function field.  Normalization
uses that field to repair failures of integral closedness without changing the rational functions.  On an affine
integral scheme $X=\Spec A$, one replaces $A$ by its integral closure $\overline A$ in $K(X)=\Frac(A)$.  The
compatibility of integral closure with localization makes these affine repairs glue.

\section{Normal domains and normal schemes}
\begin{definition}[Normal domain]\label{def:vi28-normal-domain}
A domain $A$ with fraction field $K$ is \emph{normal} if it is integrally closed in $K$: every $z\in K$ integral
over $A$ already lies in $A$.
\end{definition}

\begin{definition}[Normal integral scheme]\label{def:vi28-normal-scheme}
An integral scheme $X$ is \emph{normal} if every local ring $\OO_{X,x}$ is an integrally closed domain.
Equivalently, every affine open $U=\Spec A\subseteq X$ has $A$ a normal domain.
\end{definition}

Normality is thus strictly stronger than integrality.  The cusp is integral but not normal; affine space over a
field is normal.

\input{chapters/ch28_normalization/figures/figure_01}

\section{Integral closure inside the function field}
Let $A$ be a domain and $K=\Frac(A)$.  Its integral closure in $K$ is
\[
\overline A=\{z\in K: z\text{ is integral over }A\}.
\]
The algebraic theory of integral dependence and integral closure belongs to Volume V.  Here we use its geometric
consequence: $A\subseteq\overline A\subseteq K$, and $\overline A$ is a normal domain with the same fraction field
$K$.

\begin{proposition}[Minimal normal enlargement]\label{prop:vi28-minimal-normal}
If $A\subseteq B\subseteq K$ and $B$ is integrally closed in $K$, then $\overline A\subseteq B$.  Thus
$\overline A$ is the smallest normal intermediate domain containing $A$ inside $K$.
\end{proposition}

\input{chapters/ch28_normalization/figures/figure_02}

\section{Affine normalization}
\begin{definition}[Affine normalization]\label{def:vi28-affine-normalization}
For an integral affine scheme $X=\Spec A$ with $K=\Frac(A)$, define
\[
X^\nu:=\Spec\overline A,
\]
where $\overline A$ is the integral closure of $A$ in $K$.  The inclusion $A\hookrightarrow\overline A$ induces
the \emph{normalization morphism}
\[
\nu:X^\nu\longrightarrow X.
\]
\end{definition}

The morphism $\nu$ is integral and dominant; by lying over it is surjective.  It preserves the function field, so it is birational in the function-field sense.  In the finite-type setting of varieties, it is moreover an isomorphism over a nonempty dense open.  If $A$ is already normal, $\nu$ is an isomorphism.

\input{chapters/ch28_normalization/figures/figure_03}

\section{Normalization commutes with localization}
The decisive gluing fact is
\[
\overline{S^{-1}A}=S^{-1}\overline A
\]
for every multiplicative set $S\subseteq A$.  In particular,
\[
\overline{A_f}=(\overline A)_f.
\]
Hence the normalization of $D(f)\subseteq\Spec A$ is the inverse image of $D(f)$ inside $\Spec\overline A$.

\begin{theorem}[Restriction to opens]\label{thm:vi28-open-restriction}
If $X$ is integral with normalization $\nu:X^\nu\to X$ and $U\subseteq X$ is nonempty open, then
$\nu^{-1}(U)\to U$ is the normalization of $U$.
\end{theorem}

\input{chapters/ch28_normalization/figures/figure_04}

\section{Global normalization by gluing}
Let $X$ be integral and choose an affine cover $X=\bigcup_i U_i$ with $U_i=\Spec A_i$.  Normalize each chart:
\[
U_i^\nu=\Spec\overline{A_i}.
\]
On overlaps, common distinguished refinements from VI/20 and localization compatibility identify the normalized
pieces canonically.  These identifications satisfy the cocycle condition, so the $U_i^\nu$ glue to a normal
integral scheme $X^\nu$ and the affine maps glue to
\[
\nu:X^\nu\to X.
\]
The construction is independent of the chosen affine cover.

\input{chapters/ch28_normalization/figures/figure_05}

\section{Universal property}
Normalization is characterized by a factorization property.

\begin{theorem}[Universal property of normalization]\label{thm:vi28-universal}
Let $X$ be integral, let $\nu:X^\nu\to X$ be its normalization, and let $Z$ be a normal integral scheme.  Every
dominant morphism $f:Z\to X$ factors uniquely through $\nu$:
\[
\begin{tikzcd}
Z \arrow[rr,dashed,"\widetilde f"] \arrow[dr,"f"'] && X^\nu \arrow[dl,"\nu"]\\
& X &
\end{tikzcd}
\]
\end{theorem}

On affine charts, an element of $\overline A$ is integral over $A$; after mapping into $K(Z)$ it is integral over
the corresponding normal coordinate ring and therefore lies in that ring.  Uniqueness follows from agreement on
the common function field.

\input{chapters/ch28_normalization/figures/figure_06}

\section{The cusp: adjoining the missing parameter}
Let
\[
A=k[t^2,t^3]\subset k[t].
\]
Then $K=\Frac(A)=k(t)$ and $t$ is integral over $A$ because it satisfies
\[
T^2-t^2=0.
\]
Thus $A$ is not normal.  Its integral closure in $k(t)$ is $k[t]$, so the normalization is
\[
\mathbb A^1_k=\Spec k[t]\longrightarrow\Spec k[t^2,t^3].
\]
Geometrically this parametrizes the cusp $y^2=x^3$ by $x=t^2$, $y=t^3$.

\input{chapters/ch28_normalization/figures/figure_07}

\section{Normalization can separate branches}
For a nodal integral curve, the normalization need not be injective on points.  For example, when the displayed
curve is integral (for instance over a field of characteristic not $2$), the nodal curve
\[
y^2=x^2(x+1)
\]
admits the parametrization
\[
x=t^2-1,\qquad y=t(t^2-1).
\]
The two values $t=1$ and $t=-1$ map to the node $(0,0)$.  Thus normalization preserves the function field while
it can separate distinct branches through a singular point.

\input{chapters/ch28_normalization/figures/figure_08}

\section{Finite normalization}
The normalization morphism is always integral, but it is not automatically finite for an arbitrary domain.  A
central finite-type case is safe and important:

\begin{theorem}[Finite normalization over a field]\label{thm:vi28-finite-normalization}
If $A$ is a finitely generated domain over a field $k$, then its integral closure in $\Frac(A)$ is a finite
$A$-module.  Consequently the normalization of an integral finite-type $k$-scheme is finite.
\end{theorem}

This theorem uses the finiteness theory of integral closure developed in commutative algebra.  It is the bridge
from an abstract universal construction to a finite geometric map for algebraic varieties.

\input{chapters/ch28_normalization/figures/figure_09}

\section{Warnings and boundaries}
Normalization is not the same as reduction: reduction removes nilpotents without changing the topological space,
whereas normalization applies to an integral scheme and may change the point set above singularities.  Normality
can also fail after arbitrary field extension in positive characteristic, so normalization does not commute
naively with every base change.

VI/28 does not reprove the algebraic theory of integral extensions from Volume V, and it does not yet analyze
codimension-one criteria, discrete valuations, or divisor class groups.  Those belong to later chapters.  The
next chapter, VI/29, begins the dimension arc.

\section{Examples}
\input{chapters/ch28_normalization/examples}
\input{chapters/ch28_normalization/advanced_examples}

\section{Problem dossiers}
\input{chapters/ch28_normalization/problems/problem_01}
\input{chapters/ch28_normalization/problems/problem_02}
\input{chapters/ch28_normalization/problems/problem_03}
\input{chapters/ch28_normalization/problems/problem_04}
\input{chapters/ch28_normalization/problems/problem_05}
\input{chapters/ch28_normalization/problems/problem_06}
\input{chapters/ch28_normalization/problems/problem_07}
\input{chapters/ch28_normalization/problems/problem_08}
\input{chapters/ch28_normalization/problems/problem_09}
\input{chapters/ch28_normalization/problems/problem_10}
\input{chapters/ch28_normalization/problems/problem_11}
\input{chapters/ch28_normalization/problems/problem_12}
\input{chapters/ch28_normalization/problems/problem_13}
\input{chapters/ch28_normalization/problems/problem_14}
\input{chapters/ch28_normalization/problems/problem_15}
\input{chapters/ch28_normalization/problems/problem_16}
\input{chapters/ch28_normalization/problems/problem_17}
\input{chapters/ch28_normalization/problems/problem_18}
\input{chapters/ch28_normalization/problems/problem_19}
\input{chapters/ch28_normalization/problems/problem_20}

\section{Exercises}
\input{chapters/ch28_normalization/exercises}
\subsection*{Hints}
\input{chapters/ch28_normalization/exercise_hints}
\subsection*{Solutions}
\input{chapters/ch28_normalization/exercise_solutions}

\section{Challenges}
\input{chapters/ch28_normalization/challenges/challenge_01}
\input{chapters/ch28_normalization/challenges/challenge_02}
\input{chapters/ch28_normalization/challenges/challenge_03}
\input{chapters/ch28_normalization/challenges/challenge_04}
\input{chapters/ch28_normalization/challenges/challenge_05}

\section{Chapter synthesis}
Normalization keeps the function field fixed and enlarges every affine coordinate ring only by elements already
forced integrally inside that field.  Localization compatibility turns this local algebra into a global scheme,
and the universal property makes the result canonical.  In finite-type geometry the normalization map is finite,
so singular integral models can be compared to a normal model without leaving the same birational class.

\input{chapters/ch28_normalization/figures/figure_10}
