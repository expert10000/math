\begin{problem}[Sheafification preserves coproducts]
Use the adjunction to prove that sheafification preserves arbitrary coproducts. In particular, for presheaves $\{\mathcal F_i\}_{i\in I}$ identify
\[
a\!\left(\coprod_{i\in I}^{\PreSh}\mathcal F_i\right)
\cong
\coprod_{i\in I}^{\Sh}a(\mathcal F_i).
\]
\end{problem}

\paragraph{Why this problem matters.}
This is the first structural payoff of recognizing sheafification as a left adjoint.

\paragraph{Useful ideas before starting.}
Use the universal property, stalk preservation, and the fact that a morphism of sheaves is an isomorphism when it is so on every stalk.  Keep the distinction between a presheaf statement on sections and a local statement on germs explicit.

\paragraph{Guided hints.}
Test both sides by maps into an arbitrary sheaf $\mathcal G$ and use the universal properties of coproduct and adjunction.

\ifdefined\FullProblemDossiers
\begin{solution}
For every sheaf $\mathcal G$,
\[
\Hom_{\Sh}\!\left(a(\coprod_i\mathcal F_i),\mathcal G\right)
\cong
\Hom_{\PreSh}\!\left(\coprod_i\mathcal F_i,i\mathcal G\right)
\cong
\prod_i\Hom_{\PreSh}(\mathcal F_i,i\mathcal G)
\]
\[
\cong
\prod_i\Hom_{\Sh}(a\mathcal F_i,\mathcal G)
\cong
\Hom_{\Sh}\!\left(\coprod_i^{\Sh}a\mathcal F_i,\mathcal G\right).
\]
These bijections are natural in $\mathcal G$. By the Yoneda principle the two source objects are canonically isomorphic. Thus $a$ preserves coproducts, as every left adjoint must.
\end{solution}

\paragraph{What happened mathematically.}
Later quotient and cokernel constructions will use the same left-adjoint philosophy, but their specific legacy problems remain reserved for VI/15.

\paragraph{Alternative approaches.}
Whenever possible one may replace the espace-etale model by the two-plus construction, or conversely replace a cover/refinement argument by the universal property.  The two approaches agree because both construct the same associated sheaf.

\paragraph{Extensions.}
In VI/15, compare objectwise cokernels with sheaf cokernels.

\paragraph{Related problems.}
Compare with VI.14.P1 for the complete legacy construction and with the later sheaf chapters for operations that are deliberately not migrated here.

\paragraph{Provenance.}
New advanced synthesis problem from the adjunction; it does not migrate AG1 Exercise 5 or AG2 Problem 8.
\else
\paragraph{Provenance.}
New advanced synthesis problem from the adjunction; it does not migrate AG1 Exercise 5 or AG2 Problem 8.
\fi
