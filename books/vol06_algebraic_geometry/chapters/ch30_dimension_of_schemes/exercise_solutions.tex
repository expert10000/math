\paragraph{Solution VI.30.E1.} There is no strict inclusion between prime ideals, so every prime chain has length $0$.

\paragraph{Solution VI.30.E2.} $(0)\subsetneq(x)\subsetneq(x,y)$ has length $2$.

\paragraph{Solution VI.30.E3.} $\dim k[x_1,x_2,x_3,x_4]=4$, hence $\dim\mathbb A^4_k=4$.

\paragraph{Solution VI.30.E4.} Always $\dim Y\le2$. Without more hypotheses it may be $2$, $1$, $0$, or $-\infty$ for the empty subset under that convention.

\paragraph{Solution VI.30.E5.} A nilpotent closed immersion is a homeomorphism on underlying spaces, so the irreducible-closed chains are identical.

\paragraph{Solution VI.30.E6.} The dimension is $\max\{2,5\}=5$.

\paragraph{Solution VI.30.E7.} For example $D(t)$ has ring $k[t,t^{-1}]$, still of dimension $1$.

\paragraph{Solution VI.30.E8.} The primes of $k[x,y]_{(x)}$ correspond to primes of $k[x,y]$ contained in $(x)$, namely $(0)$ and $(x)$. $D(x)$ excludes $(x)$, leaving $(0)$.

\paragraph{Solution VI.30.E9.} It is $3$.

\paragraph{Solution VI.30.E10.} The transcendence degree is $2$, so $\dim X=2$.

\paragraph{Solution VI.30.E11.} The dimension is $0$.

\paragraph{Solution VI.30.E12.} Dominance would give $K(Y)\hookrightarrow K(X)$, forcing $2=\dim Y\le\dim X=1$, impossible.

\paragraph{Solution VI.30.E13.} The transcendence degrees agree, so $\dim X=4$.

\paragraph{Solution VI.30.E14.} The dimension is $10$.

\paragraph{Solution VI.30.E15.} The supremum of component dimensions is $0$.

\paragraph{Solution VI.30.E16.} Yes. In $\Spec k[x,y]_{(x)}$, the dense open $D(x)$ has dimension $0$ while the scheme has dimension $1$.

\paragraph{Solution VI.30.E17.} The dimension is $d+1$.

\paragraph{Solution VI.30.E18.} Reduction removes nilpotents and changes the structure sheaf while preserving the topological space, so dimension cannot detect that difference.

\paragraph{Solution VI.30.E19.} Both dimensions are $1$.

\paragraph{Solution VI.30.E20.} The dimensions are equal when the relevant integral extension is faithful/surjective on spectra; in the standard integral-extension setup, $\dim B=\dim A$.

\paragraph{Solution VI.30.E21.} The one-point space has no strict nontrivial chain, hence dimension $0$.

\paragraph{Solution VI.30.E22.} No. If $X=\mathbb A^1_k\sqcup\mathbb A^1_k$ and $Y$ is one component, then $Y\subsetneq X$ is closed but both dimensions equal $1$.

\paragraph{Solution VI.30.E23.} No. Every nonempty proper open subset of $\mathbb A^1_k$, such as $D(t)$, still has dimension $1$.

\paragraph{Solution VI.30.E24.} $X=\coprod_{n\ge0}\mathbb A^n_k$ has $\dim X=\infty$.

\paragraph{Solution VI.30.E25.} Global dimension measures chains of irreducible closed subsets of all of $X$. Local dimension at $x$ is $\dim\mathcal O_{X,x}$ and measures prime chains in the local ring; it is developed in VI/32.

\paragraph{Solution VI.30.E26.} Dimension is an absolute chain length invariant. Codimension measures how deeply a closed irreducible subset sits inside an ambient scheme and is developed in VI/31.