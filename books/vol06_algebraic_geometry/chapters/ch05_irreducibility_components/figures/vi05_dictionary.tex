\begin{figure}[htbp]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=12mm and 15mm,
  box/.style={draw,rounded corners,align=center,inner sep=7pt,text width=34mm},
  arrow/.style={-{Latex[length=2.4mm]},thick},
  iff/.style={<->,>=Latex,thick}
]
\node[box] (prime) {$I(X)$ prime};
\node[box,right=of prime] (domain) {$k[X]$ an integral domain};
\node[box,right=of domain] (irr) {$X$ irreducible};
\node[box,right=of irr] (conn) {$X$ connected};
\node[box,below=of conn] (idem) {$k[X]$ has only\\trivial idempotents};
\node[box,left=of idem] (prod) {no nontrivial product\\decomposition of $k[X]$};
\draw[iff] (prime)--(domain);
\draw[iff] (domain)--(irr);
\draw[arrow] (irr)--(conn);
\draw[iff] (conn)--node[right,align=left]{\scriptsize $k$ algebraically\\\scriptsize closed} (idem);
\draw[iff] (idem)--(prod);
\end{tikzpicture}}
\caption{The two ``one-piece'' dictionaries.  Irreducibility is detected by primality and the absence of zero divisors; connectedness is detected by idempotents and product splittings.}
\label{fig:vi05-one-piece-dictionary}
\end{figure}
