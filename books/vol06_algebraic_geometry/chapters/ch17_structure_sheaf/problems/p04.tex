\begin{problem}[Recover the ring from global sections]\label{prob:vi17-p04}
Show that the canonical map $A\to\Gamma(\Spec A,\OO_{\Spec A})$ is an isomorphism.
\end{problem}

\ifdefined\FullProblemDossiers
\paragraph{Why this problem matters.}
This is the first indication that an affine ring is not lost when it is converted into geometry.

\paragraph{Useful ideas.}
Use $\Spec A=D(1)$ and the principal-open formula.

\paragraph{Guided hints.}
\begin{enumerate}
\item Compute $A_1$.
\item Check that $a\in A$ maps to the family $a/1$ of germs.
\end{enumerate}

\begin{solution}
Since $X=D(1)$, Theorem~\ref{thm:vi17-basic-open} gives
\[\Gamma(X,\OO_X)=\OO_X(D(1))\cong A_1\cong A.\]
Under this identification $a$ maps to the section whose germ at every prime is $a/1$.
\end{solution}

\paragraph{What happened mathematically.}
Affine geometry keeps the original ring as the ring of global regular functions.

\paragraph{Extensions.}
VI/19 upgrades this observation to the universal property of affine schemes.

\paragraph{Related problems.}
VI.17.P3 and VI.17.P5 develop neighboring aspects of the same localization-sheaf calculus.

\paragraph{Provenance.}
\textbf{New synthesis; the corresponding universal morphism statement is deliberately reserved for AG3 Problem 12 in VI/19.}
\else
\paragraph{Provenance.} \textbf{New synthesis; the corresponding universal morphism statement is deliberately reserved for AG3 Problem 12 in VI/19.}
\fi
