\begin{problem}[The generic stalk is the function field]\label{prob:vi17-p11}
Let $A$ be a domain and $X=\Spec A$.  Prove $\OO_{X,(0)}\cong\Frac(A)$ and explain why every nonempty principal open contains $(0)$.
\end{problem}

\ifdefined\FullProblemDossiers
\paragraph{Why this problem matters.}
The generic point packages rational functions as local regular functions at the densest point.

\paragraph{Useful ideas.}
Use that the complement of $(0)$ is the set of nonzero elements.

\paragraph{Guided hints.}
\begin{enumerate}
\item Compute $A_{(0)}$.
\item $D(f)$ contains $(0)$ exactly when $f
e0$.
\end{enumerate}

\begin{solution}
Since $A$ is a domain, $A\setminus(0)$ is its set of nonzero elements, so $A_{(0)}=\Frac(A)$.  The stalk theorem gives $\OO_{X,(0)}=\Frac(A)$.  A principal open $D(f)$ contains $(0)$ iff $f\notin(0)$, i.e. iff $f\ne0$; therefore every nonempty principal open contains the generic point.
\end{solution}

\paragraph{What happened mathematically.}
The function field is not added externally; it is already a stalk of the structure sheaf.

\paragraph{Extensions.}
For a reduced reducible ring, compute the local rings at the minimal primes and interpret them as componentwise function fields when they are fields.

\paragraph{Related problems.}
VI.17.P10 and VI.17.P12 develop neighboring aspects of the same localization-sheaf calculus.

\paragraph{Provenance.}
\textbf{New synthesis.  The function-field part of legacy AG6 Problem 7 remains reserved for VI/27 and is not claimed here.}
\else
\paragraph{Provenance.} \textbf{New synthesis.  The function-field part of legacy AG6 Problem 7 remains reserved for VI/27 and is not claimed here.}
\fi
