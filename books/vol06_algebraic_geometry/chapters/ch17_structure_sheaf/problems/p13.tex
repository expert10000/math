\begin{problem}[Restriction maps and transitivity]\label{prob:vi17-p13}
For $f,g,h\in A$, identify the restriction maps along $D(fgh)\subseteq D(fg)\subseteq D(f)$ and prove their composition equals the direct restriction $A_f\to A_{fgh}$.
\end{problem}

\ifdefined\FullProblemDossiers
\paragraph{Why this problem matters.}
A sheaf is a contravariant functor, and here that functoriality is exactly associativity of localization.

\paragraph{Useful ideas.}
Write the maps as successive localizations at $g$ and $h$.

\paragraph{Guided hints.}
\begin{enumerate}
\item Use $(A_f)_g\cong A_{fg}$.
\item Then localize once more at $h$.
\end{enumerate}

% VI17_FULL_SOLUTION_REFINED
% VI17_FULL_SOLUTION_REFINED
\begin{solution}
The inclusion $D(fg)\subseteq D(f)$ corresponds to inverting $g$ after $f$ has already been inverted.  Thus the first restriction is
\[
A_f\longrightarrow (A_f)_g\cong A_{fg}.
\]
Similarly, the inclusion $D(fgh)\subseteq D(fg)$ gives
\[
A_{fg}\longrightarrow (A_{fg})_h\cong A_{fgh}.
\]
Their composite sends a representative $a/f^n$ to the same formal fraction, now interpreted in a ring in which $f,g,h$ are all units.

The direct inclusion $D(fgh)\subseteq D(f)$ gives the canonical localization map
\[
A_f\longrightarrow A_{fgh}.
\]
Both maps out of $A_f$ send every element of $A_f$ identically and make $g$ and $h$ invertible.  By the universal property of localization they are equal.  Therefore restriction maps compose exactly as required for a presheaf:
\[
\rho_{D(fg),D(fgh)}\circ\rho_{D(f),D(fg)}
=
\rho_{D(f),D(fgh)}.
\]
\end{solution}



\paragraph{What happened mathematically.}
Restriction compatibility is not extra structure pasted onto the localizations; it is built into their universal property.

\paragraph{Extensions.}
Formulate restrictions for arbitrary inclusions of principal opens using universal properties rather than chosen divisibility relations.

\paragraph{Related problems.}
VI.17.P12 and VI.17.P14 develop neighboring aspects of the same localization-sheaf calculus.

\paragraph{Provenance.}
\textbf{New synthesis from the principal-open structure-sheaf calculus.}
\else
\paragraph{Provenance.} \textbf{New synthesis from the principal-open structure-sheaf calculus.}
\fi
