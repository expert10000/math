\begin{problem}[Challenge: Idempotents from clopen decompositions]\label{chal:vi17-c04}
Prove that a decomposition $\Spec A=U\sqcup V$ into two nonempty clopen subsets determines a nontrivial idempotent $e\in A$ with $U=D(e)$ and $V=D(1-e)$.
\end{problem}
