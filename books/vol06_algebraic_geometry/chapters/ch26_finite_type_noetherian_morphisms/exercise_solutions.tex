\paragraph{Solution 1.} If $B=A[b_1,\dots,b_n]$ and $B\twoheadrightarrow C$, then $C=A[\bar b_1,\dots,\bar b_n]$.

\paragraph{Solution 2.} $A_f\cong A[t]/(ft-1)$.

\paragraph{Solution 3.} A finite set of generators for $B/A$ together with one for $C/B$ generates $C/A$.

\paragraph{Solution 4.} The images of $x_1,\dots,x_n$ generate the quotient as an $A$-algebra.

\paragraph{Solution 5.} Take $A$ non-Noetherian and an infinitely generated ideal $I\subset A[x]$; $A[x]/I$ is finite type, while the relation ideal need not be finitely generated.

\paragraph{Solution 6.} By definition the inverse image of an affine target chart has a finite affine cover, hence is quasi-compact.

\paragraph{Solution 7.} An affine morphism is quasi-compact; combine with local finite type.

\paragraph{Solution 8.} Base change gives $\mathbb A^n_{S'}\to S'$, finite type by the affine-space criterion.

\paragraph{Solution 9.} Every ideal of $S^{-1}A$ is the localization of its contraction; the contraction is finitely generated, hence so is the localized ideal.

\paragraph{Solution 10.} Ascending chains or finite generation descend through the quotient map.

\paragraph{Solution 11.} Apply the one-variable Hilbert basis theorem repeatedly.

\paragraph{Solution 12.} Write $B\cong A[x_1,\dots,x_n]/I$; the polynomial ring is Noetherian and quotients preserve Noetherianity.

\paragraph{Solution 13.} $x_{n+1}$ is not in $(x_1,\dots,x_n)$, so each inclusion is strict.

\paragraph{Solution 14.} The open cover by components has no finite subcover.

\paragraph{Solution 15.} Each component has a finite affine finite-type cover; finitely many components give a finite cover of the union.

\paragraph{Solution 16.} Apply stability of finite type under base change along $\Spec\kappa(s)\to S$.

\paragraph{Solution 17.} The affine transitivity calculation proves the claim after refining source and target charts.

\paragraph{Solution 18.} Composition preserves local finite type and quasi-compactness.

\paragraph{Solution 19.} If $B=A[b_i]$, then $B\otimes_AA'=A'[b_i\otimes1]$.

\paragraph{Solution 20.} Base change preserves local finite type and quasi-compactness.

\paragraph{Solution 21.} Intersections with Noetherian affine charts are covered by distinguished opens whose coordinate rings are Noetherian localizations.

\paragraph{Solution 22.} Every open subset of a Noetherian space is quasi-compact.

\paragraph{Solution 23.} Apply the theorem: finite type over the Noetherian scheme $\Spec k$ gives a Noetherian source.

\paragraph{Solution 24.} $\coprod_{n\ge1}\mathbb A^1_k\to\Spec k$ is locally finite type but not quasi-compact.

\paragraph{Solution 25.} $\mathbb A^1_k\to\Spec k$ is finite type, but $k[t]$ is not a finite-dimensional $k$-module, so the morphism is not finite.

\paragraph{Solution 26.} $\mathbb A^1_k$ is finite type yet has infinitely many geometric points when $k$ is infinite; finiteness refers to algebra generators, not cardinality.
