\subsection*{VI.22.P11: Empty restriction criterion}
\begin{problem}\label{prob:vi22-p11}
Let $Z=V(I)\subset\Spec A$.  Prove $Z\cap D(f)=\varnothing$ iff $IA_f=A_f$, equivalently iff $f^n\in I$ for some $n\ge0$.
\par\textbf{Solution.} The restriction is $\Spec(A_f/IA_f)$, empty iff the quotient ring is zero, i.e. $1\in IA_f$.  Clearing denominators gives $f^n\in I$ for some $n$.
\end{problem}
\paragraph{Provenance.} Canonical VI/22 dossier developed from the AG5 T02 theory scope; no numbered legacy problem is claimed.
