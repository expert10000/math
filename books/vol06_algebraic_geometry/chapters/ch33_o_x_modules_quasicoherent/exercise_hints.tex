\textbf{Hint 1.} Take stalks of a finite direct sum.
\textbf{Hint 2.} Use $D(f)$ as the defining affine basis.
\textbf{Hint 3.} Pass to the direct limit over distinguished neighborhoods of $\mathfrak p$.
\textbf{Hint 4.} Localize the quotient module.
\textbf{Hint 5.} A sheaf is zero on an affine open precisely when the corresponding module is zero.
\textbf{Hint 6.} Localization commutes with direct sums.
\textbf{Hint 7.} Check on $D(f)$ and use $(M\otimes N)_f\cong M_f\otimes N_f$.
\textbf{Hint 8.} The stalk is $I_{\mathfrak p}$.
\textbf{Hint 9.} Localize the ring exact sequence at every prime.
\textbf{Hint 10.} Compute the annihilator $(t^3)$.
\textbf{Hint 11.} Both $x$ and $y$ become units on the respective opens.
\textbf{Hint 12.} Locally it is represented by a finite free module.
\textbf{Hint 13.} Use $M_{\mathfrak p}=0$ iff some element outside $\mathfrak p$ annihilates all generators.
\textbf{Hint 14.} The annihilator is $(xy)$.
\textbf{Hint 15.} Reduce to the affine tensor description.
\textbf{Hint 16.} Use $(A/I)\otimes_A M\cong M/IM$.
\textbf{Hint 17.} Use the sheaf gluing axiom.
\textbf{Hint 18.} Apply the affine equivalence.
\textbf{Hint 19.} Apply the equivalence to morphism sets.
\textbf{Hint 20.} Projective modules are locally free after localization.
\textbf{Hint 21.} Use $(A/(f))\otimes_A(A/(g))\cong A/(f,g)$.
\textbf{Hint 22.} Translate the sequence to modules using the affine equivalence.
\textbf{Hint 23.} Work affine-locally over Noetherian rings.
\textbf{Hint 24.} Compare $M_f$ with the degree-zero localization used later on $D_+(f)$.
