\begin{enumerate}
\item The module structure is componentwise and $(\mathcal O_X^{\oplus r})_x\cong\mathcal O_{X,x}^{\oplus r}$.
\item This is the defining formula for the affine module sheaf with $M=A$.
\item The stalk is the direct limit of $M_f$ over $f\notin\mathfrak p$, which is $M_{\mathfrak p}$.
\item $\widetilde{A/I}(D(f))=(A/I)_f\cong A_f/I_f$.
\item The restriction is zero exactly when every element of $A/I$ is killed after inverting a power of $f$.
\item On $D(f)$ both sides equal $(\bigoplus_iM_i)_f\cong\bigoplus_i(M_i)_f$.
\item On $D(f)$ both sides equal $M_f\otimes_{A_f}N_f$.
\item The stalk is $I_{\mathfrak p}\subseteq A_{\mathfrak p}$.
\item Localizing $0\to I\to A\to A/I\to0$ at each prime remains exact, hence the sheaf sequence is exact stalkwise.
\item The annihilator is $(t^3)$, so the support is $V(t)=\{(t)\}$ over a field.
\item Both localizations are zero, because the module is killed by $x$ and by $y$ respectively once those elements are units.
\item On a trivializing open it is $\mathcal O_U^{\oplus r}$, represented by the finitely generated module $A^r$.
\item If $M$ is generated by $m_1,\ldots,m_r$, then $M_{\mathfrak p}=0$ iff a common $s\notin\mathfrak p$ kills all generators, equivalently $\operatorname{Ann}M\not\subseteq\mathfrak p$.
\item The support is $V(xy)=V(x)\cup V(y)$.
\item For $f:\operatorname{Spec}B\to\operatorname{Spec}A$, affine pullback is extension of scalars, giving $B\otimes_A M$.
\item $(A/I)\otimes_AM\cong M/IM$.
\item Quasi-coherence gives affine module models; ordinary sheaf gluing identifies compatible local sections and morphisms uniquely.
\item Under the affine equivalence, $\widetilde M\to\widetilde N$ corresponds uniquely to $M\to N$.
\item Taking morphism sets under an equivalence yields the displayed bijection.
\item A finitely generated projective module becomes free after localizing on a suitable distinguished cover, hence its sheaf is finite locally free.
\item The tensor module is $A/(f,g)$, hence the tensor sheaf is $\widetilde{A/(f,g)}$.
\item Translate the short exact sequence to $A$-modules, where taking the underlying module is exact, then translate back.
\item On each Noetherian affine open, this is the statement that a quotient of a finite module by a finite submodule is finite.
\item Both constructions use localization to turn algebraic modules into sheaves; on $\operatorname{Proj}$ one keeps the degree-zero part of homogeneous localizations.
\end{enumerate}
