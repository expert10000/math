\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E1]
Intersect all closed sets $V(I)$ that contain $\mathfrak p$ and use
$I\subseteq\mathfrak p$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E2]
Substitute the closure formula from E1 into the definition of specialization. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E3]
If $\mathfrak p\in V(I)$ and $\mathfrak p\subseteq\mathfrak q$, compare $I$ with
$\mathfrak q$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E4]
Take complements and use E3. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E5]
For $\mathfrak p\neq\mathfrak q$, choose an element belonging to one prime but not the
other and use a principal open. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E6]
A space is $T_1$ exactly when every singleton is closed. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E7]
The closure of $\mathfrak p$ consists of all primes containing $\mathfrak p$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E8]
Use $A/\mathfrak p$ being a domain for irreducibility, then use E1 and the $T_0$ property. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E9]
Replace $I$ by $\sqrt I$ and use the prime-ideal criterion for irreducibility. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E10]
Apply E9 to $I=(0)$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E11]
Irreducible components are maximal irreducible closed subsets.  Inclusion of $V(\mathfrak p)$
reverses inclusion of primes. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E12]
Use the prime-ideal list $(0),(2),(3),(5),\dots$ and containment. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E13]
Use that $k[t]$ is a PID and nonzero prime ideals are generated by irreducibles and are
maximal. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E14]
Irreducible real polynomials have degree one or two; the quadratic ones have negative
discriminant. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E15]
A quotient by an irreducible polynomial is a field with $p^d$ elements. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E16]
Compute the quotient by $(\bar x)$ and $(\bar y)$, then use containment into
$(\bar x,\bar y)$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E17]
Every nilpotent lies in $(\bar x)$, and the quotient by $(\bar x)$ is $k[y]$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E18]
If $\eta$ were outside $U$, the closed complement would contain the closure of $\eta$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E19]
Use the order-preserving bijection between primes of $A/I$ and primes of $A$ containing
$I$, together with the quotient-spectrum homeomorphism. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}

\begin{hint}
% PEDAGOGY-ENRICHED-VI
[E20]
If $\mathfrak q_1\subseteq\mathfrak q_2$ in $B$, contract both ideals to $A$. Separate generic-point behavior from closed-point behavior by comparing prime ideals, closures, and specialization relations.
\end{hint}
