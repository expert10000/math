\paragraph{Solution 1.} It is a scheme $P$ with projections to $X,Y$ commuting over $S$ and uniquely receiving every compatible pair of maps from a test scheme.
\paragraph{Solution 2.} Transport the projections across the isomorphism; the transported object represents the same functor and is therefore a fiber product.
\paragraph{Solution 3.} Maps $T\to X\times_SS$ are pairs $(a,b)$ with $b=f\circ a$, hence exactly maps $T\to X$.
\paragraph{Solution 4.} The swapped projections define the map; swapping twice preserves both projections, so uniqueness gives the identity.
\paragraph{Solution 5.} $k[x]\otimes_k k[y,z]\cong k[x,y,z]$, hence the product is $\mathbb A^3_k$.
\paragraph{Solution 6.} The tensor product is $A/(I+J)$, so the product is $\Spec A/(I+J)$.
\paragraph{Solution 7.} It is $\Spec(B\otimes_AA_f)\cong\Spec B_{\varphi(f)}$.
\paragraph{Solution 8.} The empty scheme with its unique maps satisfies the pullback universal property.
\paragraph{Solution 9.} $\Delta$ corresponds to $B\otimes_AB\to B$, $b_1\otimes b_2\mapsto b_1b_2$.
\paragraph{Solution 10.} The graph corresponds to $B\otimes_AC\to B$, $b\otimes c\mapsto b f^\#(c)$.
\paragraph{Solution 11.} The equality is $f\circ p_X=g\circ p_Y$, required in the definition.
\paragraph{Solution 12.} After two swaps both components are the original projections, hence the composite equals the identity by uniqueness.
\paragraph{Solution 13.} Each bracketing represents triples $(a,b,c)$ whose composites to $S$ are equal, so they are canonically isomorphic.
\paragraph{Solution 14.} The ring is $k[x,y,z]$.
\paragraph{Solution 15.} The coordinate ring is $A/(I+J+K)$.
\paragraph{Solution 16.} Factorization through $\Delta$ says the ordered pair of components is $(h,h)$, i.e. the components agree.
\paragraph{Solution 17.} Factorization through $\Gamma_f$ says the pair is $(a,f\circ a)$.
\paragraph{Solution 18.} A fixed pair $(x,y)$ can support several primes of $\kappa(x)\otimes_{\kappa(s)}\kappa(y)$.
\paragraph{Solution 19.} For example, a finite separable extension $K/k$ tensored with a splitting field can decompose into a product of fields, producing several spectrum points.
\paragraph{Solution 20.} The maps $B\otimes_AA\to B$, $b\otimes a\mapsto ba$, and $b\mapsto b\otimes1$ are inverse.
\paragraph{Solution 21.} Use $(B\otimes_AC)\otimes_AA_f$ and associativity; each factor localizes compatibly over $A_f$.
\paragraph{Solution 22.} $\mathbb Z[x]\otimes_{\mathbb Z}\mathbb Z[y]\cong\mathbb Z[x,y]$.
\paragraph{Solution 23.} On triple overlaps both possible identifications are projection-compatible isomorphisms between the same pullbacks; uniqueness forces equality.
\paragraph{Solution 24.} A square with upper-left $P$ is Cartesian iff $\Hom(T,P)\cong\Hom(T,X)\times_{\Hom(T,S)}\Hom(T,Y)$ naturally in $T$.
\paragraph{Solution 25.} The point map $\Spec\kappa(s)\to S$ selects $s$ scheme-theoretically; the pullback is therefore the formal fiber object. VI/25 studies it further.
\paragraph{Solution 26.} The ledger reserves CA13-E6/E7 for VI/24 Base Change, so using them as VI/23 legacy anchors would violate the one-destination audit invariant.