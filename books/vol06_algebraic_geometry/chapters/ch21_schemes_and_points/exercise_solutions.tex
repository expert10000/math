\paragraph{E1.} Every prime of $\mathbb F_p[t]$ lies over $(p)$ because the structure map factors through $\mathbb F_p$.
\paragraph{E2.} $(2,t)$ contracts to $(2)$; $(0)$ contracts to $(0)$.
\paragraph{E3.} On a principal neighbourhood the coordinate ring localizes, and the composite to the residue field is the same localization of the germ map.
\paragraph{E4.} The image point has residue field $\mathbb C$; the choices correspond to $\mathbb R$-embeddings $\mathbb C\to\mathbb C$, namely identity and complex conjugation.
\paragraph{E5.} For primes $\mathfrak p,\mathfrak q$, $\mathfrak q$ is a specialization of $\mathfrak p$ exactly when $\mathfrak p\subseteq\mathfrak q$.
\paragraph{E6.} Over $\mathbb R$, $(t^2+1)$ is closed with residue field $\mathbb C$.
\paragraph{E7.} The map $A\to B\to\kappa(\mathfrak q)$ factors through $A_{\mathfrak p}$ and then through $\kappa(\mathfrak p)$, where $\mathfrak p=\mathfrak q\cap A$.
\paragraph{E8.} An isomorphism induces inverse local-ring isomorphisms, hence inverse residue-field maps.
\paragraph{E9.} At $(0)$, $A_{(0)}=\operatorname{Frac}(A)$ and the maximal ideal is zero.
\paragraph{E10.} The maximal ideal $\mathfrak pA_{\mathfrak p}$ contracts to $\mathfrak p$.
\paragraph{E11.} Exactly the primes $\mathfrak q\subseteq\mathfrak p$, i.e. the generalizations of $\mathfrak p$.
\paragraph{E12.} Apply preservation of specialization successively.
\paragraph{E13.} A $k$-algebra map $k[t_1,\dots,t_n]\to k$ is uniquely evaluation at an $n$-tuple.
\paragraph{E14.} The primes $(x)$ and $(y)$ have residue fields $k(y)$ and $k(x)$ respectively.
\paragraph{E15.} Both spectra have one prime, but the first structure sheaf has a nonzero nilpotent germ.
\paragraph{E16.} Both maps from the open subscheme to $\Spec\mathbb Z$ are structure morphisms, hence equal.
\paragraph{E17.} Every point maps to $(p)$; point-set image is the singleton $\{(p)\}$.
\paragraph{E18.} For example take a point with residue field having two embeddings into $K$, such as $\mathbb C/\mathbb R$ with identity and conjugation.
\paragraph{E19.} The field-valued point description gives $\kappa(x)\to k$ compatible with the base field; for a $k$-rational closed point this forces the expected residue-field behavior.
\paragraph{E20.} The claim is a Nullstellensatz consequence and belongs to the dependency chain; no new proof is consumed here.
\paragraph{E21.} Its contraction to $\mathbb Z$ is $(3)$, so its residue characteristic is $3$.
\paragraph{E22.} The stalk maps go $\OO_{Z,gf(x)}\to\OO_{Y,f(x)}\to\OO_{X,x}$; quotienting gives $\kappa(gf(x))\to\kappa(f(x))\to\kappa(x)$.
\paragraph{E23.} The inverse image of any neighbourhood of $x$ is the unique point of $\Spec\kappa(x)$, so the morphism factors through the open restriction.
\paragraph{E24.} If $a\notin\mathfrak p$, its image in $K$ is nonzero and hence invertible, so the universal property of localization applies.
\paragraph{E25.} One must also verify that the affine maps agree on overlaps; uniqueness into the affine target (or the final target here) performs the gluing.
\paragraph{E26.} The chain is $\OO_{X,x}\twoheadrightarrow\kappa(x)$ with kernel $\mathfrak m_x$, and contravariance gives $\Spec\kappa(x)\to X$.
