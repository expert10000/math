% VI21-FULL-SOLUTION-REFINEMENT: P12
\subsection*{VI.21.P12: The generic point morphism}
\begin{problem}\label{prob:vi21-p12}
Let $A$ be a domain.  Describe the canonical morphism attached to the generic point $(0)\in\Spec A$ and identify its residue field.
\end{problem}

\ifdefined\FullProblemDossiers
\paragraph{Why this problem matters.}
The generic point is not an abstract topological artifact: it carries the function field and a canonical field-spectrum morphism.

\paragraph{Useful ideas before starting.}
Translate point statements through local rings and residue fields; when the target is affine, use the VI/19 universal property and then verify the pointwise interpretation.

\paragraph{Guided hints.}
\begin{enumerate}
\item Identify the relevant canonical ring or stalk homomorphism.
\item Track maximal ideals or prime contractions to identify the image point.
\item Use locality to pass to residue fields when needed.
\item Check independence, uniqueness, or functoriality explicitly rather than only on points.
\end{enumerate}

\begin{solution}
Because $A$ is a domain, $(0)$ is prime and is the generic point of $\Spec A$.  Localization at
$(0)$ inverts every nonzero element, so
\[
\OO_{\Spec A,(0)}=A_{(0)}=\operatorname{Frac}(A).
\]
The maximal ideal of this local ring is zero, hence
\[
\kappa((0))=\operatorname{Frac}(A).
\]
The canonical point morphism is therefore
\[
\Spec\operatorname{Frac}(A)\longrightarrow\Spec A
\]
induced contravariantly by the inclusion $A\hookrightarrow\operatorname{Frac}(A)$.  The unique
source prime $(0)$ contracts to $(0)$, so the image is the generic point.  This realizes the
function field as the residue field of the generic point.
\end{solution}

\paragraph{What happened mathematically.}
The generic point is not an abstract topological artifact: it carries the function field and a canonical field-spectrum morphism.

\paragraph{Extensions.}
Compare the generic point morphisms of the irreducible components of a reducible scheme.

\paragraph{Provenance.}
\textbf{Canonical VI/21 derivative/application; not an additional legacy migration.}
\else
\paragraph{Provenance.} \textbf{Canonical VI/21 derivative/application; not an additional legacy migration.}
\fi
