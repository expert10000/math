\begin{problem}[Challenge 5: From rational functions to normalization]\label{chal:vi27-05}
For $A=k[t^2,t^3]$, work entirely inside $K(X)=k(t)$: identify elements integral over $A$, locate the missing element $t$, and predict the affine normalization before VI/28 proves the general theorem.
\end{problem}
