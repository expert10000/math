\chapter{Complex Integration}
\label{ch:iv04}

Complex integration integrates a complex-valued function along an oriented curve. Parameterization, estimates, primitives, and deformation ideas prepare the way for Cauchy's theorem.

\section*{Learning goals}
After this chapter, the reader should be able to:
\begin{itemize}
\item parameterize piecewise smooth contours and compute complex line integrals directly;
\item apply contour reversal, concatenation, and reparameterization rules correctly;
\item estimate contour integrals using the ML inequality;
\item identify primitives and use them to evaluate path-independent integrals;
\item distinguish path independence from mere equality along one family of curves;
\item choose contours adapted to the geometry of the integrand and domain;
\end{itemize}
\section*{Conceptual roadmap}
\[
\boxed{\text{local holomorphic structure}\;\longrightarrow\;\text{integral or series representation}\;\longrightarrow\;\text{global consequence}.}
\]

\section{Parametrized curves}
A piecewise $C^1$ curve $\gamma:[a,b]\to\mathbb C$ carries orientation and tangent data.

% BEGIN VOL04-EXPANSION IV04-example-01
\begin{example}[Parametrizing a line segment]\label{ex:iv04-expand-01}
Let \(\gamma(t)=t(1+i)\) for \(0\le t\le1\). Then \(\gamma'(t)=1+i\), and
\[
\int_\gamma z\,dz=\int_0^1 t(1+i)(1+i)\,dt
=\frac{(1+i)^2}{2}=i.
\]
The calculation makes the definition operational: substitute the parametrization and multiply by its complex velocity.
\end{example}
% END VOL04-EXPANSION IV04-example-01
\section{Contour integral}
Define $\int_\gamma f(z)dz=\int_a^bf(\gamma(t))\gamma'(t)dt$.

\section{Reparameterization}
Orientation-preserving reparameterizations leave contour integrals unchanged; reversing orientation changes the sign.

\section{ML estimate}
$|\int_\gamma fdz|\le L(\gamma)\sup_\gamma|f|$ gives the basic quantitative bound.

% BEGIN VOL04-EXPANSION IV04-example-02
\begin{example}[A circle integral with orientation]\label{ex:iv04-expand-02}
For the positively oriented circle \(\gamma(t)=Re^{it}\), \(0\le t\le2\pi\),
\[
\int_\gamma \overline z\,dz
=\int_0^{2\pi}Re^{-it}\,iRe^{it}\,dt
=2\pi iR^2.
\]
Reversing the orientation changes the sign. The example also shows that closed contour integrals need not vanish for nonholomorphic integrands.
\end{example}
% END VOL04-EXPANSION IV04-example-02
\section{Primitives}
If $F'=f$, then contour integrals of $f$ depend only on endpoints.

\section{Closed curves}
Functions with primitives integrate to zero around every closed curve.

\section{Polygonal and circular contours}
Line segments, circles, and piecewise polygonal paths provide standard computational contours.

% BEGIN VOL04-EXPANSION IV04-example-03
\begin{example}[A primitive makes the path irrelevant]\label{ex:iv04-expand-03}
For \(f(z)=3z^2\), a primitive is \(F(z)=z^3\). Therefore every piecewise smooth path from \(z=-1\) to \(z=2+i\) has
\[
\int_\gamma 3z^2\,dz=F(2+i)-F(-1)=(2+i)^3+1.
\]
No parametrization of the chosen path is needed once a primitive is known.
\end{example}
% END VOL04-EXPANSION IV04-example-03
\section{Deformation preview}
When holomorphicity holds in the intervening region, later theorems will make integrals invariant under suitable contour deformations.

\section{Parameter singularities}
Care is required when curves pass through poles, branch cuts, or points where the integrand is not defined.

\section{Core structural results}

\begin{theorem}[Reparameterization invariance]\label{thm:iv04-01}
An orientation-preserving $C^1$ change of parameter leaves the contour integral unchanged.
\begin{proof}
Substitute the new parameter into the real integral and use the chain rule and ordinary change-of-variables formula.
\end{proof}
\end{theorem}

\begin{theorem}[ML inequality]\label{thm:iv04-02}
If $f$ is continuous on a rectifiable curve $\gamma$, then $|\int_\gamma fdz|\le L(\gamma)\max_\gamma|f|$.
\begin{proof}
Take absolute values in the parametrized integral and bound $|f(\gamma(t))|$ by its maximum; the remaining integral of $|\gamma'|$ is the curve length.
\end{proof}
\end{theorem}

\begin{theorem}[Fundamental theorem for contour integrals]\label{thm:iv04-03}
If $F'=f$ on a domain containing $\gamma$, then $\int_\gamma fdz=F(\gamma(b))-F(\gamma(a))$.
\begin{proof}
Differentiate $F(\gamma(t))$ by the chain rule and integrate the resulting ordinary derivative over the parameter interval.
\end{proof}
\end{theorem}

\begin{theorem}[Primitive criterion on connected domains]\label{thm:iv04-04}
If every closed contour integral of continuous $f$ vanishes in a connected open set, then $f$ has a primitive.
\begin{proof}
Fix a base point and define $F(z)$ by integrating $f$ along any path from the base point to $z$; closed-integral vanishing gives path independence, and a short terminal segment proves $F'=f$.
\end{proof}
\end{theorem}

\section{Worked examples}

\begin{example}[Parametrized curves diagnostic]\label{ex:iv04-worked-01}
Give a rigorous worked analysis of Parametrized curves. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion. A piecewise $C^1$ curve $\gamma:[a,b]\to\mathbb C$ carries orientation and tangent data. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{example}

\begin{example}[Contour integral diagnostic]\label{ex:iv04-worked-02}
Give a rigorous worked analysis of Contour integral. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion. Define $\int_\gamma f(z)dz=\int_a^bf(\gamma(t))\gamma'(t)dt$. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{example}

\begin{example}[Reparameterization diagnostic]\label{ex:iv04-worked-03}
Give a rigorous worked analysis of Reparameterization. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion. Orientation-preserving reparameterizations leave contour integrals unchanged; reversing orientation changes the sign. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{example}

\begin{example}[ML estimate diagnostic]\label{ex:iv04-worked-04}
Give a rigorous worked analysis of ML estimate. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion. $|\int_\gamma fdz|\le L(\gamma)\sup_\gamma|f|$ gives the basic quantitative bound. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{example}

\section{Solved dossiers}
The dossiers are canonical solved problems. The provenance ledger separately records which retained corpus rules guided each topic and which dossiers were newly designed.

\begin{problem}[Parametrized curves diagnostic]\label{prob:iv04-dossier-01}
Give a rigorous worked analysis of Parametrized curves. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion.
\end{problem}
\begin{solution}
A piecewise $C^1$ curve $\gamma:[a,b]\to\mathbb C$ carries orientation and tangent data. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{solution}

\begin{problem}[Contour integral diagnostic]\label{prob:iv04-dossier-02}
Give a rigorous worked analysis of Contour integral. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion.
\end{problem}
\begin{solution}
Define $\int_\gamma f(z)dz=\int_a^bf(\gamma(t))\gamma'(t)dt$. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{solution}

\begin{problem}[Reparameterization diagnostic]\label{prob:iv04-dossier-03}
Give a rigorous worked analysis of Reparameterization. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion.
\end{problem}
\begin{solution}
Orientation-preserving reparameterizations leave contour integrals unchanged; reversing orientation changes the sign. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{solution}

\begin{problem}[ML estimate diagnostic]\label{prob:iv04-dossier-04}
Give a rigorous worked analysis of ML estimate. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion.
\end{problem}
\begin{solution}
$|\int_\gamma fdz|\le L(\gamma)\sup_\gamma|f|$ gives the basic quantitative bound. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{solution}

\begin{problem}[Primitives diagnostic]\label{prob:iv04-dossier-05}
Give a rigorous worked analysis of Primitives. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion.
\end{problem}
\begin{solution}
If $F'=f$, then contour integrals of $f$ depend only on endpoints. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{solution}

\begin{problem}[Closed curves diagnostic]\label{prob:iv04-dossier-06}
Give a rigorous worked analysis of Closed curves. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion.
\end{problem}
\begin{solution}
Functions with primitives integrate to zero around every closed curve. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{solution}

\begin{problem}[Polygonal and circular contours diagnostic]\label{prob:iv04-dossier-07}
Give a rigorous worked analysis of Polygonal and circular contours. State the relevant holomorphic, contour, series, or singularity hypothesis and derive the central conclusion.
\end{problem}
\begin{solution}
Line segments, circles, and piecewise polygonal paths provide standard computational contours. The decisive step is to use the complex-analytic structure globally rather than reason from real-variable intuition alone.
\end{solution}

\begin{problem}[Reparameterization invariance proof dossier]\label{prob:iv04-dossier-08}
Prove the following theorem-level statement and identify the essential hypothesis: An orientation-preserving $C^1$ change of parameter leaves the contour integral unchanged.
\end{problem}
\begin{solution}
Substitute the new parameter into the real integral and use the chain rule and ordinary change-of-variables formula. This identifies the mechanism that makes the complex-analytic conclusion stronger than its real-variable analogue.
\end{solution}

\begin{problem}[ML inequality proof dossier]\label{prob:iv04-dossier-09}
Prove the following theorem-level statement and identify the essential hypothesis: If $f$ is continuous on a rectifiable curve $\gamma$, then $|\int_\gamma fdz|\le L(\gamma)\max_\gamma|f|$.
\end{problem}
\begin{solution}
Take absolute values in the parametrized integral and bound $|f(\gamma(t))|$ by its maximum; the remaining integral of $|\gamma'|$ is the curve length. This identifies the mechanism that makes the complex-analytic conclusion stronger than its real-variable analogue.
\end{solution}

\begin{problem}[Fundamental theorem for contour integrals proof dossier]\label{prob:iv04-dossier-10}
Prove the following theorem-level statement and identify the essential hypothesis: If $F'=f$ on a domain containing $\gamma$, then $\int_\gamma fdz=F(\gamma(b))-F(\gamma(a))$.
\end{problem}
\begin{solution}
Differentiate $F(\gamma(t))$ by the chain rule and integrate the resulting ordinary derivative over the parameter interval. This identifies the mechanism that makes the complex-analytic conclusion stronger than its real-variable analogue.
\end{solution}

\begin{problem}[Primitive criterion on connected domains proof dossier]\label{prob:iv04-dossier-11}
Prove the following theorem-level statement and identify the essential hypothesis: If every closed contour integral of continuous $f$ vanishes in a connected open set, then $f$ has a primitive.
\end{problem}
\begin{solution}
Fix a base point and define $F(z)$ by integrating $f$ along any path from the base point to $z$; closed-integral vanishing gives path independence, and a short terminal segment proves $F'=f$. This identifies the mechanism that makes the complex-analytic conclusion stronger than its real-variable analogue.
\end{solution}

\begin{problem}[Chapter synthesis]\label{prob:iv04-dossier-12}
Connect the definitions and principal theorems of this chapter into one reusable complex-analysis strategy.
\end{problem}
\begin{solution}
Complex integration integrates a complex-valued function along an oriented curve. Parameterization, estimates, primitives, and deformation ideas prepare the way for Cauchy's theorem. The reusable strategy is to identify the analytic domain, choose the correct local expansion or contour representation, apply the structural theorem, and then audit singularities and topology.
\end{solution}

\section{Exercises with complete solutions}

\begin{exercise}\label{exr:iv04-01}
State and apply the central principle behind Parametrized curves. Give one concrete consequence.
\end{exercise}
\begin{hint}
Parameterize each smooth contour segment \(z=\gamma(t)\) and convert \(\int_\Gamma f(z)dz\) to \(\int f(\gamma(t))\gamma'(t)dt\).
\end{hint}
\begin{solution}
A piecewise $C^1$ curve $\gamma:[a,b]\to\mathbb C$ carries orientation and tangent data. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\begin{exercise}\label{exr:iv04-02}
State and apply the central principle behind Contour integral. Give one concrete consequence.
\end{exercise}
\begin{hint}
Reverse orientation by replacing \(t\) with the reversed parameter; this changes the sign of the contour integral.
\end{hint}
\begin{solution}
Define $\int_\gamma f(z)dz=\int_a^bf(\gamma(t))\gamma'(t)dt$. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\begin{exercise}\label{exr:iv04-03}
State and apply the central principle behind Reparameterization. Give one concrete consequence.
\end{exercise}
\begin{hint}
For concatenated paths, split the parameter interval and add the segment integrals.
\end{hint}
\begin{solution}
Orientation-preserving reparameterizations leave contour integrals unchanged; reversing orientation changes the sign. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\begin{exercise}\label{exr:iv04-04}
State and apply the central principle behind ML estimate. Give one concrete consequence.
\end{exercise}
\begin{hint}
Apply the ML bound \(|\int_\Gamma fdz|\le \max_\Gamma|f|\,L(\Gamma)\) after estimating both maximum and length.
\end{hint}
\begin{solution}
$|\int_\gamma fdz|\le L(\gamma)\sup_\gamma|f|$ gives the basic quantitative bound. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\begin{exercise}\label{exr:iv04-05}
State and apply the central principle behind Primitives. Give one concrete consequence.
\end{exercise}
\begin{hint}
If \(F'=f\), evaluate by \(F(\gamma(b))-F(\gamma(a))\); this is the quickest path-independence test.
\end{hint}
\begin{solution}
If $F'=f$, then contour integrals of $f$ depend only on endpoints. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\begin{exercise}\label{exr:iv04-06}
State and apply the central principle behind Closed curves. Give one concrete consequence.
\end{exercise}
\begin{hint}
To disprove path independence, compare integrals around two paths whose difference forms a closed loop enclosing a singularity.
\end{hint}
\begin{solution}
Functions with primitives integrate to zero around every closed curve. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\begin{exercise}\label{exr:iv04-07}
State and apply the central principle behind Polygonal and circular contours. Give one concrete consequence.
\end{exercise}
\begin{hint}
Choose contours so that the integrand or parameterization simplifies on each segment rather than forcing one formula globally.
\end{hint}
\begin{solution}
Line segments, circles, and piecewise polygonal paths provide standard computational contours. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\begin{exercise}\label{exr:iv04-08}
State and apply the central principle behind Deformation preview. Give one concrete consequence.
\end{exercise}
\begin{hint}
Before any contour deformation, list the singularities and make sure none are crossed.
\end{hint}
\begin{solution}
When holomorphicity holds in the intervening region, later theorems will make integrals invariant under suitable contour deformations. The same principle can then be reused in later contour, residue, conformal, or Riemann-surface arguments.
\end{solution}

\section*{Chapter summary}
The chapter now forms part of the canonical complex-analysis chain from holomorphicity through residues, global continuation, and Riemann surfaces.
% BEGIN VOL04-EXPANSION IV04-exercises-01
\section{Graded supplementary exercises}
These exercises extend the protected chapter with a balanced set of computations, proofs, hypothesis tests, applications, and challenges.

\subsection*{Standard computations}

\begin{exercise}[Straight segment]\label{exr:iv04-expand-01}
Evaluate \(\int_\gamma z\,dz\) for \(\gamma(t)=t\), \(0\le t\le2\).
\end{exercise}
\begin{hint}
Substitute the parametrization.
\end{hint}
\begin{solution}
The integral is \(\int_0^2 t\,dt=2\).
\end{solution}

\begin{exercise}[Quarter circle]\label{exr:iv04-expand-02}
Parametrize the counterclockwise quarter circle of radius two from \(2\) to \(2i\).
\end{exercise}
\begin{hint}
Use an exponential parametrization.
\end{hint}
\begin{solution}
A convenient choice is \(\gamma(t)=2e^{it}\) for \(0\le t\le\pi/2\).
\end{solution}

\begin{exercise}[Reverse path]\label{exr:iv04-expand-03}
If \(\int_\gamma f(z)\,dz=3-i\), find the integral over the reversed path.
\end{exercise}
\begin{hint}
Reversal changes the sign of the velocity.
\end{hint}
\begin{solution}
The value is \(-3+i\).
\end{solution}

\begin{exercise}[Primitive evaluation]\label{exr:iv04-expand-04}
Evaluate \(\int_\gamma 2z\,dz\) from \(1\) to \(i\) along any path.
\end{exercise}
\begin{hint}
Use the primitive \(z^2\).
\end{hint}
\begin{solution}
The value is \(i^2-1^2=-2\).
\end{solution}

\begin{exercise}[ML bound]\label{exr:iv04-expand-05}
A contour has length \(5\) and \(|f|\le3\) on it. Bound \(|\int_\gamma f\,dz|\).
\end{exercise}
\begin{hint}
Use the ML inequality.
\end{hint}
\begin{solution}
The integral has modulus at most \(15\).
\end{solution}

\subsection*{Proofs}

\begin{exercise}[Reparametrization]\label{exr:iv04-expand-06}
Prove invariance of a contour integral under an orientation preserving smooth reparametrization.
\end{exercise}
\begin{hint}
Make a one variable substitution in the parameter integral.
\end{hint}
\begin{solution}
If \(t=\phi(s)\) with \(\phi'>0\), then \((\gamma\circ\phi)'=\gamma'(\phi)\phi'\). The ordinary substitution formula gives the same integral.
\end{solution}

\begin{exercise}[Concatenation]\label{exr:iv04-expand-07}
Prove that the integral over a concatenated path is the sum of the two integrals.
\end{exercise}
\begin{hint}
Split the parameter interval at the joining time.
\end{hint}
\begin{solution}
After a standard reparametrization, the parameter integral splits into two ordinary integrals, one for each component path.
\end{solution}

\begin{exercise}[ML inequality]\label{exr:iv04-expand-08}
Prove \(|\int_\gamma f\,dz|\le ML\) when \(|f|\le M\) and the contour length is \(L\).
\end{exercise}
\begin{hint}
Take absolute values inside the parameter integral.
\end{hint}
\begin{solution}
The triangle inequality gives at most \(\int M|\gamma'(t)|dt=M L\).
\end{solution}

\begin{exercise}[Primitive theorem]\label{exr:iv04-expand-09}
If \(F'=f\), prove \(\int_\gamma f\,dz=F(\gamma(b))-F(\gamma(a))\).
\end{exercise}
\begin{hint}
Differentiate \(F(\gamma(t))\).
\end{hint}
\begin{solution}
The chain rule gives \((F\circ\gamma)'=f(\gamma(t))\gamma'(t)\). The real fundamental theorem of calculus then gives the endpoint difference.
\end{solution}

\subsection*{Counterexamples and hypothesis tests}

\begin{exercise}[Closed does not imply zero]\label{exr:iv04-expand-10}
Give a continuous integrand whose integral around a closed circle is nonzero.
\end{exercise}
\begin{hint}
Use conjugation.
\end{hint}
\begin{solution}
For \(f(z)=\overline z\) on \(|z|=R\), direct parametrization gives \(2\pi iR^2\ne0\).
\end{solution}

\begin{exercise}[Endpoint data alone]\label{exr:iv04-expand-11}
Can endpoint data determine \(\int_\gamma f\,dz\) for an arbitrary continuous \(f\)?
\end{exercise}
\begin{hint}
Path independence requires additional structure such as a primitive.
\end{hint}
\begin{solution}
No. Without a primitive or a relevant holomorphic theorem, different paths with the same endpoints may give different integrals.
\end{solution}

\begin{exercise}[Orientation forgotten]\label{exr:iv04-expand-12}
What error results from reversing a contour but keeping the same integral value?
\end{exercise}
\begin{hint}
Track the sign of the derivative of the reversed parametrization.
\end{hint}
\begin{solution}
Reversal multiplies the integral by \(-1\). Forgetting orientation therefore changes the answer by a sign.
\end{solution}

\subsection*{Applications and investigations}

\begin{exercise}[Arc contribution estimate]\label{exr:iv04-expand-13}
A semicircular arc has radius \(R\), and \(|f(z)|\le C/R^2\) there. Show its integral tends to zero as \(R\to\infty\).
\end{exercise}
\begin{hint}
The arc length is \(\pi R\).
\end{hint}
\begin{solution}
The ML bound is \(\pi R\cdot C/R^2=\pi C/R\to0\). This estimate will be central in real integral contours.
\end{solution}

\begin{exercise}[Path choice]\label{exr:iv04-expand-14}
Why is a polygonal parametrization often useful for numerical contour integration?
\end{exercise}
\begin{hint}
Each segment has a constant derivative.
\end{hint}
\begin{solution}
The integral becomes a sum of ordinary one dimensional integrals on simple intervals, making both quadrature and error control transparent.
\end{solution}

\subsection*{Challenge problems}

\begin{exercise}[Unit circle logarithmic integrand]\label{exr:iv04-expand-15}
Directly parametrize \(\int_{|z|=1} dz/z\).
\end{exercise}
\begin{hint}
Use \(z=e^{it}\).
\end{hint}
\begin{solution}
Then \(dz=ie^{it}dt\), so the integral is \(\int_0^{2\pi}i\,dt=2\pi i\).
\end{solution}

\begin{exercise}[Piecewise primitive]\label{exr:iv04-expand-16}
Suppose a contour crosses a point where a chosen primitive formula changes branch. Why must the integral be split with care?
\end{exercise}
\begin{hint}
A primitive must be single valued and differentiable on a neighborhood of each relevant path piece.
\end{hint}
\begin{solution}
One may use valid local primitives on separate pieces, but endpoint cancellations only work after branch values are chosen consistently at the joins.
\end{solution}

% END VOL04-EXPANSION IV04-exercises-01
