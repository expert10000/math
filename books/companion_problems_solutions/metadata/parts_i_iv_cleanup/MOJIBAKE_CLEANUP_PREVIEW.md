# Conservative mojibake cleanup preview

- mode: **APPLY**

## `books/companion_problems_solutions/chapters/part01_volume_i/chapter.tex`

- suspicious-marker score before: **410**
- suspicious-marker score after: **15**
- changed lines: **95**

### L15

Before:
```text
Ă˘â‚¬â€ť Level Sets of a Cubic Surface
```

After:
```text
— Level Sets of a Cubic Surface
```

### L32

Before:
```text
		is a (not necessarily connected) smooth curve in the $(x,y)$Ă˘â‚¬â€śplane
```

After:
```text
		is a (not necessarily connected) smooth curve in the $(x,y)$–plane
```

### L173

Before:
```text
	oval lying in the $(x,y)$Ă˘â‚¬â€śplane.
```

After:
```text
	oval lying in the $(x,y)$–plane.
```

### L277

Before:
```text
Ă˘â‚¬â€ť Sum of linear maps; images/kernels; idempotents
```

After:
```text
— Sum of linear maps; images/kernels; idempotents
```

### L348

Before:
```text
Ă˘â‚¬â€ť Distance bounds from a disk (triangle inequalities)
```

After:
```text
— Distance bounds from a disk (triangle inequalities)
```

### L404

Before:
```text
Ă˘â‚¬â€ť Common complementary subspace
```

After:
```text
— Common complementary subspace
```

### L444

Before:
```text
Ă˘â‚¬â€ť Fifth roots of unity factorization and a trig identity
```

After:
```text
— Fifth roots of unity factorization and a trig identity
```

### L503

Before:
```text
\section*{Problem 5 Ă˘â‚¬â€ť Solve \(\sin z=2\) in \(\mathbb{C}\)}
```

After:
```text
\section*{Problem 5 — Solve \(\sin z=2\) in \(\mathbb{C}\)}
```

### L588

Before:
```text
Ă˘â‚¬â€ť Which modified lists are bases?
```

After:
```text
— Which modified lists are bases?
```

### L667

Before:
```text
Ă˘â‚¬â€ť Generalised Eigenspace Decomposition Theorem
```

After:
```text
— Generalised Eigenspace Decomposition Theorem
```

### L1008

Before:
```text
Ă˘â‚¬â€ť Descending images, ascending kernels, and rank constraints
```

After:
```text
— Descending images, ascending kernels, and rank constraints
```

### L1096

Before:
```text
Ă˘â‚¬â€ť A real $\lambda$ with $\det(A+\lambda B)\neq 0$; real similarity
```

After:
```text
— A real $\lambda$ with $\det(A+\lambda B)\neq 0$; real similarity
```

### L1139

Before:
```text
Ă˘â‚¬â€ť Concurrency of the altitudes (orthocenter) via vectors
```

After:
```text
— Concurrency of the altitudes (orthocenter) via vectors
```

### L1213

Before:
```text
Ă˘â‚¬â€ť Cross products in a triangle; sine rule; a converse
```

After:
```text
— Cross products in a triangle; sine rule; a converse
```

### L1314

Before:
```text
Ă˘â‚¬â€ť Image, kernel, and solvability of \(Mx=d\) (Vandermonde-type matrix)
```

After:
```text
— Image, kernel, and solvability of \(Mx=d\) (Vandermonde-type matrix)
```

### L1517

Before:
```text
Ă˘â‚¬â€ť Restriction and induced quotient map; block matrices
```

After:
```text
— Restriction and induced quotient map; block matrices
```

### L1570

Before:
```text
Ă˘â‚¬â€ť Dual of \(P\) as sequences; dual maps of \(D\) and \(S\)
```

After:
```text
— Dual of \(P\) as sequences; dual maps of \(D\) and \(S\)
```

### L1624

Before:
```text
Ă˘â‚¬â€ť Nondegenerate skew-symmetric forms and maximal isotropic dimension
```

After:
```text
— Nondegenerate skew-symmetric forms and maximal isotropic dimension
```

### L1670

Before:
```text
Ă˘â‚¬â€ť Medians of a complex triangle are concurrent
```

After:
```text
— Medians of a complex triangle are concurrent
```

### L1757

Before:
```text
Ă˘â‚¬â€ť Solving linear vector equations with \(\times\) and \(\cdot\)
```

After:
```text
— Solving linear vector equations with \(\times\) and \(\cdot\)
```

### L1826

Before:
```text
Ă˘â‚¬â€ť Cyclic basis for a nilpotent map; commuting endomorphisms
```

After:
```text
— Cyclic basis for a nilpotent map; commuting endomorphisms
```

### L1872

Before:
```text
Ă˘â‚¬â€ť Functionals and intersections of kernels
```

After:
```text
— Functionals and intersections of kernels
```

### L1991

Before:
```text
Ă˘â‚¬â€ť Bases for $U$, $W$, $U\cap W$, and a description of $U+W$ in $\mathbb R^5$
```

After:
```text
— Bases for $U$, $W$, $U\cap W$, and a description of $U+W$ in $\mathbb R^5$
```

### L2064

Before:
```text
Ă˘â‚¬â€ť A vector equation with dotĂ˘â‚¬â€śproduct constraints and equality cases
```

After:
```text
— A vector equation with dot–product constraints and equality cases
```

### L2140

Before:
```text
Ă˘â‚¬â€ť LagrangeĂ˘â‚¬â„˘s identity, Jacobi identity, and scalar triple products
```

After:
```text
— Lagrange’s identity, Jacobi identity, and scalar triple products
```

### L2292

Before:
```text
Ă˘â‚¬â€ť Geometric meaning of affine combinations (line and plane)
```

After:
```text
— Geometric meaning of affine combinations (line and plane)
```

### L2353

Before:
```text
Ă˘â‚¬â€ť Characterizing trace; (non)existence of \([\,\alpha,\beta\,]=id\)
```

After:
```text
— Characterizing trace; (non)existence of \([\,\alpha,\beta\,]=id\)
```

### L2424

Before:
```text
Ă˘â‚¬â€ť The subspace of maps sending $Y$ into $Z$
```

After:
```text
— The subspace of maps sending $Y$ into $Z$
```

### L2458

Before:
```text
Ă˘â‚¬â€ť Equivalent characterizations of a direct sum
```

After:
```text
— Equivalent characterizations of a direct sum
```

### L2607

Before:
```text
Ă˘â‚¬â€ť Realizing any \(P\in GL_n(\mathbb F)\) as a product of change-of-basis matrices
```

After:
```text
— Realizing any \(P\in GL_n(\mathbb F)\) as a product of change-of-basis matrices
```

### L2630

Before:
```text
\subsection*{Problem 3 Ă˘â‚¬â€ť Rank factorization and \(\operatorname{rank}(A)=\operatorname{rank}(A^T)\)}
```

After:
```text
\subsection*{Problem 3 — Rank factorization and \(\operatorname{rank}(A)=\operatorname{rank}(A^T)\)}
```

### L2681

Before:
```text
\subsection*{Problem 3 Ă˘â‚¬â€ť Rank factorization and \(\operatorname{rank}(A)=\operatorname{rank}(A^T)\)}
```

After:
```text
\subsection*{Problem 3 — Rank factorization and \(\operatorname{rank}(A)=\operatorname{rank}(A^T)\)}
```

### L2694

Before:
```text
Ă˘â‚¬â€ť The composition operator $\Phi(\theta)=\beta\circ\theta\circ\alpha$
```

After:
```text
— The composition operator $\Phi(\theta)=\beta\circ\theta\circ\alpha$
```

### L2824

Before:
```text
Ă˘â‚¬â€ť Trace zero endomorphisms: zero diagonal form and commutators
```

After:
```text
— Trace zero endomorphisms: zero diagonal form and commutators
```

### L2866

Before:
```text
\subsection*{Problem 6 Ă˘â‚¬â€ť Adjugate identities and \(\operatorname{rk}(\operatorname{adj}A)\)}
```

After:
```text
\subsection*{Problem 6 — Adjugate identities and \(\operatorname{rk}(\operatorname{adj}A)\)}
```

### L3305

Before:
```text
Ă˘â‚¬â€ť Dual bases under basis changes in dimension 4
```

After:
```text
— Dual bases under basis changes in dimension 4
```

### L3341

Before:
```text
\subsection*{Problem 8 Ă˘â‚¬â€ť Trace pairing gives an isomorphism \(M_{n\times m}\cong (M_{m\times n})^*\)}
```

After:
```text
\subsection*{Problem 8 — Trace pairing gives an isomorphism \(M_{n\times m}\cong (M_{m\times n})^*\)}
```

### L3485

Before:
```text
Ă˘â‚¬â€ť A linear system depending on a parameter \(t\)
```

After:
```text
— A linear system depending on a parameter \(t\)
```

### L3638

Before:
```text
Ă˘â‚¬â€ť Completing an orthogonal matrix
```

After:
```text
— Completing an orthogonal matrix
```

### L3681

Before:
```text
\section*{Problem 4 Ă˘â‚¬â€ť Spectral decomposition and $A^{-1}$}
```

After:
```text
\section*{Problem 4 — Spectral decomposition and $A^{-1}$}
```

### L3760

Before:
```text
Ă˘â‚¬â€ť Diagonalise a quadric; minimum distance to the origin
```

After:
```text
— Diagonalise a quadric; minimum distance to the origin
```

### L3976

Before:
```text
Ă˘â‚¬â€ť Determinant factorization by row operations
```

After:
```text
— Determinant factorization by row operations
```

### L4096

Before:
```text
Ă˘â‚¬â€ť Eigenvalues of an upper triangular matrix
```

After:
```text
— Eigenvalues of an upper triangular matrix
```

### L4156

Before:
```text
Ă˘â‚¬â€ť A circulant determinant and its factorization
```

After:
```text
— A circulant determinant and its factorization
```

### L4254

Before:
```text
Ă˘â‚¬â€ť Determinant of an odd-dimensional antisymmetric matrix
```

After:
```text
— Determinant of an odd-dimensional antisymmetric matrix
```

### L4277

Before:
```text
Ă˘â‚¬â€ť Hadamard's inequality (entrywise bound version)
```

After:
```text
— Hadamard's inequality (entrywise bound version)
```

### L5230

Before:
```text
Ă˘â‚¬â€ť Cofactors, inverse, and solving a linear system
```

After:
```text
— Cofactors, inverse, and solving a linear system
```

### L5415

Before:
```text
Ă˘â‚¬â€ť Matrices of a linear map in nonstandard bases (and change of basis)
```

After:
```text
— Matrices of a linear map in nonstandard bases (and change of basis)
```

### L5621

Before:
```text
Ă˘â‚¬â€ť Determinant of a Ă˘â‚¬Ĺ›\(p\) on diagonal, \(1\) elsewhereĂ˘â‚¬ĹĄ matrix
```

After:
```text
— Determinant of a “\(p\) on diagonal, \(1\) elsewhere” matrix
```

### L5672

Before:
```text
Ă˘â‚¬â€ť Eigenvalues of $\alpha^2$ and comparison of eigenspaces
```

After:
```text
— Eigenvalues of $\alpha^2$ and comparison of eigenspaces
```

### L5695

Before:
```text
\subsection*{Problem 6 Ă˘â‚¬â€ť Jordan form and $B^{1000}$}
```

After:
```text
\subsection*{Problem 6 — Jordan form and $B^{1000}$}
```

### L5751

Before:
```text
Ă˘â‚¬â€ť Maximize a cyclic quadratic form under $\sum a_i=0$ and $\sum a_i^2=1$
```

After:
```text
— Maximize a cyclic quadratic form under $\sum a_i=0$ and $\sum a_i^2=1$
```

### L5812

Before:
```text
Ă˘â‚¬â€ť Jordan form of $J_n(\lambda)^k$
```

After:
```text
— Jordan form of $J_n(\lambda)^k$
```

### L5888

Before:
```text
Ă˘â‚¬â€ť Odd-dimensional orthogonal matrices with \(\det=1\) have eigenvalue \(1\)
```

After:
```text
— Odd-dimensional orthogonal matrices with \(\det=1\) have eigenvalue \(1\)
```

### L5923

Before:
```text
Ă˘â‚¬â€ť Invertibility and inverse via elementary row operations
```

After:
```text
— Invertibility and inverse via elementary row operations
```

### L6032

Before:
```text
Ă˘â‚¬â€ť Cayley transform of an anti-Hermitian matrix
```

After:
```text
— Cayley transform of an anti-Hermitian matrix
```

### L6097

Before:
```text
Ă˘â‚¬â€ť Inverses and transposes
```

After:
```text
— Inverses and transposes
```

### L6173

Before:
```text
Ă˘â‚¬â€ť Minimal polynomial over $\mathbb C$ has real coefficients
```

After:
```text
— Minimal polynomial over $\mathbb C$ has real coefficients
```

### L6194

Before:
```text
Ă˘â‚¬â€ť A Hermitian form on $\mathbb C^3$ and a roots-of-unity polarization identity
```

After:
```text
— A Hermitian form on $\mathbb C^3$ and a roots-of-unity polarization identity
```

### L6523

Before:
```text
Ă˘â‚¬â€ť A symmetric matrix of finite order is an involution
```

After:
```text
— A symmetric matrix of finite order is an involution
```

### L6563

Before:
```text
Ă˘â‚¬â€ť Eigenvalues, eigenspace dimensions, diagonalizability
```

After:
```text
— Eigenvalues, eigenspace dimensions, diagonalizability
```

### L6631

Before:
```text
\section*{Problem 13 Ă˘â‚¬â€ť Characteristic polynomial of \(A^{-1}\)}
```

After:
```text
\section*{Problem 13 — Characteristic polynomial of \(A^{-1}\)}
```

### L6961

Before:
```text
Ă˘â‚¬â€ť ModulusĂ˘â‚¬â€śargument forms on the unit circle; a Cayley transform locus
```

After:
```text
— Modulus–argument forms on the unit circle; a Cayley transform locus
```

### L6977

Before:
```text
\noindent\textbf{(i) ModulusĂ˘â‚¬â€śargument of \(1+z\).}
```

After:
```text
\noindent\textbf{(i) Modulus–argument of \(1+z\).}
```

### L6987

Before:
```text
\noindent\textbf{(ii) ModulusĂ˘â‚¬â€śargument of \(1-\bar z\).}
```

After:
```text
\noindent\textbf{(ii) Modulus–argument of \(1-\bar z\).}
```

### L7025

Before:
```text
\noindent\textbf{(i) ModulusĂ˘â‚¬â€śargument of \(1+z\).}
```

After:
```text
\noindent\textbf{(i) Modulus–argument of \(1+z\).}
```

### L7035

Before:
```text
\noindent\textbf{(ii) ModulusĂ˘â‚¬â€śargument of \(1-\bar z\).}
```

After:
```text
\noindent\textbf{(ii) Modulus–argument of \(1-\bar z\).}
```

### L7071

Before:
```text
Ă˘â‚¬â€ť Non-similarity of unipotent matrices; identify a given matrix
```

After:
```text
— Non-similarity of unipotent matrices; identify a given matrix
```

### L7160

Before:
```text
Ă˘â‚¬â€ť Eigenvalues and determinant of the ``$\lambda$ on diagonal, $1$ elsewhere'' matrix
```

After:
```text
— Eigenvalues and determinant of the ``$\lambda$ on diagonal, $1$ elsewhere'' matrix
```

### L7298

Before:
```text
Ă˘â‚¬â€ť Eigenvalues/eigenspaces; simultaneous diagonalisation
```

After:
```text
— Eigenvalues/eigenspaces; simultaneous diagonalisation
```

### L7436

Before:
```text
Ă˘â‚¬â€ť One eigenvalue, not diagonalizable
```

After:
```text
— One eigenvalue, not diagonalizable
```

### L7517

Before:
```text
Ă˘â‚¬â€ť Anti-hermitian and unitary matrices: eigenvalues and orthogonality
```

After:
```text
— Anti-hermitian and unitary matrices: eigenvalues and orthogonality
```

### L7627

Before:
```text
Ă˘â‚¬â€ť Column operations on a block matrix and \(\det(AB)=\det(A)\det(B)\)
```

After:
```text
— Column operations on a block matrix and \(\det(AB)=\det(A)\det(B)\)
```

### L7902

Before:
```text
Ă˘â‚¬â€ť Congruence to the identity over $\mathbb R,\mathbb C,\mathbb Q$
```

After:
```text
— Congruence to the identity over $\mathbb R,\mathbb C,\mathbb Q$
```

### L8024

Before:
```text
Ă˘â‚¬â€ť Change of basis to a Jordan block; compute $A^n$
```

After:
```text
— Change of basis to a Jordan block; compute $A^n$
```

### L8164

Before:
```text
Ă˘â‚¬â€ť Basic identities for Hermitian matrices and unitary similarity
```

After:
```text
— Basic identities for Hermitian matrices and unitary similarity
```

### L8282

Before:
```text
Ă˘â‚¬â€ť Char.\ and minimal polynomials do not determine JNF in $4\times 4$
```

After:
```text
— Char.\ and minimal polynomials do not determine JNF in $4\times 4$
```

### L8644

Before:
```text
Ă˘â‚¬â€ť Diagonalising a rotated quadratic form
```

After:
```text
— Diagonalising a rotated quadratic form
```

### L8758

Before:
```text
Ă˘â‚¬â€ť Vandermonde determinant; evaluation functionals; Lagrange dual basis
```

After:
```text
— Vandermonde determinant; evaluation functionals; Lagrange dual basis
```

### L8819

Before:
```text
Ă˘â‚¬â€ť Change-of-basis matrix of a linear map
```

After:
```text
— Change-of-basis matrix of a linear map
```

### L8890

Before:
```text
Ă˘â‚¬â€ť Determinant of a circulant matrix
```

After:
```text
— Determinant of a circulant matrix
```

### L9097

Before:
```text
Ă˘â‚¬â€ť Normal operators and an orthonormal eigenbasis
```

After:
```text
— Normal operators and an orthonormal eigenbasis
```

### L9172

Before:
```text
Ă˘â‚¬â€ť Cayley--Hamilton for $2\times 2$, powers of a specific matrix
```

After:
```text
— Cayley--Hamilton for $2\times 2$, powers of a specific matrix
```

### L9328

Before:
```text
Ă˘â‚¬â€ť Rank and signature of quadratic forms; diagonalization to $\pm 1$
```

After:
```text
— Rank and signature of quadratic forms; diagonalization to $\pm 1$
```

### L9386

Before:
```text
\section{Problem 3 Ă˘â‚¬â€ť The Frobenius trace inner product and the quadratic form $\mathrm{tr}(A^2)$}
```

After:
```text
\section{Problem 3 — The Frobenius trace inner product and the quadratic form $\mathrm{tr}(A^2)$}
```

### L9595

Before:
```text
Ă˘â‚¬â€ť Eigenvalues/eigenvectors; reality + orthogonality \(\Leftrightarrow\) Hermitian
```

After:
```text
— Eigenvalues/eigenvectors; reality + orthogonality \(\Leftrightarrow\) Hermitian
```

### L9705

Before:
```text
Ă˘â‚¬â€ť Orthonormal eigenbases and quadric surfaces
```

After:
```text
— Orthonormal eigenbases and quadric surfaces
```

### L9837

Before:
```text
Ă˘â‚¬â€ť Operator norm: basic and useful properties
```

After:
```text
— Operator norm: basic and useful properties
```

### L9873

Before:
```text
Finite-dimensional Riesz: $\exists a$ with $Ax=\langle a,x\rangle$ and by CauchyĂ˘â‚¬â€śSchwarz
```

After:
```text
Finite-dimensional Riesz: $\exists a$ with $Ax=\langle a,x\rangle$ and by Cauchy–Schwarz
```

### L9902

Before:
```text
	\par\noindent\textbullet\quad RankĂ˘â‚¬â€śone matrix $A=uv^\top$: $\|A\|_{op}=\|u\|\,\|v\|=\|A\|_F$ (right equality case).
```

After:
```text
	\par\noindent\textbullet\quad Rank–one matrix $A=uv^\top$: $\|A\|_{op}=\|u\|\,\|v\|=\|A\|_F$ (right equality case).
```

### L9955

Before:
```text
Ă˘â‚¬â€ť Apollonius circle; a coaxal family and orthogonal intersections
```

After:
```text
— Apollonius circle; a coaxal family and orthogonal intersections
```

### L10115

Before:
```text
Ă˘â‚¬â€ť An $L^2[-1,1]$ inner product on $P_n$ and Legendre-type eigenvectors
```

After:
```text
— An $L^2[-1,1]$ inner product on $P_n$ and Legendre-type eigenvectors
```

### L10603

Before:
```text
Ă˘â‚¬â€ť Spherical unit vectors form an orthonormal right-handed frame
```

After:
```text
— Spherical unit vectors form an orthonormal right-handed frame
```

### L10758

Before:
```text
Ă˘â‚¬â€ť Positive definite operators: square roots and polar decomposition
```

After:
```text
— Positive definite operators: square roots and polar decomposition
```

### L10840

Before:
```text
Ă˘â‚¬â€ť A positive definite quadratic form; orthonormal basis; Gram--Schmidt
```

After:
```text
— A positive definite quadratic form; orthonormal basis; Gram--Schmidt
```

## `books/companion_problems_solutions/chapters/part02_volume_ii/chapter.tex`

- suspicious-marker score before: **1208**
- suspicious-marker score after: **185**
- changed lines: **246**

### L263

Before:
```text
Ă˘â‚¬â€ť Complete metric on $(0,1]$ with the usual topology
```

After:
```text
— Complete metric on $(0,1]$ with the usual topology
```

### L487

Before:
```text
Ă˘â‚¬â€ť Dual of a normed space is Banach; dual of the completion
```

After:
```text
— Dual of a normed space is Banach; dual of the completion
```

### L632

Before:
```text
Ă˘â‚¬â€ť Slices of a distribution and smooth approximation
```

After:
```text
— Slices of a distribution and smooth approximation
```

### L659

Before:
```text
Ă˘â‚¬â€ť Expanding maps on a compact space are onto
```

After:
```text
— Expanding maps on a compact space are onto
```

### L964

Before:
```text
	\par\noindent\textbullet\quad Order $m$: depends on derivatives up to order $m$ Ă˘â‚¬â€ť increasingly singular.
```

After:
```text
	\par\noindent\textbullet\quad Order $m$: depends on derivatives up to order $m$ — increasingly singular.
```

### L1088

Before:
```text
Ă˘â‚¬â€ť Global diffeomorphism under $\|Df - I\| \le \mu < 1$
```

After:
```text
— Global diffeomorphism under $\|Df - I\| \le \mu < 1$
```

### L1111

Before:
```text
\textbf{(TwoĂ˘â‚¬â€śsided Lipschitz via line integration).}\;
```

After:
```text
\textbf{(Two–sided Lipschitz via line integration).}\;
```

### L1158

Before:
```text
Then $f$ is not locally invertible near $0$, and the twoĂ˘â‚¬â€śsided Lipschitz bound can fail.
```

After:
```text
Then $f$ is not locally invertible near $0$, and the two–sided Lipschitz bound can fail.
```

### L1237

Before:
```text
	\par\noindent\textbullet\quad FastĂ˘â‚¬â€śCauchy subsequence trick: from a Cauchy sequence choose a subsequence with
```

After:
```text
	\par\noindent\textbullet\quad Fast–Cauchy subsequence trick: from a Cauchy sequence choose a subsequence with
```

### L1771

Before:
```text
The goal is to guide you through a proof of the StoneĂ˘â‚¬â€śWeierstrass (Bernstein) theorem in one variable:
```

After:
```text
The goal is to guide you through a proof of the Stone–Weierstrass (Bernstein) theorem in one variable:
```

### L1985

Before:
```text
	\textbf{Bounded \& equicontinuous $\Rightarrow$ totally bounded (Arzel\`aĂ˘â‚¬â€śAscoli).}
```

After:
```text
	\textbf{Bounded \& equicontinuous $\Rightarrow$ totally bounded (Arzel\`a–Ascoli).}
```

### L1998

Before:
```text
	On $[a,b]$, any bounded, equicontinuous family in $C([a,b])$ has a uniformly convergent subsequence (Arzel\`aĂ˘â‚¬â€śAscoli).
```

After:
```text
	On $[a,b]$, any bounded, equicontinuous family in $C([a,b])$ has a uniformly convergent subsequence (Arzel\`a–Ascoli).
```

### L2306

Before:
```text
Ă˘â‚¬â€ť Extending a uniform polynomial limit from an annulus
```

After:
```text
— Extending a uniform polynomial limit from an annulus
```

### L2570

Before:
```text
	\par\noindent\textbullet\quad On a finiteĂ˘â‚¬â€śmeasure set (here length $1$): $\displaystyle \|f\|_{1}\le \|f\|_{\infty}$.
```

After:
```text
	\par\noindent\textbullet\quad On a finite–measure set (here length $1$): $\displaystyle \|f\|_{1}\le \|f\|_{\infty}$.
```

### L2573

Before:
```text
	\par\noindent\textbullet\quad Uniform limits of bounded RiemannĂ˘â‚¬â€śintegrable functions are RiemannĂ˘â‚¬â€śintegrable (boundedness preserved).
```

After:
```text
	\par\noindent\textbullet\quad Uniform limits of bounded Riemann–integrable functions are Riemann–integrable (boundedness preserved).
```

### L2574

Before:
```text
	\par\noindent\textbullet\quad $C([0,1])$ is dense in $L^{1}([0,1])$; the $\|\cdot\|_{1}$Ă˘â‚¬â€ścompletion of $C([0,1])$ is $L^{1}$.
```

After:
```text
	\par\noindent\textbullet\quad $C([0,1])$ is dense in $L^{1}([0,1])$; the $\|\cdot\|_{1}$–completion of $C([0,1])$ is $L^{1}$.
```

### L2581

Before:
```text
(Quickies) Ă˘â‚¬â€ť Statements and Theory
```

After:
```text
(Quickies) — Statements and Theory
```

### L2689

Before:
```text
Ă˘â‚¬â€ť Positive distributions have order $0$
```

After:
```text
— Positive distributions have order $0$
```

### L2965

Before:
```text
Ă˘â‚¬â€ť Positive separation
```

After:
```text
— Positive separation
```

### L3207

Before:
```text
Ă˘â‚¬â€ť Which duals contain $u_f$ for $f(x)=e^x\cos(e^x)$?
```

After:
```text
— Which duals contain $u_f$ for $f(x)=e^x\cos(e^x)$?
```

### L3225

Before:
```text
Here $|f(x)|\le e^x$, so the growth is exponential. To see nonĂ˘â‚¬â€śtemperateness directly, set
```

After:
```text
Here $|f(x)|\le e^x$, so the growth is exponential. To see non–temperateness directly, set
```

### L3316

Before:
```text
	then discontinuities would produce separated image piecesĂ˘â‚¬â€ťcontradicting path-connectedness.
```

After:
```text
	then discontinuities would produce separated image pieces—contradicting path-connectedness.
```

### L3497

Before:
```text
For the constantĂ˘â‚¬â€ścoefficient elliptic operator $-\Delta+I$ on $\mathbb{R}^n$
```

After:
```text
For the constant–coefficient elliptic operator $-\Delta+I$ on $\mathbb{R}^n$
```

### L3601

Before:
```text
	\section*{Normal, Tychonoff ($T_{3\frac12}$), and SorgenfreyĂ˘â‚¬â€ťexamples and counterexamples}
```

After:
```text
	\section*{Normal, Tychonoff ($T_{3\frac12}$), and Sorgenfrey—examples and counterexamples}
```

### L3619

Before:
```text
		\par\noindent\textbullet\quad \textbf{Sorgenfrey line} $\mathbb{S}$ (base $[a,b)$): $T_6$ (perfectly normal), first countable, separable; not second countable, not LindelÄ‚Â¶f.
```

After:
```text
		\par\noindent\textbullet\quad \textbf{Sorgenfrey line} $\mathbb{S}$ (base $[a,b)$): $T_6$ (perfectly normal), first countable, separable; not second countable, not Lindelöf.
```

### L3719

Before:
```text
\noindent\textbf{Problem 3 Ă˘â‚¬â€ť Path-connected $\Rightarrow$ Connected.}
```

After:
```text
\noindent\textbf{Problem 3 — Path-connected $\Rightarrow$ Connected.}
```

### L3768

Before:
```text
\noindent\textbf{Problem 4 Ă˘â‚¬â€ť Path-components form an equivalence.}
```

After:
```text
\noindent\textbf{Problem 4 — Path-components form an equivalence.}
```

### L3817

Before:
```text
\par\medskip\noindent\textbf{Problem 5 Ă˘â‚¬â€ť Basic rules of multivariable differentiation}\par
```

After:
```text
\par\medskip\noindent\textbf{Problem 5 — Basic rules of multivariable differentiation}\par
```

### L3849

Before:
```text
\textbf{Theory (FrÄ‚Â©chet derivative).}
```

After:
```text
\textbf{Theory (Fréchet derivative).}
```

### L3940

Before:
```text
		Then, by the innerĂ˘â‚¬â€śproduct product rule, for $h=(h_1,h_2)$,
```

After:
```text
		Then, by the inner–product product rule, for $h=(h_1,h_2)$,
```

### L3969

Before:
```text
	\par\medskip\noindent\textbf{Fr\'echet derivative Ă˘â‚¬â€ť full picture}\par
```

After:
```text
	\par\medskip\noindent\textbf{Fr\'echet derivative — full picture}\par
```

### L4078

Before:
```text
	\textbf{G\^ateaux but not Fr\'echet Ă˘â‚¬â€ť two examples.}
```

After:
```text
	\textbf{G\^ateaux but not Fr\'echet — two examples.}
```

### L4114

Before:
```text
	\par\medskip\noindent\textbf{Problem 6 Ă˘â‚¬â€ť Connectedness and Path-connectedness in $\mathbb{R}^2$}\par
```

After:
```text
	\par\medskip\noindent\textbf{Problem 6 — Connectedness and Path-connectedness in $\mathbb{R}^2$}\par
```

### L4207

Before:
```text
Ă˘â‚¬â€ť Strict contractions on complete vs compact spaces; non-expansive maps
```

After:
```text
— Strict contractions on complete vs compact spaces; non-expansive maps
```

### L4235

Before:
```text
\noindent\textbf{Part 1 Ă˘â‚¬â€ť Strictly contractive, complete, no fixed point (example).}
```

After:
```text
\noindent\textbf{Part 1 — Strictly contractive, complete, no fixed point (example).}
```

### L4265

Before:
```text
\noindent\textbf{Part 2 Ă˘â‚¬â€ť Compact $X\subset\mathbb{R}^n$: existence and uniqueness.}
```

After:
```text
\noindent\textbf{Part 2 — Compact $X\subset\mathbb{R}^n$: existence and uniqueness.}
```

### L4303

Before:
```text
\noindent\textbf{Part 3 Ă˘â‚¬â€ť Non-expansive maps ($\le$) need not have fixed points.}
```

After:
```text
\noindent\textbf{Part 3 — Non-expansive maps ($\le$) need not have fixed points.}
```

### L4321

Before:
```text
Ă˘â‚¬â€ť Statements and Theory
```

After:
```text
— Statements and Theory
```

### L4465

Before:
```text
Ă˘â‚¬â€ť Smoothness and bounds for $\widehat f$ with compact support
```

After:
```text
— Smoothness and bounds for $\widehat f$ with compact support
```

### L4477

Before:
```text
Ă˘â‚¬â€ť Distributions with zero derivative are constants
```

After:
```text
— Distributions with zero derivative are constants
```

### L4638

Before:
```text
	Hence one of $A,B$ is emptyĂ˘â‚¬â€ťcontradiction. So $K$ is connected.
```

After:
```text
	Hence one of $A,B$ is empty—contradiction. So $K$ is connected.
```

### L4670

Before:
```text
	\par\medskip\noindent\textbf{Problem 8 Ă˘â‚¬â€ť Antipodal Points on the Circle (BorsukĂ˘â‚¬â€śUlam in 1D)}\par
```

After:
```text
	\par\medskip\noindent\textbf{Problem 8 — Antipodal Points on the Circle (Borsuk–Ulam in 1D)}\par
```

### L4675

Before:
```text
Ă˘â‚¬â€ť A bounded $L^1$ sequence with no weakly convergent subsequence
```

After:
```text
— A bounded $L^1$ sequence with no weakly convergent subsequence
```

### L4714

Before:
```text
Ă˘â‚¬â€ť Compactness and sequential compactness in $C[0,1]$
```

After:
```text
— Compactness and sequential compactness in $C[0,1]$
```

### L4779

Before:
```text
	\par\noindent\textbullet\quad The family $X$ is bounded and closed but not equicontinuous; by the ArzelÄ‚Â Ă˘â‚¬â€śAscoli theorem it is not relatively compact.
```

After:
```text
	\par\noindent\textbullet\quad The family $X$ is bounded and closed but not equicontinuous; by the Arzelà–Ascoli theorem it is not relatively compact.
```

### L4942

Before:
```text
	\par\noindent\textbullet\quad Show that $\widehat{f} \in C^{\infty}(\mathbb{R}^{n})$ and that for any multiĂ˘â‚¬â€śindex $\alpha$,
```

After:
```text
	\par\noindent\textbullet\quad Show that $\widehat{f} \in C^{\infty}(\mathbb{R}^{n})$ and that for any multi–index $\alpha$,
```

### L5010

Before:
```text
Since each derivative is obtained as an $L^1$Ă˘â‚¬â€ślimit under the integral,
```

After:
```text
Since each derivative is obtained as an $L^1$–limit under the integral,
```

### L5500

Before:
```text
	is $\,\mathbb{Z}^2$Ă˘â‚¬â€śperiodic and hence factors through the quotient, yielding a homeomorphism
```

After:
```text
	is $\,\mathbb{Z}^2$–periodic and hence factors through the quotient, yielding a homeomorphism
```

### L5688

Before:
```text
Ă˘â‚¬â€ť StepĂ˘â‚¬â€śfunction limits by polynomials
```

After:
```text
— Step–function limits by polynomials
```

### L5715

Before:
```text
(ii) Let $\Omega=\{ \Re z>0\}\cup\{ \Re z<0\}=\mathbb C\setminus i\mathbb R$ and define $f\equiv1$ on the right halfĂ˘â‚¬â€śplane and $f\equiv0$ on the left. Again $\mathbb C\setminus\Omega$ is connected, so Runge yields polynomials $Q_n$ converging to $f$ on compacta; the diagonal argument gives the claimed pointwise limits.
```

After:
```text
(ii) Let $\Omega=\{ \Re z>0\}\cup\{ \Re z<0\}=\mathbb C\setminus i\mathbb R$ and define $f\equiv1$ on the right half–plane and $f\equiv0$ on the left. Again $\mathbb C\setminus\Omega$ is connected, so Runge yields polynomials $Q_n$ converging to $f$ on compacta; the diagonal argument gives the claimed pointwise limits.
```

### L5774

Before:
```text
\section*{Problem 5 Ă˘â‚¬â€ť Continued Fractions for $100/37$ and $\sqrt{7}$}
```

After:
```text
\section*{Problem 5 — Continued Fractions for $100/37$ and $\sqrt{7}$}
```

### L6128

Before:
```text
	\par\noindent\textbullet\quad \textbf{Boundedness of a subset of $X$} Ă˘â‚¬â€ť \emph{Not topological.}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Boundedness of a subset of $X$} — \emph{Not topological.}
```

### L6132

Before:
```text
	\par\noindent\textbullet\quad \textbf{Closedness of a subset of $X$} Ă˘â‚¬â€ť \emph{Topological.}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Closedness of a subset of $X$} — \emph{Topological.}
```

### L6136

Before:
```text
	\par\noindent\textbullet\quad \textbf{Ă˘â‚¬Ĺ›Closed and boundedĂ˘â‚¬ĹĄ for a subset of $X$} Ă˘â‚¬â€ť \emph{Not topological.}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{“Closed and bounded” for a subset of $X$} — \emph{Not topological.}
```

### L6141

Before:
```text
	\par\noindent\textbullet\quad \textbf{Total boundedness of $X$} Ă˘â‚¬â€ť \emph{Not topological.}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Total boundedness of $X$} — \emph{Not topological.}
```

### L6147

Before:
```text
	\par\noindent\textbullet\quad \textbf{Completeness of $X$} Ă˘â‚¬â€ť \emph{Not topological.}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Completeness of $X$} — \emph{Not topological.}
```

### L6153

Before:
```text
	\par\noindent\textbullet\quad \textbf{Ă˘â‚¬Ĺ›$X$ is complete and totally bounded$\,\,$Ă˘â‚¬ĹĄ} Ă˘â‚¬â€ť \emph{Topological (for metric spaces).}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{“$X$ is complete and totally bounded$\,\,$”} — \emph{Topological (for metric spaces).}
```

### L6160

Before:
```text
	(Neither completeness nor total boundedness alone is topological; see (iv)Ă˘â‚¬â€ś(v).)
```

After:
```text
	(Neither completeness nor total boundedness alone is topological; see (iv)–(v).)
```

### L6258

Before:
```text
Ă˘â‚¬â€ť Polynomial (vs.\ rational) approximation on a punctured disc
```

After:
```text
— Polynomial (vs.\ rational) approximation on a punctured disc
```

### L6682

Before:
```text
	By HahnĂ˘â‚¬â€śBanach, separate $x$ from $A_k$ with $\varphi_k\in X'$, contradicting
```

After:
```text
	By Hahn–Banach, separate $x$ from $A_k$ with $\varphi_k\in X'$, contradicting
```

### L6711

Before:
```text
		\par\noindent\textbullet\quad \emph{Optimization:} Direct methodĂ˘â‚¬â€ťweak limits of minimizing sequences can be averaged to obtain strong convergence.
```

After:
```text
		\par\noindent\textbullet\quad \emph{Optimization:} Direct method—weak limits of minimizing sequences can be averaged to obtain strong convergence.
```

### L6948

Before:
```text
Ă˘â‚¬â€ť Polynomial approximation of $1/z$ on a semicircle
```

After:
```text
— Polynomial approximation of $1/z$ on a semicircle
```

### L7015

Before:
```text
Ă˘â‚¬â€ť Stability of translations and difference quotients
```

After:
```text
— Stability of translations and difference quotients
```

### L7237

Before:
```text
	generate the usual FrÄ‚Â©chet topology. Convergence $f_k\to f$ means
```

After:
```text
	generate the usual Fréchet topology. Convergence $f_k\to f$ means
```

### L7772

Before:
```text
	\par\noindent\textbullet\quad MeanĂ˘â‚¬â€śValue Theorem: $|f(x)-f(t)|\le |x-t|\;\sup|f'|$.
```

After:
```text
	\par\noindent\textbullet\quad Mean–Value Theorem: $|f(x)-f(t)|\le |x-t|\;\sup|f'|$.
```

### L7852

Before:
```text
	and the tail is dominated by a convergent $1/m^{2}$Ă˘â‚¬â€śtype series. Hence by the Weierstrass MĂ˘â‚¬â€śtest,
```

After:
```text
	and the tail is dominated by a convergent $1/m^{2}$–type series. Hence by the Weierstrass M–test,
```

### L7983

Before:
```text
	\section*{Problem 12$^{\ast}$ Ă˘â‚¬â€ť Lebesgue decomposition}
```

After:
```text
	\section*{Problem 12$^{\ast}$ — Lebesgue decomposition}
```

### L8078

Before:
```text
	$\|\cdot\|_{\infty}$ is a norm on $R([0,1])$. If $(f_n)$ is $\|\cdot\|_{\infty}$Ă˘â‚¬â€śCauchy, then
```

After:
```text
	$\|\cdot\|_{\infty}$ is a norm on $R([0,1])$. If $(f_n)$ is $\|\cdot\|_{\infty}$–Cauchy, then
```

### L8079

Before:
```text
	$f_n\to f$ uniformly for some bounded $f$, and uniform limits of RiemannĂ˘â‚¬â€śintegrable functions are
```

After:
```text
	$f_n\to f$ uniformly for some bounded $f$, and uniform limits of Riemann–integrable functions are
```

### L8080

Before:
```text
	RiemannĂ˘â‚¬â€śintegrable; thus $f\in R([0,1])$. Therefore $R([0,1])$ is closed in the complete space of bounded
```

After:
```text
	Riemann–integrable; thus $f\in R([0,1])$. Therefore $R([0,1])$ is closed in the complete space of bounded
```

### L8086

Before:
```text
Ă˘â‚¬â€ť Radial projection to $\partial D$ and continuity in the base point
```

After:
```text
— Radial projection to $\partial D$ and continuity in the base point
```

### L8271

Before:
```text
	\par\medskip\noindent\textbf{Problem 7 Ă˘â‚¬â€ť Intersection of Nested Compact Connected Sets}\par
```

After:
```text
	\par\medskip\noindent\textbf{Problem 7 — Intersection of Nested Compact Connected Sets}\par
```

### L8327

Before:
```text
Ă˘â‚¬â€ť Wiener and Vitali covering lemmas
```

After:
```text
— Wiener and Vitali covering lemmas
```

### L8735

Before:
```text
Ă˘â‚¬â€ť Characterization on a Runge domain
```

After:
```text
— Characterization on a Runge domain
```

### L8742

Before:
```text
RungeĂ˘â‚¬â€śOkaĂ˘â‚¬â€śWeil theorem, holomorphic functions on $U$ can be uniformly approximated on compacts
```

After:
```text
Runge–Oka–Weil theorem, holomorphic functions on $U$ can be uniformly approximated on compacts
```

### L8745

Before:
```text
($\Rightarrow$) For $f\in\mathcal O(U)$ and $K\Subset U$, RungeĂ˘â‚¬â€śOkaĂ˘â‚¬â€śWeil yields a polynomial $P$
```

After:
```text
($\Rightarrow$) For $f\in\mathcal O(U)$ and $K\Subset U$, Runge–Oka–Weil yields a polynomial $P$
```

### L8757

Before:
```text
Ă˘â‚¬â€ť Riesz' Lemma and non-compactness of the unit ball
```

After:
```text
— Riesz' Lemma and non-compactness of the unit ball
```

### L8904

Before:
```text
		giving the RieszĂ˘â‚¬â€ślemma vector at step $k$ without any abstract selection.
```

After:
```text
		giving the Riesz–lemma vector at step $k$ without any abstract selection.
```

### L9419

Before:
```text
				with the FrÄ‚Â©chet topology generated by seminorms
```

After:
```text
				with the Fréchet topology generated by seminorms
```

### L9482

Before:
```text
		FrÄ‚Â©chet topology: $p_{K,m}(\phi) =
```

After:
```text
		Fréchet topology: $p_{K,m}(\phi) =
```

### L9640

Before:
```text
Given $x_1,x_2\in\mathbb{R}^n$, by Picard--LindelÄ‚Â¶f there exist
```

After:
```text
Given $x_1,x_2\in\mathbb{R}^n$, by Picard--Lindelöf there exist
```

### L9647

Before:
```text
(b) Now assume a HÄ‚Â¶lder condition: there are $K>0$ and $\alpha\in(0,1)$ with
```

After:
```text
(b) Now assume a Hölder condition: there are $K>0$ and $\alpha\in(0,1)$ with
```

### L9665

Before:
```text
	\par\noindent\textbullet\quad \textbf{GrÄ‚Â¶nwall (differential form).} If $u\ge0$ is absolutely continuous and
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Grönwall (differential form).} If $u\ge0$ is absolutely continuous and
```

### L9667

Before:
```text
	\par\noindent\textbullet\quad \textbf{Osgood / generalized GrÄ‚Â¶nwall.} If $u'(t)\le Ku(t)^\alpha$ with
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Osgood / generalized Grönwall.} If $u'(t)\le Ku(t)^\alpha$ with
```

### L9683

Before:
```text
By GrÄ‚Â¶nwall, $u(t)\le u(0)e^{Kt}=\|x_1-x_2\|e^{Kt}$.
```

After:
```text
By Grönwall, $u(t)\le u(0)e^{Kt}=\|x_1-x_2\|e^{Kt}$.
```

### L9686

Before:
```text
\emph{(b) HÄ‚Â¶lder case.}
```

After:
```text
\emph{(b) Hölder case.}
```

### L9727

Before:
```text
	For $f(x)=x/\|x\|$ we use the scalarĂ˘â‚¬â€śvector product and the chain rule.
```

After:
```text
	For $f(x)=x/\|x\|$ we use the scalar–vector product and the chain rule.
```

### L9906

Before:
```text
For (c), by SobolevĂ˘â‚¬â€śMorrey,
```

After:
```text
For (c), by Sobolev–Morrey,
```

### L9980

Before:
```text
Ă˘â‚¬â€ť Matrix squaring and local square root
```

After:
```text
— Matrix squaring and local square root
```

### L10245

Before:
```text
	\par\medskip\noindent\textbf{Problem 14 Ă˘â‚¬â€ť Convergence of Series of Functions}\par
```

After:
```text
	\par\medskip\noindent\textbf{Problem 14 — Convergence of Series of Functions}\par
```

### L10361

Before:
```text
Ă˘â‚¬â€ť Differentiation of an integral
```

After:
```text
— Differentiation of an integral
```

### L10415

Before:
```text
Show using the PicardĂ˘â‚¬â€śLindelÄ‚Â¶f theorem that there exists $\varepsilon>0$ such that
```

After:
```text
Show using the Picard–Lindelöf theorem that there exists $\varepsilon>0$ such that
```

### L10442

Before:
```text
	\par\noindent\textbullet\quad \textbf{Picard--LindelÄ‚Â¶f theorem.}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Picard--Lindelöf theorem.}
```

### L10466

Before:
```text
By PicardĂ˘â‚¬â€śLindelÄ‚Â¶f, there exists $\varepsilon>0$ and a unique $C^1$ function
```

After:
```text
By Picard–Lindelöf, there exists $\varepsilon>0$ and a unique $C^1$ function
```

### L10588

Before:
```text
Ă˘â‚¬â€ť Power Series with $\sum a_n$ Convergent
```

After:
```text
— Power Series with $\sum a_n$ Convergent
```

### L10770

Before:
```text
Ă˘â‚¬â€ť Local inversion on the curve $x^3+y^3-3xy=0$
```

After:
```text
— Local inversion on the curve $x^3+y^3-3xy=0$
```

### L10832

Before:
```text
\section*{Problem 14\textsuperscript{*} Ă˘â‚¬â€ť Maximum principles for C\textsuperscript{2} functions}
```

After:
```text
\section*{Problem 14\textsuperscript{*} — Maximum principles for C\textsuperscript{2} functions}
```

### L10939

Before:
```text
Ă˘â‚¬â€ť Differentiability of the determinant function
```

After:
```text
— Differentiability of the determinant function
```

### L11023

Before:
```text
which generalize to FaÄ‚Â  di BrunoĂ˘â‚¬â„˘s formula for arbitrary order.
```

After:
```text
which generalize to Faà di Bruno’s formula for arbitrary order.
```

### L11046

Before:
```text
Ă˘â‚¬â€ť Bounded Derivative and a Classical Oscillating Example
```

After:
```text
— Bounded Derivative and a Classical Oscillating Example
```

### L11217

Before:
```text
Ă˘â‚¬â€ť One partial continuous near $a$ + the other exists at $a$ $\Rightarrow$ differentiability at $a$
```

After:
```text
— One partial continuous near $a$ + the other exists at $a$ $\Rightarrow$ differentiability at $a$
```

### L11264

Before:
```text
Ă˘â‚¬â€ť Periodic GreenĂ˘â‚¬â„˘s function on the circle
```

After:
```text
— Periodic Green’s function on the circle
```

### L11630

Before:
```text
$D^\alpha:H^s\to H^{s-k}=H^{\frac n2+\gamma}$. For $0<\gamma<1$ the (noninteger) MorreyĂ˘â‚¬â€śSobolev embedding yields
```

After:
```text
$D^\alpha:H^s\to H^{s-k}=H^{\frac n2+\gamma}$. For $0<\gamma<1$ the (noninteger) Morrey–Sobolev embedding yields
```

### L11732

Before:
```text
Ă˘â‚¬â€ť Moving points and uniform convergence
```

After:
```text
— Moving points and uniform convergence
```

### L11784

Before:
```text
Ă˘â‚¬â€ť Irrationality of $\sqrt3+\sqrt5$ and $e^2$
```

After:
```text
— Irrationality of $\sqrt3+\sqrt5$ and $e^2$
```

### L11849

Before:
```text
\par\noindent\textbullet\quad If $\mu=m$ and $\nu=\gamma$ is the Cantor (middleĂ˘â‚¬â€śthird) probability measure, then
```

After:
```text
\par\noindent\textbullet\quad If $\mu=m$ and $\nu=\gamma$ is the Cantor (middle–third) probability measure, then
```

### L11885

Before:
```text
Ă˘â‚¬â€ť Minimal-matching distance on unordered $q$-tuples
```

After:
```text
— Minimal-matching distance on unordered $q$-tuples
```

### L11980

Before:
```text
Ă˘â‚¬â€ť A pointwiseĂ˘â‚¬â€śvanishing polynomial sequence outside $0$
```

After:
```text
— A pointwise–vanishing polynomial sequence outside $0$
```

### L12028

Before:
```text
Ă˘â‚¬â€ť Which $(f_n)$ converge uniformly on $X$?
```

After:
```text
— Which $(f_n)$ converge uniformly on $X$?
```

### L12121

Before:
```text
\section*{Problem 4. {L1} on $C([0,1])$ and on bounded RiemannĂ˘â‚¬â€śintegrable functions}
```

After:
```text
\section*{Problem 4. {L1} on $C([0,1])$ and on bounded Riemann–integrable functions}
```

### L12204

Before:
```text
Ă˘â‚¬â€ť Powers preserve uniform convergence
```

After:
```text
— Powers preserve uniform convergence
```

### L12364

Before:
```text
Ă˘â‚¬â€ť Distributions annihilated by a power of $x$
```

After:
```text
— Distributions annihilated by a power of $x$
```

### L12509

Before:
```text
Ă˘â‚¬â€ť Hat functions and piecewiseĂ˘â‚¬â€ślinear interpolation
```

After:
```text
— Hat functions and piecewise–linear interpolation
```

### L12637

Before:
```text
Ă˘â‚¬â€ť Approximate identities
```

After:
```text
— Approximate identities
```

### L12889

Before:
```text
Ă˘â‚¬â€ť Weak-* limits of $\sin(nx)$ and $\sin^2(nx)$
```

After:
```text
— Weak-* limits of $\sin(nx)$ and $\sin^2(nx)$
```

### L13597

Before:
```text
Ă˘â‚¬â€ť Nonzero functionals are open mappings
```

After:
```text
— Nonzero functionals are open mappings
```

### L13720

Before:
```text
	is the triangle inequality for the $L^p$--norm.  HÄ‚Â¶lder's inequality
```

After:
```text
	is the triangle inequality for the $L^p$--norm.  Hölder's inequality
```

### L13893

Before:
```text
Ă˘â‚¬â€ť Moving points vs.\ uniform convergence on $[a,b]$
```

After:
```text
— Moving points vs.\ uniform convergence on $[a,b]$
```

### L13994

Before:
```text
		admits a weakly convergent subsequence (BanachĂ˘â‚¬â€śAlaoglu + reflexivity).
```

After:
```text
		admits a weakly convergent subsequence (Banach–Alaoglu + reflexivity).
```

### L14217

Before:
```text
	\par\medskip\noindent\textbf{Problem 16 Ă˘â‚¬â€ť Implicit Function Theorem via a block map}\par
```

After:
```text
	\par\medskip\noindent\textbf{Problem 16 — Implicit Function Theorem via a block map}\par
```

### L14237

Before:
```text
		This blockĂ˘â‚¬â€śtriangular matrix has
```

After:
```text
		This block–triangular matrix has
```

### L14504

Before:
```text
The summand $e^{-|x+k|^2/2}$ decays superĂ˘â‚¬â€śexponentially in $|k|$, so the series converges absolutely and defines
```

After:
```text
The summand $e^{-|x+k|^2/2}$ decays super–exponentially in $|k|$, so the series converges absolutely and defines
```

### L14505

Before:
```text
a smooth 1Ă˘â‚¬â€śperiodic function on $\mathbb T^n$.
```

After:
```text
a smooth 1–periodic function on $\mathbb T^n$.
```

### L14554

Before:
```text
Ă˘â‚¬â€ť A Schauder basis for $C([-1,1])$ from hat functions
```

After:
```text
— A Schauder basis for $C([-1,1])$ from hat functions
```

### L14574

Before:
```text
	Consequently, for any $f\in C([-1,1])$ the piecewiseĂ˘â‚¬â€ślinear interpolant
```

After:
```text
	Consequently, for any $f\in C([-1,1])$ the piecewise–linear interpolant
```

### L14585

Before:
```text
	\par\noindent\textbullet\quad \textbf{Dyadic refinement and midpoint hats (FaberĂ˘â‚¬â€śSchauder atoms).}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Dyadic refinement and midpoint hats (Faber–Schauder atoms).}
```

### L14596

Before:
```text
	\par\noindent\textbullet\quad \textbf{Telescoping of piecewiseĂ˘â‚¬â€ślinear interpolants.}
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Telescoping of piecewise–linear interpolants.}
```

### L14597

Before:
```text
	Let $g(u)=f(2u-1)$ and let $g_{2^K}$ be the piecewiseĂ˘â‚¬â€ślinear interpolant on the dyadic
```

After:
```text
	Let $g(u)=f(2u-1)$ and let $g_{2^K}$ be the piecewise–linear interpolant on the dyadic
```

### L14665

Before:
```text
Let $g_{2^K}$ be the piecewiseĂ˘â‚¬â€ślinear interpolant on the dyadic grid $\{j/2^K\}$.
```

After:
```text
Let $g_{2^K}$ be the piecewise–linear interpolant on the dyadic grid $\{j/2^K\}$.
```

### L14690

Before:
```text
	\par\noindent\textbullet\quad $f(x)=ax+b$: all detail coefficients $d_{k,m}$ vanish; only the lowĂ˘â‚¬â€śorder terms
```

After:
```text
	\par\noindent\textbullet\quad $f(x)=ax+b$: all detail coefficients $d_{k,m}$ vanish; only the low–order terms
```

### L14695

Before:
```text
The system $\{\phi_n\}$ is a Schauder basis for $C([-1,1])$ (the FaberĂ˘â‚¬â€śSchauder basis):
```

After:
```text
The system $\{\phi_n\}$ is a Schauder basis for $C([-1,1])$ (the Faber–Schauder basis):
```

### L14701

Before:
```text
Ă˘â‚¬â€ť Removing $\mathbb Q$
```

After:
```text
— Removing $\mathbb Q$
```

### L14752

Before:
```text
Ă˘â‚¬â€ť Limsup of Open Dense Sets
```

After:
```text
— Limsup of Open Dense Sets
```

### L14916

Before:
```text
		\par\noindent\textbullet\quad If $\mu=m$ and $\nu=\gamma$ is the Cantor (middleĂ˘â‚¬â€śthird) probability measure, then
```

After:
```text
		\par\noindent\textbullet\quad If $\mu=m$ and $\nu=\gamma$ is the Cantor (middle–third) probability measure, then
```

### L15003

Before:
```text
Ă˘â‚¬â€ť Approximations of a RiemannĂ˘â‚¬â€śIntegrable Function on $[0,1]$
```

After:
```text
— Approximations of a Riemann–Integrable Function on $[0,1]$
```

### L15007

Before:
```text
	\par\noindent\textbullet\quad \textbf{No.} A uniform limit of continuous functions is continuous; a RiemannĂ˘â‚¬â€śintegrable $f$ can be discontinuous.
```

After:
```text
	\par\noindent\textbullet\quad \textbf{No.} A uniform limit of continuous functions is continuous; a Riemann–integrable $f$ can be discontinuous.
```

### L15009

Before:
```text
	\par\noindent\textbullet\quad \textbf{Yes.} Since $f\in\mathcal R[0,1]$, upper/lower sums approximate $f$ in $L^1$. Step functions are $L^1$Ă˘â‚¬â€śapproximable by continuous functions (smooth their jumps in small neighborhoods). Hence there exist continuous $f_n$ with $\int_0^1|f_n-f|\to0$.
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Yes.} Since $f\in\mathcal R[0,1]$, upper/lower sums approximate $f$ in $L^1$. Step functions are $L^1$–approximable by continuous functions (smooth their jumps in small neighborhoods). Hence there exist continuous $f_n$ with $\int_0^1|f_n-f|\to0$.
```

### L15026

Before:
```text
Ă˘â‚¬â€ť Algebraic zeros and coefficients
```

After:
```text
— Algebraic zeros and coefficients
```

### L15034

Before:
```text
(i) If $\sum_{k=0}^m a_k\alpha^k=0$ with $a_k\in\mathbb Z$, then certainly $\alpha$ is a root of a polynomial with rational coefficients. Conversely, if $\sum_{k=0}^m q_k\alpha^k=0$ with $q_k\in\mathbb Q$, multiply by a common denominator to obtain an integerĂ˘â‚¬â€ścoefficient polynomial vanishing at $\alpha$.
```

After:
```text
(i) If $\sum_{k=0}^m a_k\alpha^k=0$ with $a_k\in\mathbb Z$, then certainly $\alpha$ is a root of a polynomial with rational coefficients. Conversely, if $\sum_{k=0}^m q_k\alpha^k=0$ with $q_k\in\mathbb Q$, multiply by a common denominator to obtain an integer–coefficient polynomial vanishing at $\alpha$.
```

### L15141

Before:
```text
Ă˘â‚¬â€ť Uniform Continuity and Boundedness on $[0,\infty)$
```

After:
```text
— Uniform Continuity and Boundedness on $[0,\infty)$
```

### L15329

Before:
```text
Ă˘â‚¬â€ť Can a discontinuous function be uniformly approximated by polynomials?
```

After:
```text
— Can a discontinuous function be uniformly approximated by polynomials?
```

### L15641

Before:
```text
where $q$ is the HÄ‚Â¶lder conjugate of $p$, i.e.\ $\frac1p+\frac1q=1$.
```

After:
```text
where $q$ is the Hölder conjugate of $p$, i.e.\ $\frac1p+\frac1q=1$.
```

### L15686

Before:
```text
\par\noindent\textbullet\quad Applying HÄ‚Â¶lder's inequality to the right-hand side gives
```

After:
```text
\par\noindent\textbullet\quad Applying Hölder's inequality to the right-hand side gives
```

### L15795

Before:
```text
Ă˘â‚¬â€ť Bounded $L^p$ sequence and convergence of means
```

After:
```text
— Bounded $L^p$ sequence and convergence of means
```

### L15818

Before:
```text
	Since $(f_j)$ is bounded in $L^p$, HÄ‚Â¶lder gives
```

After:
```text
	Since $(f_j)$ is bounded in $L^p$, Hölder gives
```

### L15831

Before:
```text
Ă˘â‚¬â€ť Moment conditions force $f\equiv0$
```

After:
```text
— Moment conditions force $f\equiv0$
```

### L15990

Before:
```text
Ă˘â‚¬â€ť Limit at Infinity Implies Uniform Continuity on $[0,\infty)$
```

After:
```text
— Limit at Infinity Implies Uniform Continuity on $[0,\infty)$
```

### L16022

Before:
```text
Ă˘â‚¬â€ť Smooth Approximation of a Periodic $L^p$ Function
```

After:
```text
— Smooth Approximation of a Periodic $L^p$ Function
```

### L16112

Before:
```text
Ă˘â‚¬â€ť Fixed and antipodal points for a diskĂ˘â‚¬â€śextendable circle map
```

After:
```text
— Fixed and antipodal points for a disk–extendable circle map
```

### L16136

Before:
```text
	If $g$ is \emph{nullĂ˘â‚¬â€śhomotopic} (homotopic to a constant), then $\deg g=0$.
```

After:
```text
	If $g$ is \emph{null–homotopic} (homotopic to a constant), then $\deg g=0$.
```

### L16144

Before:
```text
	Hence $g$ is nullĂ˘â‚¬â€śhomotopic and $\deg g=0$.
```

After:
```text
	Hence $g$ is null–homotopic and $\deg g=0$.
```

### L16178

Before:
```text
	If $g$ is nullĂ˘â‚¬â€śhomotopic, then $\deg g=0$.
```

After:
```text
	If $g$ is null–homotopic, then $\deg g=0$.
```

### L16182

Before:
```text
	from $g$ to the constant map $z\mapsto G(0)$. Hence $g$ is nullĂ˘â‚¬â€śhomotopic and $\deg g=0$.
```

After:
```text
	from $g$ to the constant map $z\mapsto G(0)$. Hence $g$ is null–homotopic and $\deg g=0$.
```

### L16308

Before:
```text
	$\|z\|_\infty\ge \|z\|_2\ge \|z\|_1$ (HÄ‚Â¶lder).
```

After:
```text
	$\|z\|_\infty\ge \|z\|_2\ge \|z\|_1$ (Hölder).
```

### L16366

Before:
```text
Ă˘â‚¬â€ť Minkowski functional (gauge) of a convex $0$-neighborhood
```

After:
```text
— Minkowski functional (gauge) of a convex $0$-neighborhood
```

### L16424

Before:
```text
	whose spatial FourierĂ˘â‚¬â€śPlancherel transform is given by
```

After:
```text
	whose spatial Fourier–Plancherel transform is given by
```

### L16432

Before:
```text
Ă˘â‚¬â€ť Prescribing values on a finite independent set
```

After:
```text
— Prescribing values on a finite independent set
```

### L16449

Before:
```text
Ă˘â‚¬â€ť Sign, Heaviside, and principal values
```

After:
```text
— Sign, Heaviside, and principal values
```

### L16572

Before:
```text
\par\noindent\textbullet\quad The HÄ‚Â¶lder conjugate exponent $q$ of $p$ is defined by
```

After:
```text
\par\noindent\textbullet\quad The Hölder conjugate exponent $q$ of $p$ is defined by
```

### L16629

Before:
```text
	Applying HÄ‚Â¶lder ($p=q=2$) yields
```

After:
```text
	Applying Hölder ($p=q=2$) yields
```

### L16823

Before:
```text
\section*{Exercise 11 Ă˘â‚¬â€ť Haar system on $\mathbb{R}$}
```

After:
```text
\section*{Exercise 11 — Haar system on $\mathbb{R}$}
```

### L17124

Before:
```text
Ă˘â‚¬â€ť A Heisenberg-type inequality and uncertainty
```

After:
```text
— A Heisenberg-type inequality and uncertainty
```

### L17222

Before:
```text
	so $1/h$ is unbounded on $E$Ă˘â‚¬â€ťthe same contradiction as above.
```

After:
```text
	so $1/h$ is unbounded on $E$—the same contradiction as above.
```

### L17264

Before:
```text
Ă˘â‚¬â€ť Absolutely Convergent Coefficients and Trig Series
```

After:
```text
— Absolutely Convergent Coefficients and Trig Series
```

### L17277

Before:
```text
(b) Since $|a_m\sin(mx)|\le|a_m|$ and $\sum|a_m|<\infty$, the MĂ˘â‚¬â€śtest yields uniform convergence of
```

After:
```text
(b) Since $|a_m\sin(mx)|\le|a_m|$ and $\sum|a_m|<\infty$, the M–test yields uniform convergence of
```

### L17286

Before:
```text
\section*{Problem 8 Ă˘â‚¬â€ť Uniformity and Continuity of $\displaystyle f(x)=\sum_{m=1}^\infty (x-m)^{-2}$ on $X$}
```

After:
```text
\section*{Problem 8 — Uniformity and Continuity of $\displaystyle f(x)=\sum_{m=1}^\infty (x-m)^{-2}$ on $X$}
```

### L17409

Before:
```text
	\par\medskip\noindent\textbf{Sequences vs Series of Functions Ă˘â‚¬â€ť Definitions}\par
```

After:
```text
	\par\medskip\noindent\textbf{Sequences vs Series of Functions — Definitions}\par
```

### L17444

Before:
```text
	\par\medskip\noindent\textbf{Problem 15 Ă˘â‚¬â€ť The series $\sum_{n=1}^{\infty}(x-n)^{-2}$ on $X=\mathbb{R}\setminus\mathbb{N}$}\par
```

After:
```text
	\par\medskip\noindent\textbf{Problem 15 — The series $\sum_{n=1}^{\infty}(x-n)^{-2}$ on $X=\mathbb{R}\setminus\mathbb{N}$}\par
```

### L17461

Before:
```text
	\section*{Exercise 3 Ă˘â‚¬â€ť Decomposing a bounded functional on $L^{p}$}
```

After:
```text
	\section*{Exercise 3 — Decomposing a bounded functional on $L^{p}$}
```

### L17670

Before:
```text
Ă˘â‚¬â€ť Uniform limit of bounded functions; pointwise limit can be unbounded
```

After:
```text
— Uniform limit of bounded functions; pointwise limit can be unbounded
```

### L17985

Before:
```text
Ă˘â‚¬â€ť Gibbs Phenomenon for a Square Wave
```

After:
```text
— Gibbs Phenomenon for a Square Wave
```

### L18052

Before:
```text
Ă˘â‚¬â€ť Convergence from a separating family of seminorms
```

After:
```text
— Convergence from a separating family of seminorms
```

### L18100

Before:
```text
Suppose $f\in L^p_{\mathrm{loc}}(\mathbb R^n)$ is $\mathbb Z^n$Ă˘â‚¬â€śperiodic and let
```

After:
```text
Suppose $f\in L^p_{\mathrm{loc}}(\mathbb R^n)$ is $\mathbb Z^n$–periodic and let
```

### L18112

Before:
```text
Then $f_\delta\in C^\infty(\mathbb R^n)$ is 1Ă˘â‚¬â€śperiodic and $f_\delta\to f$ in $L^p(\mathbb T^n)$ as $\delta\downarrow0$.
```

After:
```text
Then $f_\delta\in C^\infty(\mathbb R^n)$ is 1–periodic and $f_\delta\to f$ in $L^p(\mathbb T^n)$ as $\delta\downarrow0$.
```

### L18155

Before:
```text
Ă˘â‚¬â€ť Uncountably many transcendental numbers
```

After:
```text
— Uncountably many transcendental numbers
```

### L18354

Before:
```text
Ă˘â‚¬â€ť A meromorphic function and a Fourier series
```

After:
```text
— A meromorphic function and a Fourier series
```

### L18467

Before:
```text
Ă˘â‚¬â€ť Uniform Convergence on $(-1,1)$ with Radius $1$
```

After:
```text
— Uniform Convergence on $(-1,1)$ with Radius $1$
```

### L18477

Before:
```text
has radius $R=\big(\limsup (n^{-2})^{1/n}\big)^{-1}=1$, and by the MĂ˘â‚¬â€śtest (since $\sum 1/n^2<\infty$ and $|x|^n\le1$ on $(-1,1)$) the series converges uniformly on $(-1,1)$.
```

After:
```text
has radius $R=\big(\limsup (n^{-2})^{1/n}\big)^{-1}=1$, and by the M–test (since $\sum 1/n^2<\infty$ and $|x|^n\le1$ on $(-1,1)$) the series converges uniformly on $(-1,1)$.
```

### L18525

Before:
```text
Ă˘â‚¬â€ť Gibbs Phenomenon for a Square Wave
```

After:
```text
— Gibbs Phenomenon for a Square Wave
```

### L18948

Before:
```text
Ă˘â‚¬â€ť Best Denominator $<10$ Approximating $71/49$
```

After:
```text
— Best Denominator $<10$ Approximating $71/49$
```

### L18966

Before:
```text
Ă˘â‚¬â€ť Fourier Series of the Sawtooth Function
```

After:
```text
— Fourier Series of the Sawtooth Function
```

### L19065

Before:
```text
Ă˘â‚¬â€ť Quickies (uniform convergence \& regularity)
```

After:
```text
— Quickies (uniform convergence \& regularity)
```

### L19180

Before:
```text
Ă˘â‚¬â€ť Discontinuities under uniform limits
```

After:
```text
— Discontinuities under uniform limits
```

### L19283

Before:
```text
Ă˘â‚¬â€ť Dirichlet energy and eigenvalues
```

After:
```text
— Dirichlet energy and eigenvalues
```

### L19480

Before:
```text
One may build $p_n$ via Faber polynomials of the arc or via discrete leastĂ˘â‚¬â€śsquares fits on $K$;
```

After:
```text
One may build $p_n$ via Faber polynomials of the arc or via discrete least–squares fits on $K$;
```

### L19497

Before:
```text
Ă˘â‚¬â€ť Norm $\Rightarrow$ weak $\Rightarrow$ weak-* on $X'$
```

After:
```text
— Norm $\Rightarrow$ weak $\Rightarrow$ weak-* on $X'$
```

### L19559

Before:
```text
On $[0,1]$, $f_n(x)=\sin(2\pi n x)$ is bounded in $L^2$ and $f_n\rightharpoonup 0$ (RiemannĂ˘â‚¬â€śLebesgue).
```

After:
```text
On $[0,1]$, $f_n(x)=\sin(2\pi n x)$ is bounded in $L^2$ and $f_n\rightharpoonup 0$ (Riemann–Lebesgue).
```

### L19608

Before:
```text
hence (by reflexivity and RellichĂ˘â‚¬â€śKondrachov) there is a subsequence $u_{k_j}$ with
```

After:
```text
hence (by reflexivity and Rellich–Kondrachov) there is a subsequence $u_{k_j}$ with
```

### L19674

Before:
```text
Ă˘â‚¬â€ť Uniform limits: sums vs products
```

After:
```text
— Uniform limits: sums vs products
```

### L19797

Before:
```text
\par\noindent\textbullet\quad \textbf{ToleranceĂ˘â‚¬â€śdriven truncation.}
```

After:
```text
\par\noindent\textbullet\quad \textbf{Tolerance–driven truncation.}
```

### L19807

Before:
```text
Ă˘â‚¬â€ť Weak convergence, weak $\not\Rightarrow$ strong, and lower semicontinuity of the norm
```

After:
```text
— Weak convergence, weak $\not\Rightarrow$ strong, and lower semicontinuity of the norm
```

### L19883

Before:
```text
Ă˘â‚¬â€ť Separate continuity vs.\ joint continuity; Lipschitz criterion
```

After:
```text
— Separate continuity vs.\ joint continuity; Lipschitz criterion
```

### L19917

Before:
```text
\textbf{(2) Uniform oneĂ˘â‚¬â€śvariable Lipschitz $\Rightarrow$ joint continuity).}\;
```

After:
```text
\textbf{(2) Uniform one–variable Lipschitz $\Rightarrow$ joint continuity).}\;
```

### L20012

Before:
```text
Ă˘â‚¬â€ť Mazur's separation theorem
```

After:
```text
— Mazur's separation theorem
```

### L20126

Before:
```text
	for which Young's and HÄ‚Â¶lder's inequalities hold and for which the
```

After:
```text
	for which Young's and Hölder's inequalities hold and for which the
```

### L20321

Before:
```text
	which is bounded and RiemannĂ˘â‚¬â€śintegrable with $\int_{0}^{1}|f|=0$ but $f\not\equiv 0$.
```

After:
```text
	which is bounded and Riemann–integrable with $\int_{0}^{1}|f|=0$ but $f\not\equiv 0$.
```

### L20443

Before:
```text
Ă˘â‚¬â€ť From $f(nx)\to0$ to $f(t)\to0$
```

After:
```text
— From $f(nx)\to0$ to $f(t)\to0$
```

### L20461

Before:
```text
	$\|z\|_\infty\ge \|z\|_2\ge \|z\|_1$ (HÄ‚Â¶lder).
```

After:
```text
	$\|z\|_\infty\ge \|z\|_2\ge \|z\|_1$ (Hölder).
```

### L20663

Before:
```text
Ă˘â‚¬â€ť Winding number of a polynomial image
```

After:
```text
— Winding number of a polynomial image
```

### L20672

Before:
```text
(i) directly from the definition; (ii) by factoring; (iii) by the dogĂ˘â‚¬â€śwalking lemma.
```

After:
```text
(i) directly from the definition; (ii) by factoring; (iii) by the dog–walking lemma.
```

### L20695

Before:
```text
$(\pi,2\pi)$. Since $\Re\,\gamma(t)>0$ on $(0,1)$, the loop lies in the right halfĂ˘â‚¬â€śplane,
```

After:
```text
$(\pi,2\pi)$. Since $\Re\,\gamma(t)>0$ on $(0,1)$, the loop lies in the right half–plane,
```

### L20713

Before:
```text
\noindent\textbf{(b)(iii) By the dogĂ˘â‚¬â€śwalking lemma.}
```

After:
```text
\noindent\textbf{(b)(iii) By the dog–walking lemma.}
```

### L20989

Before:
```text
\begin{tcolorbox}[title=AbelĂ˘â‚¬â€śDirichlet Cheat-Sheet,colback=white,colframe=black,
```

After:
```text
\begin{tcolorbox}[title=Abel–Dirichlet Cheat-Sheet,colback=white,colframe=black,
```

### L20997

Before:
```text
	\textit{PowerĂ˘â‚¬â€śseries form (Abel transform).} For $x\in\mathbb R$,
```

After:
```text
	\textit{Power–series form (Abel transform).} For $x\in\mathbb R$,
```

### L21027

Before:
```text
\par\noindent\textbullet\quad Scaling $f$ and $g$ yields the general form of HÄ‚Â¶lder's inequality:
```

After:
```text
\par\noindent\textbullet\quad Scaling $f$ and $g$ yields the general form of Hölder's inequality:
```

### L21042

Before:
```text
Ă˘â‚¬â€ť Uniform Limit of Uniformly Continuous Functions
```

After:
```text
— Uniform Limit of Uniformly Continuous Functions
```

### L21067

Before:
```text
yet converge pointwise to the nonĂ˘â‚¬â€śuniformly continuous limit $\sin(x^2)$.
```

After:
```text
yet converge pointwise to the non–uniformly continuous limit $\sin(x^2)$.
```

### L21082

Before:
```text
	$n$ a nonĂ˘â‚¬â€śnegative integer and $a$ an odd integer.
```

After:
```text
	$n$ a non–negative integer and $a$ an odd integer.
```

### L21165

Before:
```text
	When $z,w$ are nonĂ˘â‚¬â€ścolinear, $d(z,w)=|z|+|w| \le d(z,u)+d(u,w)$ since in every case
```

After:
```text
	When $z,w$ are non–colinear, $d(z,w)=|z|+|w| \le d(z,u)+d(u,w)$ since in every case
```

### L21206

Before:
```text
$^\ast$ Ă˘â‚¬â€ť A Nowhere Differentiable Continuous Function
```

After:
```text
$^\ast$ — A Nowhere Differentiable Continuous Function
```

### L21212

Before:
```text
	\par\noindent\textbullet\quad Define $f(x)=\sum_{n=0}^\infty (3/4)^n\varphi(4^n x)$. Since $0\le\varphi\le1$, the Weierstrass MĂ˘â‚¬â€śtest shows uniform convergence, so $f$ is continuous.
```

After:
```text
	\par\noindent\textbullet\quad Define $f(x)=\sum_{n=0}^\infty (3/4)^n\varphi(4^n x)$. Since $0\le\varphi\le1$, the Weierstrass M–test shows uniform convergence, so $f$ is continuous.
```

### L21221

Before:
```text
	and for $n\ge m+1$ we have $4^n\delta_m\in 2\mathbb Z$ so by $2$Ă˘â‚¬â€śperiodicity the difference is $0$.
```

After:
```text
	and for $n\ge m+1$ we have $4^n\delta_m\in 2\mathbb Z$ so by $2$–periodicity the difference is $0$.
```

### L21244

Before:
```text
definition of the WeierstrassĂ˘â‚¬â€śtype nowhere differentiable function
```

After:
```text
definition of the Weierstrass–type nowhere differentiable function
```

### L21380

Before:
```text
Ă˘â‚¬â€ť Fixed points on the open disc
```

After:
```text
— Fixed points on the open disc
```

### L21391

Before:
```text
Automorphisms of the unit disc \(B\subset\mathbb{C}\) are MÄ‚Â¶bius maps
```

After:
```text
Automorphisms of the unit disc \(B\subset\mathbb{C}\) are Möbius maps
```

### L21623

Before:
```text
Ă˘â‚¬â€ť From polynomial accuracy to convergence for all continuous $f$
```

After:
```text
— From polynomial accuracy to convergence for all continuous $f$
```

### L21676

Before:
```text
Ă˘â‚¬â€ť Maximal degree of exactness for an $n$Ă˘â‚¬â€śpoint rule
```

After:
```text
— Maximal degree of exactness for an $n$–point rule
```

### L21684

Before:
```text
	(GaussĂ˘â‚¬â€śLegendre; the $\alpha_j$ are the zeros of $P_n$).
```

After:
```text
	(Gauss–Legendre; the $\alpha_j$ are the zeros of $P_n$).
```

### L21721

Before:
```text
	that no $n$Ă˘â‚¬â€śpoint rule can be exact for all polynomials of degree $\le 2n$.
```

After:
```text
	that no $n$–point rule can be exact for all polynomials of degree $\le 2n$.
```

### L21722

Before:
```text
	Therefore GaussĂ˘â‚¬â€śLegendre (exact to degree $2n-1$) is optimal.
```

After:
```text
	Therefore Gauss–Legendre (exact to degree $2n-1$) is optimal.
```

### L21808

Before:
```text
Ă˘â‚¬â€ť Weak-$^\ast$ limit of difference quotients of distributions
```

After:
```text
— Weak-$^\ast$ limit of difference quotients of distributions
```

### L22081

Before:
```text
Hence $\|\cdot\|_\alpha$ is a norm LipschitzĂ˘â‚¬â€śequivalent to the uniform norm.
```

After:
```text
Hence $\|\cdot\|_\alpha$ is a norm Lipschitz–equivalent to the uniform norm.
```

### L22253

Before:
```text
Ă˘â‚¬â€ť Brouwer $\Rightarrow$ a positive eigenvector
```

After:
```text
— Brouwer $\Rightarrow$ a positive eigenvector
```

### L22440

Before:
```text
Ă˘â‚¬â€ť A Dense Set with SuperĂ˘â‚¬â€śPolynomial Approximations
```

After:
```text
— A Dense Set with Super–Polynomial Approximations
```

### L22510

Before:
```text
Ă˘â‚¬â€ť Alternating Continued Fraction with $a,b>0$
```

After:
```text
— Alternating Continued Fraction with $a,b>0$
```

### L22573

Before:
```text
Ă˘â‚¬â€ť Arbitrarily slow polynomial approximation
```

After:
```text
— Arbitrarily slow polynomial approximation
```

### L22630

Before:
```text
Ă˘â‚¬â€ť A CF for $\tanh y$ and irrationality of $e$
```

After:
```text
— A CF for $\tanh y$ and irrationality of $e$
```

### L22660

Before:
```text
so $r$ rational would force $e$ rationalĂ˘â‚¬â€ťcontradiction.
```

After:
```text
so $r$ rational would force $e$ rational—contradiction.
```

### L22717

Before:
```text
	Starting from the JĂ˘â‚¬â€śfraction for $\tan z$,
```

After:
```text
	Starting from the J–fraction for $\tan z$,
```

### L22730

Before:
```text
Using $\tan(i y)=i\tanh y$ and substituting $z=iy$ in the JĂ˘â‚¬â€śfraction for $\tan$ gives
```

After:
```text
Using $\tan(i y)=i\tanh y$ and substituting $z=iy$ in the J–fraction for $\tan$ gives
```

### L22761

Before:
```text
Ă˘â‚¬â€ť Orthogonality of Chebyshev polynomials
```

After:
```text
— Orthogonality of Chebyshev polynomials
```

### L22849

Before:
```text
Ă˘â‚¬â€ť Interpolation remainder and uniform convergence
```

After:
```text
— Interpolation remainder and uniform convergence
```

### L22938

Before:
```text
Ă˘â‚¬â€ť Uniqueness of the minimax polynomial
```

After:
```text
— Uniqueness of the minimax polynomial
```

### L22986

Before:
```text
$E=\|f-p_1\|_\infty=\|f-p_2\|_\infty$. By the converse equalĂ˘â‚¬â€śripple criterion, there is an alternant
```

After:
```text
$E=\|f-p_1\|_\infty=\|f-p_2\|_\infty$. By the converse equal–ripple criterion, there is an alternant
```

### L23033

Before:
```text
	\par\noindent\textbullet\quad \textbf{GaussĂ˘â‚¬â€śLegendre} (optimal): $n$ nodes $\{x_k\}$ are the zeros of $P_n$; with suitable $A_k>0$ the rule is exact for $\deg\le 2n-1$.
```

After:
```text
	\par\noindent\textbullet\quad \textbf{Gauss–Legendre} (optimal): $n$ nodes $\{x_k\}$ are the zeros of $P_n$; with suitable $A_k>0$ the rule is exact for $\deg\le 2n-1$.
```

### L23109

Before:
```text
Ă˘â‚¬â€ť Chebyshev nodes minimize the sup norm
```

After:
```text
— Chebyshev nodes minimize the sup norm
```

### L23236

Before:
```text
Ă˘â‚¬â€ť ChebyshevĂ˘â‚¬â€śalternation for a sparse cosine series
```

After:
```text
— Chebyshev–alternation for a sparse cosine series
```

### L23381

Before:
```text
Ă˘â‚¬â€ť Degrees must blow up in uniform polynomial approximation
```

After:
```text
— Degrees must blow up in uniform polynomial approximation
```

### L23412

Before:
```text
Ă˘â‚¬â€ť Limit of $L^p$ norms
```

After:
```text
— Limit of $L^p$ norms
```

### L23420

Before:
```text
For $p\ge r$, HÄ‚Â¶lder/Lyapunov interpolation gives
```

After:
```text
For $p\ge r$, Hölder/Lyapunov interpolation gives
```

### L23442

Before:
```text
Ă˘â‚¬â€ť Embeddings, interpolation, and local $L^p$
```

After:
```text
— Embeddings, interpolation, and local $L^p$
```

### L23475

Before:
```text
		This is HÄ‚Â¶lder with exponents $\frac{p}{q}$ and $\frac{p}{p-q}$.
```

After:
```text
		This is Hölder with exponents $\frac{p}{q}$ and $\frac{p}{p-q}$.
```

### L23493

Before:
```text
	For $p<\infty$, apply HÄ‚Â¶lder to $|f|^{q}\cdot 1$ with exponents $\tfrac{p}{q}$ and $\tfrac{p}{p-q}$:
```

After:
```text
	For $p<\infty$, apply Hölder to $|f|^{q}\cdot 1$ with exponents $\tfrac{p}{q}$ and $\tfrac{p}{p-q}$:
```

### L23511

Before:
```text
	With HÄ‚Â¶lder exponents
```

After:
```text
	With Hölder exponents
```

### L23709

Before:
```text
	\textbf{HÄ‚Â¶lder from Young/Jensen.}
```

After:
```text
	\textbf{Hölder from Young/Jensen.}
```

### L23950

Before:
```text
Equality occurs for the HÄ‚Â¶lder extremizer $g(y)=h(y)^{p-1}/\|h\|_{L^p}^{\,p-1}$ (since $h\ge 0$).
```

After:
```text
Equality occurs for the Hölder extremizer $g(y)=h(y)^{p-1}/\|h\|_{L^p}^{\,p-1}$ (since $h\ge 0$).
```

## `books/companion_problems_solutions/chapters/part04_volume_iv/chapter.tex`

- suspicious-marker score before: **333**
- suspicious-marker score after: **0**
- changed lines: **118**

### L265

Before:
```text
\par\noindent\textbullet\quad \textbf{Holomorphic (MĂ¶bius) â€śinversionâ€ť} about $a$ with radius $r$:
```

After:
```text
\par\noindent\textbullet\quad \textbf{Holomorphic (Möbius) “inversion”} about $a$ with radius $r$:
```

### L270

Before:
```text
	This is a MĂ¶bius transformation (holomorphic on $\mathbb C\setminus\{a\}$).
```

After:
```text
	This is a Möbius transformation (holomorphic on $\mathbb C\setminus\{a\}$).
```

### L333

Before:
```text
	exists, where $\Gamma$ is a simple smooth positively oriented curve, $f$ is continuously differentiable on $\Gamma^{\ast}$, and $\Gamma_{\varepsilon}$ is the part of $\Gamma$ that is at least $\varepsilon$â€“away from $\zeta_{0}$.
```

After:
```text
	exists, where $\Gamma$ is a simple smooth positively oriented curve, $f$ is continuously differentiable on $\Gamma^{\ast}$, and $\Gamma_{\varepsilon}$ is the part of $\Gamma$ that is at least $\varepsilon$–away from $\zeta_{0}$.
```

### L512

Before:
```text
\par\noindent\textbullet\quad Use Cauchyâ€™s integral formula to compute
```

After:
```text
\par\noindent\textbullet\quad Use Cauchy’s integral formula to compute
```

### L524

Before:
```text
Cauchyâ€™s integral formula: if $f$ is holomorphic on and inside a simple closed curve $\gamma$, then
```

After:
```text
Cauchy’s integral formula: if $f$ is holomorphic on and inside a simple closed curve $\gamma$, then
```

### L1503

Before:
```text
the general threeâ€“boundaryâ€“points construction described earlier.  In
```

After:
```text
the general three–boundary–points construction described earlier.  In
```

### L1660

Before:
```text
\paragraph{\textbf{Two maps that are both called â€śinversionâ€ť.}}
```

After:
```text
\paragraph{\textbf{Two maps that are both called “inversion”.}}
```

### L1721

Before:
```text
\paragraph{\textbf{Relation between them: â€śreflection + geometric inversionâ€ť.}}
```

After:
```text
\paragraph{\textbf{Relation between them: “reflection + geometric inversion”.}}
```

### L1757

Before:
```text
\subsection*{\textbf{Three common â€śinversionsâ€ť in complex analysis and geometry}}
```

After:
```text
\subsection*{\textbf{Three common “inversions” in complex analysis and geometry}}
```

### L1759

Before:
```text
\paragraph{\textbf{(A) The basic MĂ¶bius inversion (unit-circle inversion).}}
```

After:
```text
\paragraph{\textbf{(A) The basic Möbius inversion (unit-circle inversion).}}
```

### L1784

Before:
```text
\paragraph{\textbf{(C) Holomorphic â€ścircle inversionâ€ť (a MĂ¶bius map).}}
```

After:
```text
\paragraph{\textbf{(C) Holomorphic “circle inversion” (a Möbius map).}}
```

### L1815

Before:
```text
		Unit MĂ¶bius inversion & $J(z)=\dfrac1z$ &
```

After:
```text
		Unit Möbius inversion & $J(z)=\dfrac1z$ &
```

### L1830

Before:
```text
	\subsection*{\textbf{Inversion as a MĂ¶bius transformation (and its geometric cousin)}}
```

After:
```text
	\subsection*{\textbf{Inversion as a Möbius transformation (and its geometric cousin)}}
```

### L1832

Before:
```text
	\paragraph{\textbf{MĂ¶bius transformations.}}
```

After:
```text
	\paragraph{\textbf{Möbius transformations.}}
```

### L1833

Before:
```text
	A MĂ¶bius map is
```

After:
```text
	A Möbius map is
```

### L1843

Before:
```text
	\subsection*{\textbf{1) Where is the simple inversion $1/z$ in MĂ¶bius form?}}
```

After:
```text
	\subsection*{\textbf{1) Where is the simple inversion $1/z$ in Möbius form?}}
```

### L1854

Before:
```text
	\subsection*{\textbf{2) Where is the â€śradius-$r$â€ť holomorphic inversion $r^{2}/z$?}}
```

After:
```text
	\subsection*{\textbf{2) Where is the “radius-$r$” holomorphic inversion $r^{2}/z$?}}
```

### L1896

Before:
```text
	\paragraph{\textbf{Not MĂ¶bius (anti-holomorphic).}}
```

After:
```text
	\paragraph{\textbf{Not Möbius (anti-holomorphic).}}
```

### L1901

Before:
```text
	and it is \emph{not} MĂ¶bius because it depends on $\overline{z}$ (it is anti-holomorphic).
```

After:
```text
	and it is \emph{not} Möbius because it depends on $\overline{z}$ (it is anti-holomorphic).
```

### L1903

Before:
```text
	\paragraph{\textbf{But it becomes MĂ¶bius after composing with a reflection.}}
```

After:
```text
	\paragraph{\textbf{But it becomes Möbius after composing with a reflection.}}
```

### L1963

Before:
```text
	We want to prove: every MĂ¶bius map
```

After:
```text
	We want to prove: every Möbius map
```

### L1977

Before:
```text
	\paragraph{\textbf{Step 2: reduce an arbitrary MĂ¶bius map to generators.}}
```

After:
```text
	\paragraph{\textbf{Step 2: reduce an arbitrary Möbius map to generators.}}
```

### L2145

Before:
```text
	\par\noindent\textbullet\quad \textbf{(iii)} Zeros at $1/n\to 0$ in $D(0,2)\setminus\{-1\}$ force identity with a meromorphic function that has a pole at $-1$ â€” contradiction.
```

After:
```text
	\par\noindent\textbullet\quad \textbf{(iii)} Zeros at $1/n\to 0$ in $D(0,2)\setminus\{-1\}$ force identity with a meromorphic function that has a pole at $-1$ — contradiction.
```

### L2190

Before:
```text
	\par\noindent\textbullet\quad Use Schwarz's Lemma to prove that any conformal equivalence $\Phi:D(0,1)\to D(0,1)$ is given by a MĂ¶bius transformation.
```

After:
```text
	\par\noindent\textbullet\quad Use Schwarz's Lemma to prove that any conformal equivalence $\Phi:D(0,1)\to D(0,1)$ is given by a Möbius transformation.
```

### L2220

Before:
```text
a MĂ¶bius automorphism of the disk.
```

After:
```text
a Möbius automorphism of the disk.
```

### L3066

Before:
```text
By Cauchyâ€™s formula,
```

After:
```text
By Cauchy’s formula,
```

### L3086

Before:
```text
i.e. $f$ is constant on the circle $|z-a|=r$. For such an $r$, Cauchyâ€™s formula for $n\ge1$ gives
```

After:
```text
i.e. $f$ is constant on the circle $|z-a|=r$. For such an $r$, Cauchy’s formula for $n\ge1$ gives
```

### L3128

Before:
```text
	But $\dfrac{z}{z+1}$ has a pole at $z=-1$, whereas $f$ is holomorphic there â€” a contradiction.
```

After:
```text
	But $\dfrac{z}{z+1}$ has a pole at $z=-1$, whereas $f$ is holomorphic there — a contradiction.
```

### L3519

Before:
```text
By Cauchyâ€™s integral formula, if $|z|<1$ then $F(z)=z^{m}$, while if $|z|>1$ then $F(z)=0$.
```

After:
```text
By Cauchy’s integral formula, if $|z|<1$ then $F(z)=z^{m}$, while if $|z|>1$ then $F(z)=0$.
```

### L3561

Before:
```text
By Cauchyâ€™s integral formula,
```

After:
```text
By Cauchy’s integral formula,
```

### L3573

Before:
```text
By Cauchyâ€™s theorem,
```

After:
```text
By Cauchy’s theorem,
```

### L3681

Before:
```text
(Rotationâ€“scaling; holomorphic).
```

After:
```text
(Rotation–scaling; holomorphic).
```

### L3732

Before:
```text
We use: (a) \emph{Liouvilleâ€™s theorem}: a bounded entire function is constant; (b) \emph{Cauchy estimates}:
```

After:
```text
We use: (a) \emph{Liouville’s theorem}: a bounded entire function is constant; (b) \emph{Cauchy estimates}:
```

### L3759

Before:
```text
If $f=u+iv$ with $|u|>|v|$ everywhere, then $\Re\big(f^2\big)=u^2-v^2>0$, so $g:=f^2$ maps $\mathbb C$ into the right halfâ€“plane.
```

After:
```text
If $f=u+iv$ with $|u|>|v|$ everywhere, then $\Re\big(f^2\big)=u^2-v^2>0$, so $g:=f^2$ maps $\mathbb C$ into the right half–plane.
```

### L3792

Before:
```text
Then the Cauchyâ€“Riemann equations hold for $(u,v)$ and $f:=u+iv$ is holomorphic with $\Re f=u$.
```

After:
```text
Then the Cauchy–Riemann equations hold for $(u,v)$ and $f:=u+iv$ is holomorphic with $\Re f=u$.
```

### L3795

Before:
```text
On nonâ€“simply connected domains a closed form need not be exact; a certificate is a nonzero loop integral
```

After:
```text
On non–simply connected domains a closed form need not be exact; a certificate is a nonzero loop integral
```

### L3814

Before:
```text
Hence the Cauchyâ€“Riemann equations hold and
```

After:
```text
Hence the Cauchy–Riemann equations hold and
```

### L3889

Before:
```text
If $f$ omitted $b$ near $a$, then $g=1/(f-b)$ would be bounded near $a$, hence removable there by Riemannâ€™s theorem.
```

After:
```text
If $f$ omitted $b$ near $a$, then $g=1/(f-b)$ would be bounded near $a$, hence removable there by Riemann’s theorem.
```

### L4283

Before:
```text
(Cauchyâ€™s derivative formula via residues).
```

After:
```text
(Cauchy’s derivative formula via residues).
```

### L4290

Before:
```text
\emph{Residue at a higherâ€“order pole.}
```

After:
```text
\emph{Residue at a higher–order pole.}
```

### L4323

Before:
```text
which yields the claimed formula. For $n=0$ this reduces to Cauchyâ€™s integral formula.
```

After:
```text
which yields the claimed formula. For $n=0$ this reduces to Cauchy’s integral formula.
```

### L4330

Before:
```text
â€” area from a $1$-form (what $\tfrac12(x\,dy-y\,dx)$ measures)
```

After:
```text
— area from a $1$-form (what $\tfrac12(x\,dy-y\,dx)$ measures)
```

### L4589

Before:
```text
	\par\noindent\textbullet\quad Using Cauchyâ€™s residue theorem applied to $\Gamma^{(N)}$, show that for $0\le\theta\le 2\pi$,
```

After:
```text
	\par\noindent\textbullet\quad Using Cauchy’s residue theorem applied to $\Gamma^{(N)}$, show that for $0\le\theta\le 2\pi$,
```

### L4628

Before:
```text
Cauchyâ€™s residue theorem states that for such a closed contour,
```

After:
```text
Cauchy’s residue theorem states that for such a closed contour,
```

### L4936

Before:
```text
â€” Solution
```

After:
```text
— Solution
```

### L5100

Before:
```text
By Cauchyâ€™s residue theorem,
```

After:
```text
By Cauchy’s residue theorem,
```

### L5304

Before:
```text
	\subsection*{(a) \(f(x)=e^{-|x|}\) â€” Transform and step-by-step inverse}
```

After:
```text
	\subsection*{(a) \(f(x)=e^{-|x|}\) — Transform and step-by-step inverse}
```

### L5322

Before:
```text
		&&\text{(close upward; Jordanâ€™s lemma)}\\
```

After:
```text
		&&\text{(close upward; Jordan’s lemma)}\\
```

### L5335

Before:
```text
	\subsection*{(b) \(f(x)=e^{-a^{2}x^{2}}\) (\(a>0\)) â€” Transform and step-by-step inverse}
```

After:
```text
	\subsection*{(b) \(f(x)=e^{-a^{2}x^{2}}\) (\(a>0\)) — Transform and step-by-step inverse}
```

### L5885

Before:
```text
Via $e^{2iz}=e^{-2y}e^{2ix}$ a horizontal strip $y\in(y_1,y_2)$ maps to an annulus; the MĂ¶bius map $w\mapsto i\frac{w+1}{w-1}$ then sends annuli to circular bands bounded by the circles $u^2+(v+\coth 2y)^2=\csch^2 2y$.
```

After:
```text
Via $e^{2iz}=e^{-2y}e^{2ix}$ a horizontal strip $y\in(y_1,y_2)$ maps to an annulus; the Möbius map $w\mapsto i\frac{w+1}{w-1}$ then sends annuli to circular bands bounded by the circles $u^2+(v+\coth 2y)^2=\csch^2 2y$.
```

### L6196

Before:
```text
\emph{Higherâ€“order pole.} If $g$ has a pole of order $m$ at $w$,
```

After:
```text
\emph{Higher–order pole.} If $g$ has a pole of order $m$ at $w$,
```

### L6324

Before:
```text
Temperature in a right halfâ€“plane with a removed disk
```

After:
```text
Temperature in a right half–plane with a removed disk
```

### L6335

Before:
```text
	Consider the real-parameter MĂ¶bius map
```

After:
```text
	Consider the real-parameter Möbius map
```

### L6344

Before:
```text
	so the $y$â€“axis maps to the unit circle $|\zeta|=1$.
```

After:
```text
	so the $y$–axis maps to the unit circle $|\zeta|=1$.
```

### L6359

Before:
```text
	the $\cos\theta$â€“dependence must drop out:
```

After:
```text
	the $\cos\theta$–dependence must drop out:
```

### L6387

Before:
```text
	(right halfâ€“plane with the closed disk $\{|z-a|\le b\}$ removed) onto the concentric circular \emph{annulus}
```

After:
```text
	(right half–plane with the closed disk $\{|z-a|\le b\}$ removed) onto the concentric circular \emph{annulus}
```

### L6400

Before:
```text
	Let $U(\zeta)$ denote the temperature in the $\zeta$â€“plane. Harmonicity is preserved by conformal maps, so
```

After:
```text
	Let $U(\zeta)$ denote the temperature in the $\zeta$–plane. Harmonicity is preserved by conformal maps, so
```

### L6459

Before:
```text
	the MĂ¶bius map $\displaystyle \zeta=\frac{z-\alpha}{z+\alpha}$ carries
```

After:
```text
	the Möbius map $\displaystyle \zeta=\frac{z-\alpha}{z+\alpha}$ carries
```

### L6746

Before:
```text
Thus strip $\leftrightarrow$ disk/exterior-disk mappings for $\tanh$ and $\coth$ are just the exponentialâ€™s strip-to-sector equivalences composed with a Cayley map.
```

After:
```text
Thus strip $\leftrightarrow$ disk/exterior-disk mappings for $\tanh$ and $\coth$ are just the exponential’s strip-to-sector equivalences composed with a Cayley map.
```

### L6792

Before:
```text
For each inverse, the principal domain is obtained by slitting the $w$â€“plane along the images of the multiple values.
```

After:
```text
For each inverse, the principal domain is obtained by slitting the $w$–plane along the images of the multiple values.
```

### L6947

Before:
```text
	Center via $\phi_a$ (item 3), then items (11)â€“(12).
```

After:
```text
	Center via $\phi_a$ (item 3), then items (11)–(12).
```

### L6999

Before:
```text
	MĂ¶bius normalizes the circles; then a power map; then Cayley.
```

After:
```text
	Möbius normalizes the circles; then a power map; then Cayley.
```

### L7013

Before:
```text
	\boxed{\,w=z+\frac{1}{z}\,}\ \ \text{then a real MĂ¶bius adjustment}.
```

After:
```text
	\boxed{\,w=z+\frac{1}{z}\,}\ \ \text{then a real Möbius adjustment}.
```

### L7058

Before:
```text
Circles/lines orthogonal to $\partial\mathbb D$ map to \emph{vertical} lines in the $w$â€“plane.
```

After:
```text
Circles/lines orthogonal to $\partial\mathbb D$ map to \emph{vertical} lines in the $w$–plane.
```

### L7082

Before:
```text
is a MĂ¶bius map sending $L_1\!\to L_2$ with
```

After:
```text
is a Möbius map sending $L_1\!\to L_2$ with
```

### L7318

Before:
```text
â€” Conformal mappings
```

After:
```text
— Conformal mappings
```

### L7325

Before:
```text
	from where use MĂ¶bius.
```

After:
```text
	from where use Möbius.
```

### L7351

Before:
```text
		\par\noindent\textbullet\quad \textbf{MĂ¶bius to exterior unit disk.} Use
```

After:
```text
		\par\noindent\textbullet\quad \textbf{Möbius to exterior unit disk.} Use
```

### L7390

Before:
```text
		\par\noindent\textbullet\quad \textbf{Real MĂ¶bius to upper half-plane.}
```

After:
```text
		\par\noindent\textbullet\quad \textbf{Real Möbius to upper half-plane.}
```

### L7439

Before:
```text
	Seek a real-coefficient MĂ¶bius transformation
```

After:
```text
	Seek a real-coefficient Möbius transformation
```

### L7805

Before:
```text
\par\noindent\textbullet\quad Inversion in a circle is \emph{anti-conformal} (it involves complex conjugation), and the composition of two inversions is conformal; in particular, any composition of an \emph{even} number of inversions is holomorphic (hence MĂ¶bius on $\widehat{\mathbb C}$).
```

After:
```text
\par\noindent\textbullet\quad Inversion in a circle is \emph{anti-conformal} (it involves complex conjugation), and the composition of two inversions is conformal; in particular, any composition of an \emph{even} number of inversions is holomorphic (hence Möbius on $\widehat{\mathbb C}$).
```

### L8041

Before:
```text
If $f$ had a primitive on the annulus $0<|z|<1$, every closed integral would vanishâ€”a contradiction. Hence no primitive exists there.
```

After:
```text
If $f$ had a primitive on the annulus $0<|z|<1$, every closed integral would vanish—a contradiction. Hence no primitive exists there.
```

### L8164

Before:
```text
\emph{Cauchyâ€™s root bound (polynomials).}
```

After:
```text
\emph{Cauchy’s root bound (polynomials).}
```

### L8172

Before:
```text
\emph{RouchĂ©â€™s theorem (context).}
```

After:
```text
\emph{Rouché’s theorem (context).}
```

### L8266

Before:
```text
\emph{Example 1 (Eulerâ€™s Beta integral).}
```

After:
```text
\emph{Example 1 (Euler’s Beta integral).}
```

### L8403

Before:
```text
A holomorphic, nowhereâ€“vanishing function $g$ on a domain $D$ admits a holomorphic branch of $\Log g$ on $D$
```

After:
```text
A holomorphic, nowhere–vanishing function $g$ on a domain $D$ admits a holomorphic branch of $\Log g$ on $D$
```

### L8623

Before:
```text
\par\noindent\textbullet\quad Show that a MĂ¶bius map sends the unit disk $\mathbb D=\{z:|z|<1\}$ onto itself iff
```

After:
```text
\par\noindent\textbullet\quad Show that a Möbius map sends the unit disk $\mathbb D=\{z:|z|<1\}$ onto itself iff
```

### L8628

Before:
```text
	\par\noindent\textbullet\quad Find a MĂ¶bius map taking the region between the circles $\{|z|=1\}$ and $\{|z-1|=5/2\}$ to an annulus $\{1<|w|<R\}$.
```

After:
```text
	\par\noindent\textbullet\quad Find a Möbius map taking the region between the circles $\{|z|=1\}$ and $\{|z-1|=5/2\}$ to an annulus $\{1<|w|<R\}$.
```

### L8630

Before:
```text
	\par\noindent\textbullet\quad Find a conformal map from an infinite strip onto an annulus. Can such a map be the restriction of a MĂ¶bius map?
```

After:
```text
	\par\noindent\textbullet\quad Find a conformal map from an infinite strip onto an annulus. Can such a map be the restriction of a Möbius map?
```

### L8632

Before:
```text
A MĂ¶bius map $M(z)=\dfrac{az+b}{cz+d}$ maps lines/circles to lines/circles and preserves angles.
```

After:
```text
A Möbius map $M(z)=\dfrac{az+b}{cz+d}$ maps lines/circles to lines/circles and preserves angles.
```

### L8674

Before:
```text
This cannot be the restriction of a MĂ¶bius map: a MĂ¶bius map sends parallel boundary lines of a strip to two lines or to two circles meeting at one point, whereas the annulus is bounded by two \emph{concentric} circles (which do not meet).
```

After:
```text
This cannot be the restriction of a Möbius map: a Möbius map sends parallel boundary lines of a strip to two lines or to two circles meeting at one point, whereas the annulus is bounded by two \emph{concentric} circles (which do not meet).
```

### L8679

Before:
```text
Temperature in a right halfâ€“plane with a removed disk
```

After:
```text
Temperature in a right half–plane with a removed disk
```

### L8690

Before:
```text
	Consider the real-parameter MĂ¶bius map
```

After:
```text
	Consider the real-parameter Möbius map
```

### L8699

Before:
```text
	so the $y$â€“axis maps to the unit circle $|\zeta|=1$.
```

After:
```text
	so the $y$–axis maps to the unit circle $|\zeta|=1$.
```

### L8714

Before:
```text
	the $\cos\theta$â€“dependence must drop out:
```

After:
```text
	the $\cos\theta$–dependence must drop out:
```

### L8742

Before:
```text
	(right halfâ€“plane with the closed disk $\{|z-a|\le b\}$ removed) onto the concentric circular \emph{annulus}
```

After:
```text
	(right half–plane with the closed disk $\{|z-a|\le b\}$ removed) onto the concentric circular \emph{annulus}
```

### L8755

Before:
```text
	Let $U(\zeta)$ denote the temperature in the $\zeta$â€“plane. Harmonicity is preserved by conformal maps, so
```

After:
```text
	Let $U(\zeta)$ denote the temperature in the $\zeta$–plane. Harmonicity is preserved by conformal maps, so
```

### L8814

Before:
```text
	the MĂ¶bius map $\displaystyle \zeta=\frac{z-\alpha}{z+\alpha}$ carries
```

After:
```text
	the Möbius map $\displaystyle \zeta=\frac{z-\alpha}{z+\alpha}$ carries
```

### L9101

Before:
```text
Thus strip $\leftrightarrow$ disk/exterior-disk mappings for $\tanh$ and $\coth$ are just the exponentialâ€™s strip-to-sector equivalences composed with a Cayley map.
```

After:
```text
Thus strip $\leftrightarrow$ disk/exterior-disk mappings for $\tanh$ and $\coth$ are just the exponential’s strip-to-sector equivalences composed with a Cayley map.
```

### L9208

Before:
```text
	Center via $\phi_a$ (item 3), then items (11)â€“(12).
```

After:
```text
	Center via $\phi_a$ (item 3), then items (11)–(12).
```

### L9260

Before:
```text
	MĂ¶bius normalizes the circles; then a power map; then Cayley.
```

After:
```text
	Möbius normalizes the circles; then a power map; then Cayley.
```

### L9274

Before:
```text
	\boxed{\,w=z+\frac{1}{z}\,}\ \ \text{then a real MĂ¶bius adjustment}.
```

After:
```text
	\boxed{\,w=z+\frac{1}{z}\,}\ \ \text{then a real Möbius adjustment}.
```

### L9319

Before:
```text
Circles/lines orthogonal to $\partial\mathbb D$ map to \emph{vertical} lines in the $w$â€“plane.
```

After:
```text
Circles/lines orthogonal to $\partial\mathbb D$ map to \emph{vertical} lines in the $w$–plane.
```

### L9343

Before:
```text
is a MĂ¶bius map sending $L_1\!\to L_2$ with
```

After:
```text
is a Möbius map sending $L_1\!\to L_2$ with
```

### L9455

Before:
```text
Temperature in a right halfâ€“plane with a removed disk
```

After:
```text
Temperature in a right half–plane with a removed disk
```

### L9466

Before:
```text
	Consider the real-parameter MĂ¶bius map
```

After:
```text
	Consider the real-parameter Möbius map
```

### L9475

Before:
```text
	so the $y$â€“axis maps to the unit circle $|\zeta|=1$.
```

After:
```text
	so the $y$–axis maps to the unit circle $|\zeta|=1$.
```

### L9490

Before:
```text
	the $\cos\theta$â€“dependence must drop out:
```

After:
```text
	the $\cos\theta$–dependence must drop out:
```

### L9518

Before:
```text
	(right halfâ€“plane with the closed disk $\{|z-a|\le b\}$ removed) onto the concentric circular \emph{annulus}
```

After:
```text
	(right half–plane with the closed disk $\{|z-a|\le b\}$ removed) onto the concentric circular \emph{annulus}
```

### L9531

Before:
```text
	Let $U(\zeta)$ denote the temperature in the $\zeta$â€“plane. Harmonicity is preserved by conformal maps, so
```

After:
```text
	Let $U(\zeta)$ denote the temperature in the $\zeta$–plane. Harmonicity is preserved by conformal maps, so
```

### L9590

Before:
```text
	the MĂ¶bius map $\displaystyle \zeta=\frac{z-\alpha}{z+\alpha}$ carries
```

After:
```text
	the Möbius map $\displaystyle \zeta=\frac{z-\alpha}{z+\alpha}$ carries
```

### L9706

Before:
```text
	Center via $\phi_a$ (item 3), then items (11)â€“(12).
```

After:
```text
	Center via $\phi_a$ (item 3), then items (11)–(12).
```

### L9758

Before:
```text
	MĂ¶bius normalizes the circles; then a power map; then Cayley.
```

After:
```text
	Möbius normalizes the circles; then a power map; then Cayley.
```

### L9772

Before:
```text
	\boxed{\,w=z+\frac{1}{z}\,}\ \ \text{then a real MĂ¶bius adjustment}.
```

After:
```text
	\boxed{\,w=z+\frac{1}{z}\,}\ \ \text{then a real Möbius adjustment}.
```

### L9817

Before:
```text
Circles/lines orthogonal to $\partial\mathbb D$ map to \emph{vertical} lines in the $w$â€“plane.
```

After:
```text
Circles/lines orthogonal to $\partial\mathbb D$ map to \emph{vertical} lines in the $w$–plane.
```

### L9841

Before:
```text
is a MĂ¶bius map sending $L_1\!\to L_2$ with
```

After:
```text
is a Möbius map sending $L_1\!\to L_2$ with
```

### L10104

Before:
```text
â€” Disk of radius \(R\)
```

After:
```text
— Disk of radius \(R\)
```

### L10475

Before:
```text
	\emph{Solution.} Using Ex.\ 3â€™s matrix and a short computation:
```

After:
```text
	\emph{Solution.} Using Ex.\ 3’s matrix and a short computation:
```

### L10625

Before:
```text
	This is classical in singularâ€“integral theory (Calder\'onâ€“Zygmund): the Cauchy singular integral operator is bounded on $L^{p}(\Gamma)$ for $1<p<\infty$, and the nontangential boundary limits exist almost everywhere.
```

After:
```text
	This is classical in singular–integral theory (Calder\'on–Zygmund): the Cauchy singular integral operator is bounded on $L^{p}(\Gamma)$ for $1<p<\infty$, and the nontangential boundary limits exist almost everywhere.
```

### L11093

Before:
```text
	Explain why \(a\) cannot be chosen arbitrarily, but is determined by the rectangleâ€™s aspect ratio.
```

After:
```text
	Explain why \(a\) cannot be chosen arbitrarily, but is determined by the rectangle’s aspect ratio.
```

### L11099

Before:
```text
	A rectangle has \(\alpha_k=\tfrac12\) at each vertex, so every factorâ€™s exponent is \(-\tfrac12\).
```

After:
```text
	A rectangle has \(\alpha_k=\tfrac12\) at each vertex, so every factor’s exponent is \(-\tfrac12\).
```

### L11118

Before:
```text
â€” Triangle with vertices \((0,0)\to(2,1)\to(1,3)\to(0,0)\)
```

After:
```text
— Triangle with vertices \((0,0)\to(2,1)\to(1,3)\to(0,0)\)
```

### L11248

Before:
```text
For \(0<a<2\) the Betaâ€“Gamma identity gives
```

After:
```text
For \(0<a<2\) the Beta–Gamma identity gives
```

### L11728

Before:
```text
\emph{Reason.} Using the Mellinâ€“type formula
```

After:
```text
\emph{Reason.} Using the Mellin–type formula
```

### L12475

Before:
```text
â€” constant curl, easy numbers
```

After:
```text
— constant curl, easy numbers
```

### L13453

Before:
```text
By Cauchyâ€™s integral formula for $f(z)=e^{z}$,
```

After:
```text
By Cauchy’s integral formula for $f(z)=e^{z}$,
```

### L13766

Before:
```text
=\dfrac{e^{-1}}{2i}$. Jordanâ€™s lemma gives
```

After:
```text
=\dfrac{e^{-1}}{2i}$. Jordan’s lemma gives
```

### L13825

Before:
```text
â€” gradient field (curl $=0$), circulation $=0$
```

After:
```text
— gradient field (curl $=0$), circulation $=0$
```

## Totals

- changed lines: **459**
- suspicious-marker score before: **1951**
- suspicious-marker score after: **200**

Only lines whose mojibake-marker score strictly decreased were changed.
No problem/solution structure, labels, environments, or file paths are intentionally rewritten.
