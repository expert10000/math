# Companion figures

This directory is reserved for reader-facing plots and diagrams added after the initial mathematical migration pass.

Visuals should be added only when they materially improve a problem or solution. Track every proposed visual in `metadata/COMPANION_PROBLEM_ATLAS.tsv` using `figure_requirement`, `figure_type`, and `figure_status`.
